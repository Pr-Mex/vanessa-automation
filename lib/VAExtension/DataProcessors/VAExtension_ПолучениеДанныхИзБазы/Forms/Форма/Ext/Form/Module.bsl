#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВыполнитьОбработку(Команда)
	ОбработкаВыполнена = Ложь;
	ТекстИсключения = "";
	ЗначениеРезультат = "";
	
	Попытка
		Данные = ПолучитьДанныеСервер();
		ЗначениеРезультат = ЗаписатьОбъектJSON(Данные);
	Исключение
		ТекстИсключения = ОписаниеОшибки();
	КонецПопытки;
	
	ОбработкаВыполнена = Истина;
КонецПроцедуры

&НаКлиенте
Процедура ЗакрытьФорму(Команда)
	Закрыть();
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьПараметры(Команда)
	
	Если НЕ ЗначениеЗаполнено(ДляУстановкиПараметров) Тогда
		Возврат;
	КонецЕсли;
	
	Данные = ПрочитатьОбъектJSON(ДляУстановкиПараметров);
	
	Если Данные.Свойство("ДатаНачала") Тогда
		ДатаНачала = Данные.ДатаНачала;
	КонецЕсли;	
	Если Данные.Свойство("ДатаОкончания") Тогда
		ДатаОкончания = Данные.ДатаОкончания;
	КонецЕсли;	
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Функция ПрочитатьОбъектJSON(ДанныеJSON, ПрочитатьВСоответствие = Ложь)
	
	ЧтениеJSON = New ЧтениеJSON;
	ЧтениеJSON.УстановитьСтроку(ДанныеJSON);
	ИменаСвойствСоЗначениямиДата = Новый Массив;
	ИменаСвойствСоЗначениямиДата.Добавить("ДатаНачала");
	ИменаСвойствСоЗначениямиДата.Добавить("ДатаОкончания");
	Значение = ПрочитатьJSON(ЧтениеJSON, ПрочитатьВСоответствие, ИменаСвойствСоЗначениямиДата);
	ЧтениеJSON.Закрыть();
	Возврат Значение;
	
КонецФункции	 

&НаКлиенте
Функция ЗаписатьОбъектJSON(ДанныеJSON) Экспорт
	#Если НЕ ВебКлиент Тогда

	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();
	ЗаписатьJSON(ЗаписьJSON, ДанныеJSON);
	Возврат ЗаписьJSON.Закрыть();
	
	#КонецЕсли
КонецФункции

&НаСервере
Функция ПолучитьДанныеСервер()
	
	Результат = Новый Массив;
	
	Если Тип = "Перечисление" Тогда
		Если Метаданные.Перечисления.Найти(Вид) = Неопределено Тогда
			Результат = Новый Структура;
			Результат.Вставить("ОшибкаНетОбъектаМетаданных", Истина);
			Результат.Вставить("ВсеОбъекты", ВсеПеречисления());
			Возврат Результат;
		КонецЕсли;
		
		ОМ = Метаданные.Перечисления[Вид];
		Для Каждого Элем Из ОМ.ЗначенияПеречисления  Цикл
			ДанныеЗначения = Новый Структура;
			ДанныеЗначения.Вставить("Имя", Элем.Имя);
			ДанныеЗначения.Вставить("Представление", Элем.Синоним);
			Результат.Добавить(ДанныеЗначения);
		КонецЦикла;
	ИначеЕсли Тип = "Справочник" Тогда
		Если Метаданные.Справочники.Найти(Вид) = Неопределено Тогда
			Результат = Новый Структура;
			Результат.Вставить("ОшибкаНетОбъектаМетаданных", Истина);
			Результат.Вставить("ВсеОбъекты", ВсеСправочники());
			Возврат Результат;
		КонецЕсли;
		
		Возврат ПолучитьДанныеСправочника();
	ИначеЕсли Тип = "Документ" Тогда
		Если Метаданные.Документы.Найти(Вид) = Неопределено Тогда
			Результат = Новый Структура;
			Результат.Вставить("ОшибкаНетОбъектаМетаданных", Истина);
			Результат.Вставить("ВсеОбъекты", ВсеДокументы());
			Возврат Результат;
		КонецЕсли;
		
		Возврат ПолучитьДанныеДокумента();
	ИначеЕсли Тип = "Константа" Тогда
		Если Метаданные.Константы.Найти(Вид) = Неопределено Тогда
			Результат = Новый Структура;
			Результат.Вставить("ОшибкаНетОбъектаМетаданных", Истина);
			Результат.Вставить("ВсеОбъекты", ВсеКонстанты());
			Возврат Результат;
		КонецЕсли;
		Возврат ПолучитьДанныеКонстанты();
	ИначеЕсли Тип = "ВсеКонстанты" Тогда
		Возврат ПолучитьДанныеВсехКонстант();
	ИначеЕсли Тип = "ДанныеПоНавигационнойСсылке" Тогда
		Возврат ДанныеОбъектаПоНавигационнойСсылке();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Функция ПолучитьСсылкуИзНавигационной(Стр)
	
	Если ПустаяСтрока(Стр) Тогда
		ВызватьИсключение "Не передана навигационная ссылка";
	КонецЕсли;	
    
    ПерваяПозиция = Найти(Стр, "e1cib/data/");
	Если ПерваяПозиция = 0 Тогда
		ВызватьИсключение "В навигационной ссылке не найдена строка ""e1cib/data/""";
	КонецЕсли;
	
    ВтораяПозиция = Найти(Стр, "?ref=");
	Если ВтораяПозиция = 0 Тогда
		ВызватьИсключение "В навигационной ссылке не найдена строка ""?ref=""";
	КонецЕсли;	
    
    ПредставлениеТипа   = Сред(Стр, ПерваяПозиция + 11, ВтораяПозиция - ПерваяПозиция - 11);
	
	Попытка
    	ШаблонЗначения = ЗначениеВСтрокуВнутр(ПредопределенноеЗначение(ПредставлениеТипа + ".ПустаяСсылка"));
	Исключение
		ТекстСообщения = "Из навигационной ссылки <%1> не получилось вычислить ссылку на объект базы данных по типу <%2>.";
		ТекстСообщения = СтрЗаменить(ТекстСообщения, "%1", Стр);
		ТекстСообщения = СтрЗаменить(ТекстСообщения, "%2", ПредставлениеТипа);
		ВызватьИсключение ТекстСообщения
	КонецПопытки;
   	ЗначениеСсылки = СтрЗаменить(ШаблонЗначения, "00000000000000000000000000000000", Сред(Стр, ВтораяПозиция + 5));
	
	Попытка
    	Возврат ЗначениеИзСтрокиВнутр(ЗначениеСсылки);
	Исключение
		ТекстСообщения = "Из навигационной ссылки <%1> не получилось вычислить ссылку на объект базы данных.";
		ТекстСообщения = СтрЗаменить(ТекстСообщения, "%1", Стр);
		ВызватьИсключение ТекстСообщения
	КонецПопытки;
    
КонецФункции

&НаСервере
Функция ОпределитьТипПоСсылке(СсылкаНаЭлемент)
КонецФункции

&НаСервере
Функция ПолучитьЗначенияОбъектаСправочник(ИмяСправочника)
	
	
КонецФункции

&НаСервере
Функция ПолучитьЗначенияОбъектаДокумент(ИмяДокумента)
	
	
КонецФункции

&НаСервере
Функция ПолучитьДанныеКонстанты()
	
	ТекКонстанта = Константы[Вид];
	ЗначениеКонстанты = ТекКонстанта.Получить();
	
	ДанныеКонстанты = Новый Структура;
	ДанныеКонстанты.Вставить("Имя", Метаданные.Константы[Вид].Имя);
	ДанныеКонстанты.Вставить("Представление", Метаданные.Константы[Вид].Синоним);
	ДанныеКонстанты.Вставить("Значение", Строка(ЗначениеКонстанты));
	ДанныеКонстанты.Вставить("ТипЗначения", Строка(ТипЗнч(ЗначениеКонстанты)));
	
	// Дополнительно возвращаем общее количество констант в базе,
	// чтобы MCP-клиент мог видеть, сколько всего констант определено в конфигурации.
	Результат = Новый Структура;
	Результат.Вставить("КоличествоКонстант", Метаданные.Константы.Количество());
	Результат.Вставить("ДанныеКонстанты", ДанныеКонстанты);
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Функция ПолучитьДанныеВсехКонстант()

	// Параметры диапазона (1-based, включительные границы).
	// Если реквизиты формы не заполнены, берём весь список.
	НачальнаяГраница = ?(ЗначениеЗаполнено(НачальныйНомер), НачальныйНомер, 1);
	КонечнаяГраница  = ?(ЗначениеЗаполнено(КонечныйНомер),  КонечныйНомер,  Метаданные.Константы.Количество());

	// Если НачальнаяГраница > КонечнаяГраница, итерация ниже естественным образом
	// вернёт пустой массив - валидация на уровне MCP-инструмента выдаст ошибку INVALID_PARAMS.

	Данные = Новый Массив;

	ТекущийНомер = 0;
	Для Каждого ТекКонстанта Из Метаданные.Константы Цикл
		ТекущийНомер = ТекущийНомер + 1;

		Если ТекущийНомер < НачальнаяГраница Тогда
			Продолжить;
		КонецЕсли;
		Если ТекущийНомер > КонечнаяГраница Тогда
			Прервать;
		КонецЕсли;

		Имя = ТекКонстанта.Имя;
		ЗначениеКонстанты = Константы[Имя].Получить();

		ДанныеОднойКонстанты = Новый Структура;
		ДанныеОднойКонстанты.Вставить("Имя", Имя);
		ДанныеОднойКонстанты.Вставить("Представление", Метаданные.Константы[Имя].Синоним);
		ДанныеОднойКонстанты.Вставить("Значение", Строка(ЗначениеКонстанты));
		ДанныеОднойКонстанты.Вставить("ТипЗначения", Строка(ТипЗнч(ЗначениеКонстанты)));

		Данные.Добавить(ДанныеОднойКонстанты);
	КонецЦикла;

	// Обёртка для согласованности с ПолучитьДанныеКонстанты:
	// КоличествоКонстант - общее количество в базе,
	// ДанныеКонстант - отфильтрованный массив значений.
	Результат = Новый Структура;
	Результат.Вставить("КоличествоКонстант", Метаданные.Константы.Количество());
	Результат.Вставить("ДанныеКонстант", Данные);

	Возврат Результат;

КонецФункции

&НаСервере
Функция ПолучитьОписаниеФункциональнойОпции(ИмяОпции)
	
	Для Каждого Опция Из Метаданные.ФункциональныеОпции Цикл
		Если Опция.Имя = ИмяОпции Тогда
			Возврат Опция;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

&НаСервере
Функция ПолучитьДанныеСправочника()
	
	Запрос = Новый Запрос;
	
	ИмяСправочника = Вид;
	
	ОМ = Метаданные.Справочники[ИмяСправочника];
	ДлинаКода = ОМ.ДлинаКода;	
	ДлинаНаименования = ОМ.ДлинаНаименования;
	
	Если ДлинаКода > 0 Тогда
		ЕстьКод = Истина;
	Иначе	
		ЕстьКод = Ложь;
	КонецЕсли;
	
	Если ДлинаНаименования > 0 Тогда
		ЕстьНаименование = Истина;
	Иначе	
		ЕстьНаименование = Ложь;
	КонецЕсли;	
	
	СтрокаКоличествоСсылок = " ПЕРВЫЕ " + XMLСтрока(КоличествоСсылок);
	Если КоличествоСсылок = 0 Тогда
		СтрокаКоличествоСсылок = "";
	КонецЕсли;	
	
	ТекстЗапроса = "ВЫБРАТЬ" + СтрокаКоличествоСсылок + "
		|___Таблица___.Ссылка КАК Ссылка,
		|___Таблица___.ПометкаУдаления КАК ПометкаУдаления";
	
	Если ЕстьНаименование Тогда
		ТекстЗапроса = ТекстЗапроса + "," + Символы.ПС + "___Таблица___.Наименование КАК Наименование";
	КонецЕсли;	
	Если ЕстьКод Тогда
		ТекстЗапроса = ТекстЗапроса + "," +  Символы.ПС + "___Таблица___.Код КАК Код";
	КонецЕсли;	
	
	// Добавляем владельца если указан
	Если ЗначениеЗаполнено(ИмяВладельца) ИЛИ ОМ.Владельцы.Количество() Тогда
		ЕстьВладелец = Истина;
		ТекстЗапроса = ТекстЗапроса + ",
		|___Таблица___.Владелец КАК Владелец";
	Иначе	
		ЕстьВладелец = Ложь;
	КонецЕсли;
	
	ТекстЗапроса = ТекстЗапроса + "
		|ИЗ
		|Справочник." + ИмяСправочника + " КАК ___Таблица___";
	
	ЕстьУсловие = Ложь;
	// Добавляем условие по владельцу
	Если ЗначениеЗаполнено(ИмяВладельца) Тогда
		ЕстьУсловие = Истина;
		ТекстЗапроса = ТекстЗапроса + "
		|ГДЕ ___Таблица___.Владелец.Наименование = &Владелец";
		
		Запрос.УстановитьПараметр("Владелец", ИмяВладельца);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОтборПоНаименованию) Тогда
		// ВидОтбораПоНаименованию: "EXACT" (точное совпадение), "PARTIAL" (подстрока в любом месте),
		// "STARTS_WITH" (наименование начинается со значения).
		// Любое другое или пустое значение трактуется как "EXACT" для обратной совместимости.
		ТекстУсловия = "%ПараметрУсловие% ___Таблица___.Наименование = &Наименование";
		ЗначениеПараметра = ОтборПоНаименованию;
		Если ВРег(ВидОтбораПоНаименованию) = "PARTIAL" Тогда
			ТекстУсловия = "%ПараметрУсловие% ___Таблица___.Наименование ПОДОБНО &Наименование";
			ЗначениеПараметра = "%" + ОтборПоНаименованию + "%";
		ИначеЕсли ВРег(ВидОтбораПоНаименованию) = "STARTS_WITH" Тогда
			ТекстУсловия = "%ПараметрУсловие% ___Таблица___.Наименование ПОДОБНО &Наименование";
			ЗначениеПараметра = ОтборПоНаименованию + "%";
		КонецЕсли;
		ТекстЗапроса = ТекстЗапроса + "
		|" + ТекстУсловия;
		Запрос.УстановитьПараметр("Наименование", ЗначениеПараметра);
		Если ЕстьУсловие Тогда
			ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "%ПараметрУсловие%", "И");
		Иначе
			ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "%ПараметрУсловие%", "ГДЕ");
		КонецЕсли;
	КонецЕсли;
	
	ТекстЗапроса = ТекстЗапроса + "
		|УПОРЯДОЧИТЬ ПО Наименование";
	
	Запрос.Текст = ТекстЗапроса;
	
	РезультатЗапроса = Запрос.Выполнить();
	
	// Отдельный запрос без фильтров для получения общего количества элементов в базе.
	// На этот запрос НЕ накладываются фильтры (по владельцу, по наименованию),
	// чтобы пользователь видел реальное количество элементов в справочнике.
	ЗапросКоличество = Новый Запрос;
	ЗапросКоличество.Текст = 
		"ВЫБРАТЬ
		|КОЛИЧЕСТВО(___Таблица___.Ссылка) КАК ВсегоЭлементов
		|ИЗ
		|Справочник." + ИмяСправочника + " КАК ___Таблица___";
	
	ВыборкаКоличество = ЗапросКоличество.Выполнить().Выбрать();
	ВсегоЭлементов = 0;
	Если ВыборкаКоличество.Следующий() Тогда
		ВсегоЭлементов = ВыборкаКоличество.ВсегоЭлементов;
	КонецЕсли;
	
	Результат = Новый Массив;
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		НавигационнаяСсылка = ПолучитьНавигационнуюСсылку(ВыборкаДетальныеЗаписи.Ссылка);
		Данные = Новый Структура;
		Данные.Вставить("Ссылка", Строка(ВыборкаДетальныеЗаписи.Ссылка));
		Данные.Вставить("НавигационнаяСсылка", НавигационнаяСсылка);
		Данные.Вставить("ПометкаУдаления", ВыборкаДетальныеЗаписи.ПометкаУдаления);
		Если ЕстьНаименование Тогда
			Данные.Вставить("Наименование", ВыборкаДетальныеЗаписи.Наименование);
		КонецЕсли;
		Если ЕстьКод Тогда
			Данные.Вставить("Код", ВыборкаДетальныеЗаписи.Код);
		КонецЕсли;
		Если ЕстьВладелец Тогда
			Данные.Вставить("Владелец", Строка(ВыборкаДетальныеЗаписи.Владелец));
		КонецЕсли;	
		Результат.Добавить(Данные);
		
	КонецЦикла;
	
	// Возвращаем обёртку {КоличествоЭлементов, ДанныеЭлементов}, чтобы MCP-клиент
	// мог видеть как список элементов, так и общее количество элементов в справочнике.
	Обёртка = Новый Структура;
	Обёртка.Вставить("КоличествоЭлементов", ВсегоЭлементов);
	Обёртка.Вставить("ДанныеЭлементов", Результат);
	Возврат Обёртка;
	
КонецФункции	

&НаСервере
Функция ПолучитьДанныеДокумента()
	
	Запрос = Новый Запрос;
	
	ИмяДокумента = Вид;
	
	ОМ = Метаданные.Документы[ИмяДокумента];
	ДлинаНомера = ОМ.ДлинаНомера;	
	
	Если ДлинаНомера > 0 Тогда
		ЕстьНомер = Истина;
	Иначе	
		ЕстьНомер = Ложь;
	КонецЕсли;
	
	Если ОМ.Проведение = Метаданные.СвойстваОбъектов.Проведение.Разрешить Тогда
		ЕстьПризнакПроведен = Истина;
	Иначе	
		ЕстьПризнакПроведен = Ложь;
	КонецЕсли;
	
	СтрокаКоличествоСсылок = " ПЕРВЫЕ " + XMLСтрока(КоличествоСсылок);
	Если КоличествоСсылок = 0 Тогда
		СтрокаКоличествоСсылок = "";
	КонецЕсли;	
	
	ТекстЗапроса = "ВЫБРАТЬ" + СтрокаКоличествоСсылок + "
		|___Таблица___.Ссылка КАК Ссылка,
		|___Таблица___.ПометкаУдаления КАК ПометкаУдаления";
	
	ТекстЗапроса = ТекстЗапроса + "," + Символы.ПС + "___Таблица___.Дата КАК Дата";
	Если ЕстьНомер Тогда
		ТекстЗапроса = ТекстЗапроса + "," +  Символы.ПС + "___Таблица___.Номер КАК Номер";
	КонецЕсли;	
	Если ЕстьПризнакПроведен Тогда
		ТекстЗапроса = ТекстЗапроса + "," +  Символы.ПС + "___Таблица___.Проведен КАК Проведен";
	КонецЕсли;	
	
	ТекстЗапроса = ТекстЗапроса + "
		|ИЗ
		|Документ." + ИмяДокумента + " КАК ___Таблица___";
	
	Если ЗначениеЗаполнено(ДатаНачала) И ЗначениеЗаполнено(ДатаОкончания) Тогда
		ТекстЗапроса = ТекстЗапроса + "
		|ГДЕ ___Таблица___.Дата >= &ДатаНачала И ___Таблица___.Дата <= &ДатаОкончания";
		Запрос.УстановитьПараметр("ДатаНачала", ДатаНачала);
		Запрос.УстановитьПараметр("ДатаОкончания", ДатаОкончания);
		ЕстьУсловие = Истина;
	Иначе	
		// Добавляем условие по ДатаНачала
		Если ЗначениеЗаполнено(ДатаНачала) Тогда
			ТекстЗапроса = ТекстЗапроса + "
			|ГДЕ ___Таблица___.Дата >= &ДатаНачала";
			
			Запрос.УстановитьПараметр("ДатаНачала", ДатаНачала);
		КонецЕсли;
		
		// Добавляем условие по ДатаНачала
		Если ЗначениеЗаполнено(ДатаОкончания) Тогда
			ТекстЗапроса = ТекстЗапроса + "
			|ГДЕ ___Таблица___.Дата <= &ДатаОкончания";
			
			Запрос.УстановитьПараметр("ДатаОкончания", ДатаОкончания);
		КонецЕсли;
	КонецЕсли;	
	
	ТекстЗапроса = ТекстЗапроса + "
		|УПОРЯДОЧИТЬ ПО Дата";
	
	Запрос.Текст = ТекстЗапроса;
	
	РезультатЗапроса = Запрос.Выполнить();
	
	// Отдельный запрос без фильтров для получения общего количества документов в базе.
	// На этот запрос НЕ накладываются фильтры по датам (date_start, date_end),
	// чтобы пользователь видел реальное количество документов в информационной базе.
	ЗапросКоличество = Новый Запрос;
	ЗапросКоличество.Текст = 
		"ВЫБРАТЬ
		|КОЛИЧЕСТВО(___Таблица___.Ссылка) КАК ВсегоДокументов
		|ИЗ
		|Документ." + ИмяДокумента + " КАК ___Таблица___";
	
	ВыборкаКоличество = ЗапросКоличество.Выполнить().Выбрать();
	ВсегоДокументов = 0;
	Если ВыборкаКоличество.Следующий() Тогда
		ВсегоДокументов = ВыборкаКоличество.ВсегоДокументов;
	КонецЕсли;
	
	Результат = Новый Массив;
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		НавигационнаяСсылка = ПолучитьНавигационнуюСсылку(ВыборкаДетальныеЗаписи.Ссылка);
		Данные = Новый Структура;
		Данные.Вставить("Ссылка", Строка(ВыборкаДетальныеЗаписи.Ссылка));
		Данные.Вставить("ПометкаУдаления", ВыборкаДетальныеЗаписи.ПометкаУдаления);
		Данные.Вставить("НавигационнаяСсылка", НавигационнаяСсылка);
		Данные.Вставить("Дата", ВыборкаДетальныеЗаписи.Дата);
		Если ЕстьНомер Тогда
			Данные.Вставить("Номер", ВыборкаДетальныеЗаписи.Номер);
		КонецЕсли;
		Если ЕстьПризнакПроведен Тогда
			Данные.Вставить("Проведен", ВыборкаДетальныеЗаписи.Проведен);
		КонецЕсли;
		Результат.Добавить(Данные);
		
	КонецЦикла;
	
	// Возвращаем обёртку {КоличествоДокументов, ДанныеДокументов}, чтобы MCP-клиент
	// мог видеть как список документов, так и общее количество документов в базе.
	Обёртка = Новый Структура;
	Обёртка.Вставить("КоличествоДокументов", ВсегоДокументов);
	Обёртка.Вставить("ДанныеДокументов", Результат);
	Возврат Обёртка;
	
КонецФункции	

&НаСервере
Функция ВсеПеречисления()
	Массив = Новый Массив;
	Для Каждого ТекОбъект Из Метаданные.Перечисления Цикл
		Массив.Добавить(ТекОбъект.Имя);
	КонецЦикла;	
	Возврат Массив;
КонецФункции	

&НаСервере
Функция ВсеСправочники()
	Массив = Новый Массив;
	Для Каждого ТекОбъект Из Метаданные.Справочники Цикл
		Массив.Добавить(ТекОбъект.Имя);
	КонецЦикла;	
	Возврат Массив;
КонецФункции	

&НаСервере
Функция ВсеДокументы()
	Массив = Новый Массив;
	Для Каждого ТекОбъект Из Метаданные.Документы Цикл
		Массив.Добавить(ТекОбъект.Имя);
	КонецЦикла;	
	Возврат Массив;
КонецФункции	

&НаСервере
Функция ВсеКонстанты()
	Массив = Новый Массив;
	Для Каждого ТекОбъект Из Метаданные.Константы Цикл
		Массив.Добавить(ТекОбъект.Имя);
	КонецЦикла;	
	Возврат Массив;
КонецФункции	

&НаСервере
Функция ДанныеОбъектаПоНавигационнойСсылке()
	СсылкаНаОбъект = ПолучитьСсылкуИзНавигационной(НавигационнаяСсылкаОбъекта);
	ТекОбъект = СсылкаНаОбъект.ПолучитьОбъект();
	Возврат VAExtensionОбщегоНазначенияСервер.ДанныеОбъектаСервер(ТекОбъект);
КонецФункции

#КонецОбласти