#Область ОписаниеПеременных

&НаКлиенте
Перем Ванесса;

&НаКлиенте
Перем глДопПараметрыВызоваMCP;

&НаКлиенте
Перем глКешБазыЗнаний, глКешВопросов, глЧастоИспользуемыеШаги;

&НаКлиенте
Перем КаталогСФайламиОзвучки;

&НаКлиенте
Перем КешДанныхУпавшегоШага;

&НаКлиенте
Перем ВнешнийФасадМсп;

#КонецОбласти

#Область ЭкспортныеПроцедурыИФункции

// Делает первичную инициализацию модуля.
&НаКлиенте
Функция ИнициализацияФормы(ВладелецФормы) Экспорт
	Ванесса = ВладелецФормы;
	Возврат Истина;
КонецФункции

// Делает отключение модуля
&НаКлиенте
Функция ОтключениеМодуля() Экспорт

	Ванесса = Неопределено;
	
КонецФункции	 

// Выполняет регистрацию инструментов MCP
&НаКлиенте
Процедура ЗарегистрироватьИнструментыMCP() Экспорт
	
	ПараметрыСозданияMCP = Новый Структура;
	ПараметрыСозданияMCP.Вставить("МодульМсп_Сервер", МодульМсп_Сервер());
	ПараметрыСозданияMCP.Вставить("МодульМсп_СхемаПараметров", Вычислить("Мсп_СхемаПараметров"));

	ВызватьРегистрациюИнструментовMCP(ПараметрыСозданияMCP);

КонецПроцедуры

// Вызывает регистрацию всех инструментов MCP с переданными модулями сервера и схемы.
// Используется штатной регистрацией (client_mcp) и внешним транспортом (плагин MCPRagSearch).
&НаКлиенте
Процедура ВызватьРегистрациюИнструментовMCP(ПараметрыСозданияMCP) Экспорт

	//MCPИнструментПолучитьВсеШаги(ПараметрыСозданияMCP);
	MCPИнструментНайтиШагиПоКлючевымСловам(ПараметрыСозданияMCP);
	//MCPИнструментПолучитьТекущуюДату(ПараметрыСозданияMCP);
	MCPИнструментСостояниеVanessaAutomation(ПараметрыСозданияMCP);
	MCPИнструментСинтаксКонтроль(ПараметрыСозданияMCP);
	MCPИнструментОткрытьФичаФайл(ПараметрыСозданияMCP);
	MCPИнструментОткрытьЗагрузитьИлиПерезагрузитьФичаФайл(ПараметрыСозданияMCP);
	MCPИнструментПолучитьСостояниеРедактора(ПараметрыСозданияMCP);
	MCPИнструментВыбратьСценарий(ПараметрыСозданияMCP);
	MCPИнструментВыбратьШаг(ПараметрыСозданияMCP);
	MCPИнструментПолучитьИмяЗаголовокАктивногоОкнаИФормы(ПараметрыСозданияMCP);
	MCPИнструментПолучитьСписокВнутреннихОконКлиентаТестирования(ПараметрыСозданияMCP);
	MCPИнструментУправлениеОкномКлиентаТестирования(ПараметрыСозданияMCP);
	MCPИнструментПолучитьИнформациюОТекущейФорме(ПараметрыСозданияMCP);
	//MCPИнструментВыполнитьОдинШагТекстом(ПараметрыСозданияMCP);
	MCPИнструментВыполнитьОдинШагВФичаФайле(ПараметрыСозданияMCP);
	MCPИнструментПодключитьКлиентТестирования(ПараметрыСозданияMCP);
	MCPИнструментВыполнитьСценарий(ПараметрыСозданияMCP);
	MCPИнструментОстановитьВыполнениеСценариев(ПараметрыСозданияMCP);
	MCPИнструментПолучитьРезультатВыполненияТестов(ПараметрыСозданияMCP);
	MCPИнструментПолучитьСписокОконКлиентаТестированияДляСнятияСкриншотов(ПараметрыСозданияMCP);
	MCPИнструментПолучитьСкриншотОкнаКлиентаТестирования(ПараметрыСозданияMCP);
	MCPИнструментУправлениеПеременными(ПараметрыСозданияMCP);
	MCPИнструментУправлениеТочкамиОстанова(ПараметрыСозданияMCP);
	MCPИнструментПолучитьПодробнуюИнформациюОШагеВСтрокеСценария(ПараметрыСозданияMCP);
	MCPИнструментПолучитьДанныеИзБазыЗнаний(ПараметрыСозданияMCP);
	MCPИнструментПолучитьДанныеОЧастоИспользуемыхШагах(ПараметрыСозданияMCP);
	MCPИнструментУправлениеКоманднымИнтерфейсом(ПараметрыСозданияMCP);
	MCPИнструментПолучитьСписокРасширений(ПараметрыСозданияMCP);
	MCPИнструментУправлениеПрофилямиКлиентовТестирования(ПараметрыСозданияMCP);
	MCPИнструментПолучитьДанныеОбОкружении(ПараметрыСозданияMCP);
	MCPИнструментГолосовыеУведомления(ПараметрыСозданияMCP);
	MCPИнструментПолучитьРеквизитыОбъекта(ПараметрыСозданияMCP);
	MCPИнструментПолучитьДанныеИзТаблицыБД(ПараметрыСозданияMCP);
	MCPИнструментСохранитьТабличныйДокументВФайл(ПараметрыСозданияMCP);
	MCPИнструментПолучитьДанныеЭлементаФормы(ПараметрыСозданияMCP);
	MCPИнструментУправлениеЭлементамиФормы(ПараметрыСозданияMCP);
	MCPИнструментЗаписьДействийПользователя(ПараметрыСозданияMCP);
	MCPИнструментЗакрытьКлиентТестирования(ПараметрыСозданияMCP);
		
КонецПроцедуры

// Отменяет регистрацию инструментов MCP
&НаКлиенте
Функция ОтменитьРегистрациюMCP() Экспорт
	модульМсп_РеестрКлиент = Вычислить("Мсп_РеестрКлиент");
	модульМсп_РеестрКлиент.ОтменитьРегистрациюПровайдера(ИмяПровайдера());
КонецФункции	

&НаКлиенте
Процедура ВызватьОшибкуMCP(ТекстСообщения) Экспорт
	МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьВОтветИнформациюОМодальномДиалоге(Строки, ОкноМодальногоДиалога) Экспорт
	Попытка
		ТекстМодальногоДиалога = Ванесса.ПолучитьТекстПредупреждения();
		Строки.Добавить("");
		Строки.Добавить(СтрокаДляОшибкиТекстМодальногоДиалога());
		Строки.Добавить(ТекстМодальногоДиалога);
	Исключение
	КонецПопытки;
	
	КнопкиМодальногоДиалога = ОкноМодальногоДиалога.НайтиОбъекты(Тип("ТестируемаяКнопкаФормы"));
	РезультатКнопки = Новый Массив;
	Для Каждого ТекКнопка Из КнопкиМодальногоДиалога Цикл
		Если НЕ ТекКнопка.ТекущаяВидимость() Тогда
			Продолжить;
		КонецЕсли;
		
		РезультатКнопки.Добавить(ТекКнопка);
	КонецЦикла;
	
	Если РезультатКнопки.Количество() > 0 Тогда
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("Кнопки модального диалога:"));
		Для Каждого ТекКнопка Из РезультатКнопки Цикл
			Строки.Добавить("  -" + ТекКнопка.Имя + " (" + ТекКнопка.ТекстЗаголовка + ")");
		КонецЦикла;	
	КонецЕсли;
	
	Строки.Добавить("");
	
КонецПроцедуры

#КонецОбласти

#Область РеализацияИнструментовMCP

// Возвращает строку json со всеми доступными шагами
&НаКлиенте
Функция ВсеДоступныеШагиMCP(Запрос, ДополнительныеПараметры) Экспорт
	
    Возврат Ванесса.ВсеДоступныеШагиMCP();
	
КонецФункции

// Возвращает строку json с шагами, которые были найдены по строке поиска
// Пример строки поиска: кнопка|кнопки|кнопку
&НаКлиенте
Функция ПоискШаговПоСтроке(Запрос, ДополнительныеПараметры) Экспорт
	search_name = Строка(ЗначениеПараметраИзЗапроса(Запрос, "search_name"));
	exclude_name = Строка(ЗначениеПараметраИзЗапроса(Запрос, "exclude_name"));
	search_description = Строка(ЗначениеПараметраИзЗапроса(Запрос, "search_description"));
	exclude_description = Строка(ЗначениеПараметраИзЗапроса(Запрос, "exclude_description"));
	search_type = Строка(ЗначениеПараметраИзЗапроса(Запрос, "search_type"));
	exclude_type = Строка(ЗначениеПараметраИзЗапроса(Запрос, "exclude_type"));
	Попытка
		limit = Число(ЗначениеПараметраИзЗапроса(Запрос, "limit"));
	Исключение
		limit = 0;
	КонецПопытки;
	
	ТолькоЦиклы = ЗначениеПараметраИзЗапроса(Запрос, "loops_only");
	Если ТолькоЦиклы = Неопределено Тогда
		ТолькоЦиклы = Ложь;
	Иначе
		Попытка
			ТолькоЦиклы = Булево(ТолькоЦиклы);
		Исключение
			ТекстСообщения = Ванесса.Локализовать("Параметр loops_only передан некорректно") + Символы.ПС + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
		КонецПопытки;
	КонецЕсли;
	
	ТолькоУсловия = ЗначениеПараметраИзЗапроса(Запрос, "conditions_only");
	Если ТолькоУсловия = Неопределено Тогда
		ТолькоУсловия = Ложь;
	Иначе
		Попытка
			ТолькоУсловия = Булево(ТолькоУсловия);
		Исключение
			ТекстСообщения = Ванесса.Локализовать("Параметр conditions_only передан некорректно") + Символы.ПС + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
		КонецПопытки;
	КонецЕсли;	
	
    Возврат ПоискШаговПоСтрокеMCP(search_name, exclude_name, search_description, exclude_description, search_type, exclude_type, limit, ТолькоЦиклы, ТолькоУсловия);
	
КонецФункции

// Возвращает строку json с шагами, которые были найдены по строке поиска
// Пример строки поиска: кнопка|кнопки|кнопку
&НаКлиенте
Функция ПолучитьИмяЗаголовокАктивногоОкнаИФормы(Запрос, ДополнительныеПараметры) Экспорт
	type = Строка(ЗначениеПараметраИзЗапроса(Запрос, "type"));
	Если type = "window_caption" ИЛИ type = "navigation_link" ИЛИ type = "form_name" ИЛИ type = "form_caption" Тогда
		
	Иначе
		ТекстСообщения = Ванесса.Локализовать("Передано неправильное значение параметра type. Допустимые варианты: window_caption, navigation_link, form_name, form_caption");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	       
	Если type = "window_caption" Тогда
		Возврат АктивноеОкно.Заголовок;
	ИначеЕсли type = "navigation_link" Тогда
		Попытка
			НавСсылка = АктивноеОкно.НавигационнаяСсылка;
		Исключение
			ТекстСообщения = Ванесса.Локализовать("У активного окна <%1> нет навигационной ссылки.");
			ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(ТекстСообщения, АктивноеОкно.Заголовок);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстСообщения);
		КонецПопытки;
		
		Если НЕ ЗначениеЗаполнено(НавСсылка) Тогда
			ТекстСообщения = Ванесса.Локализовать("У активного окна <%1> нет навигационной ссылки.");
			ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(ТекстСообщения, АктивноеОкно.Заголовок);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстСообщения);
		КонецЕсли;	
		
		Возврат НавСсылка;
	ИначеЕсли type = "form_name" Тогда
		Попытка
			ТестируемаяФорма = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяФорма"));
			Возврат ТестируемаяФорма.ИмяФормы;
		Исключение
			ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
	ИначеЕсли type = "form_caption" Тогда
		Попытка
			ТестируемаяФорма = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяФорма"));
			Возврат ТестируемаяФорма.ТекстЗаголовка;
		Исключение
			ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
	Иначе
		ТекстСообщения = Ванесса.Локализовать("Передано неправильное значение параметра type. Допустимые варианты: window_caption, navigation_link, form_name, form_caption");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
КонецФункции

// Возвращает информацию об элементах текущей формы
&НаКлиенте
Функция ПолучитьИнформациюОТекущейФорме(Запрос, ДополнительныеПараметры) Экспорт
	
	ФорматВывода = Строка(ЗначениеПараметраИзЗапроса(Запрос, "format"));
	Если НЕ ЗначениеЗаполнено(ФорматВывода) Тогда
		ФорматВывода = "full";
	КонецЕсли;
	
	Если ФорматВывода <> "full" И ФорматВывода <> "gherkin" И ФорматВывода <> "elements" И ФорматВывода <> "messages" И ФорматВывода <> "buttons" Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр format должен быть 'full', 'gherkin' или 'elements' или 'buttons' или 'messages'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	Если ФорматВывода = "messages" Тогда
		CообщенияПользователю = ПолучитьСообщенияПользователюИзОкна(АктивноеОкно);
		Если CообщенияПользователю = Неопределено Тогда
			Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В окне <%1> нет сообщений пользователю."), АктивноеОкно.Заголовок);
		Иначе	
			ТекстСообщения = Ванесса.Локализовать("## Обнаружены сообщения пользователю:") + Символы.ПС + Ванесса._СтрСоединить(CообщенияПользователю, Символы.ПС);
			Возврат ТекстСообщения;
		КонецЕсли;	
	КонецЕсли;	
	
	Идентификатор = Запрос.Идентификатор;
	Если ФорматВывода = "elements" Или ФорматВывода = "buttons" Или ФорматВывода = "full" Тогда
		MCPОткрытьИсследовательФормыДляАнализа(Идентификатор, ФорматВывода);
		Возврат Неопределено;
	КонецЕсли;
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Анализ текущей формы"));
	Строки.Добавить("");
	
	// Получаем Gherkin-состояние формы
	Если ФорматВывода = "gherkin" Тогда
		Попытка
			Ванесса.ПолучитьИзмененияФормыGherkin();
			Gherkin = Ванесса.Объект.СгенерированныйСценарий;
			Если ЗначениеЗаполнено(Gherkin) Тогда
				Строки.Добавить(Ванесса.Локализовать("## Gherkin-состояние формы"));
				Строки.Добавить("```gherkin");
				Строки.Добавить(Gherkin);
				Строки.Добавить("```");
				Строки.Добавить("");
			Иначе
				Строки.Добавить(Ванесса.Локализовать("## Gherkin-состояние формы"));
				Строки.Добавить(Ванесса.Локализовать("Нет данных."));
				Строки.Добавить("");
				Строки.Добавить(Ванесса.Локализовать("## Следующие шаги"));
				Строки.Добавить(Ванесса.Локализовать("- Подключите клиента тестирования: `connect_test_client`"));
				Строки.Добавить(Ванесса.Локализовать("- Затем повторите: `get_form_analysis`"));
				Строки.Добавить("");
			КонецЕсли;
		Исключение
			Строки.Добавить(Ванесса.Локализовать("## Gherkin-состояние формы"));
			Строки.Добавить(Ванесса.Локализовать("ошибка:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки()));
			Строки.Добавить("");
		КонецПопытки;
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Выполняет один шаг произвольным текстом
&НаКлиенте
Функция ВыполнитьОдинШагТекстом(Запрос, ДополнительныеПараметры) Экспорт
	
	ТекстШага = Строка(ЗначениеПараметраИзЗапроса(Запрос, "stepText"));
	Если ЗначениеЗаполнено(ТекстШага) Тогда
		ТекстШага = СокрЛП(Строка(ТекстШага));
	КонецЕсли;

	Если НЕ ЗначениеЗаполнено(ТекстШага) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр stepText обязателен и не может быть пустым.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если Ванесса.Объект.ИдетВыполнениеСценариев Тогда
		ТекстСообщения = Ванесса.Локализовать("Сценарии уже выполняются. Дождитесь завершения или вызовите stopExecution.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстСообщения);
	КонецЕсли;
	
	Идентификатор = Запрос.Идентификатор;
	Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
	
	Попытка
		Ванесса.Шаг(ТекстШага);
	Исключение
		ТекстСообщения = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

// Подключает клиент тестирования
&НаКлиенте
Функция ПодключитьКлиентТестирования(Запрос, ДополнительныеПараметры) Экспорт
	
	ИмяПрофиля = Строка(ЗначениеПараметраИзЗапроса(Запрос, "profileName"));
	Если Не ЗначениеЗаполнено(ИмяПрофиля) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр profileName обязателен.") + Символы.ПС + Символы.ПС + MCPСписокДоступныхПрофилей();
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Попытка
		Идентификатор = Запрос.Идентификатор;
		Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
		Результат = Ванесса.ПодключитьПрофильTestClientПоИмени(ИмяПрофиля);
		
		Если Результат Тогда
			ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
				"# Подключение клиента тестирования
				|
				|- Статус: подключен
				|- Профиль: %1
				|
				|## Следующие шаги
				|- Получите состояние формы: `get_form_analysis`
				|- Выполните шаг: `execute_feature_step`"),
				ИмяПрофиля);
		Иначе
			ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
				"# Подключение клиента тестирования
				|
				|- Статус: не удалось подключить
				|- Профиль: %1"),
				ИмяПрофиля)
				+ Символы.ПС + Символы.ПС
				+ MCPСписокДоступныхПрофилей()
				+ Символы.ПС + Символы.ПС
				+ Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
					"## Возможные причины
					|- Профиль с именем '%1' не найден в настройках
					|- Тестовое приложение не запущено
					|- Настройки подключения некорректны"),
					ИмяПрофиля);
		КонецЕсли;
		
		//MCPОтправитьРезультатИнструмента(ИдентификаторЗапроса, ТекстОтвета);
		МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), ТекстОтвета);
		Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
		Возврат Неопределено;
	Исключение
		ТекстСообщения = Ванесса.Локализовать("Ошибка подключения клиента тестирования:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки()) + Символы.ПС + Символы.ПС + MCPСписокДоступныхПрофилей();
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

// Выполняет один шаг из фича файла
&НаКлиенте
Функция ВыполнитьОдинШагВФичаФайле(Запрос, ДополнительныеПараметры) Экспорт
	
	ПутьКФайлу = Строка(ЗначениеПараметраИзЗапроса(Запрос, "filePath"));
	НомерСтроки = Число(ЗначениеПараметраИзЗапроса(Запрос, "lineNumber"));
	Если НЕ ЗначениеЗаполнено(ПутьКФайлу) Тогда
		ПутьКФайлу = Ванесса.Объект.КаталогФич;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(НомерСтроки) ИЛИ НомерСтроки < 1 Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр lineNumber обязателен и должен быть >= 1.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ПутьКФайлу = MCPНормализоватьПутьКФичаФайлу(ПутьКФайлу);
	Идентификатор = Запрос.Идентификатор;
	Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
	
	// Открыть/активировать вкладку файла в редакторе
	ДопПараметрыВызоваMCP = Новый Структура;
	ДопПараметрыВызоваMCP.Вставить("ПутьКФайлу", ПутьКФайлу);
	ДопПараметрыВызоваMCP.Вставить("НомерСтроки", НомерСтроки);
	ДопПараметрыВызоваMCP.Вставить("Тип", "ВыполнитьОдинШагВФичаФайле");
	MCPOpenFeatureFileВРедакторе(ПутьКФайлу, "ВыполнитьОдинШагВФичаФайле", ДопПараметрыВызоваMCP);
	
КонецФункции

// Получает список окон клиента тестирования для снятия скриншотов
&НаКлиенте
Функция ПолучитьСписокОконКлиентаТестированияДляСнятияСкриншотов(Запрос, ДополнительныеПараметры) Экспорт
	
	// Реальная проверка подключения клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	ProcessId = Ванесса.PIDТекущегоКлиентаТестирования();
	Если НЕ ЗначениеЗаполнено(ProcessId) Тогда
		ТекстОшибки = Ванесса.Локализовать("Не вышло получить PID процесса клиента тестирования.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;	
	
	Идентификатор = Запрос.Идентификатор;
	Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
	
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("ProcessId", ProcessId);
	ОписаниеОповещения = Новый ОписаниеОповещения("ПолученПолучитьСписокОконКлиентаТестированияДляВозвратаИхЗаголовков", ЭтаФорма, ДопПараметры);
	Ванесса.ВнешняяКомпонентаДляСкриншотов.НачатьВызовПолучитьСписокОкон(ОписаниеОповещения, ProcessId);
	
КонецФункции

// Получает скриншот окна клиента тестирования
&НаКлиенте
Функция ПолучитьСкриншотОкнаКлиентаТестирования(Запрос, ДополнительныеПараметры) Экспорт
	
	ЗаголовокОкна = Строка(ЗначениеПараметраИзЗапроса(Запрос, "window_title"));
	ИмяФайла = Строка(ЗначениеПараметраИзЗапроса(Запрос, "file_name"));
	Если НЕ ЗначениеЗаполнено(ЗаголовокОкна) Тогда
		ТекстОшибки = Ванесса.Локализовать("Не указан обязательный параметр window_title");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ИмяФайла) Тогда
		Файл = Новый Файл(ИмяФайла);
		КаталогФайла = Файл.Путь;
		Если НЕ Ванесса.ФайлСуществуетКомандаСистемы(КаталогФайла) Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Некорректное значение параметра file_name. Не найден каталог файла '%1'"), КаталогФайла);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
		КонецЕсли;	
	КонецЕсли;	
	
	// Получаем параметр color_mode (по умолчанию 'grayscale')
	ЦветовойРежим = Строка(ЗначениеПараметраИзЗапроса(Запрос, "color_mode"));
	Если НЕ ЗначениеЗаполнено(ЦветовойРежим) Тогда
		ЦветовойРежим = "grayscale";
	Иначе
		ЦветовойРежим = НРег(СокрЛП(ЦветовойРежим));
		Если ЦветовойРежим <> "color" И ЦветовойРежим <> "grayscale" Тогда
			ТекстОшибки = Ванесса.Локализовать("Некорректное значение параметра color_mode. Допустимые значения: 'grayscale' (черно-белый) или 'color' (цветной).");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
		КонецЕсли;
	КонецЕсли;
	
	// Реальная проверка подключения клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	ProcessId = Ванесса.PIDТекущегоКлиентаТестирования();
	Если НЕ ЗначениеЗаполнено(ProcessId) Тогда
		ТекстОшибки = Ванесса.Локализовать("Не вышло получить PID процесса клиента тестирования.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;	
	
	Идентификатор = Запрос.Идентификатор;
	Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
	
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("ProcessId", ProcessId);
	ДопПараметры.Вставить("ЗаголовокОкна", ЗаголовокОкна);
	ДопПараметры.Вставить("ИмяФайла", ИмяФайла);
	ДопПараметры.Вставить("ЦветовойРежим", ЦветовойРежим);
	ОписаниеОповещения = Новый ОписаниеОповещения("ПолученПолучитьСписокОконКлиентаТестированияДляСнятияСкриншотов", ЭтаФорма, ДопПараметры);
	Ванесса.ВнешняяКомпонентаДляСкриншотов.НачатьВызовПолучитьСписокОкон(ОписаниеОповещения, ProcessId);
	
КонецФункции

&НаКлиенте
Процедура ПолученПолучитьСписокОконКлиентаТестированияДляСнятияСкриншотов(РезультатВызова, ПараметрыВызова, ДопПараметры) Экспорт
	
	ОкнаДляСкриншотов = ПолучитьОкнаДляСкриншотов(РезультатВызова);
	
	Если ОкнаДляСкриншотов.Количество() = 0 Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("У процесса с ID=%1 не найдено окон для снятия скриншотов."), XMLСтрока(ДопПараметры.ProcessId));
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	НужноеОкно = Неопределено;
	Для Каждого ТекОкно Из ОкнаДляСкриншотов Цикл
		Если ТекОкно.ПолныеДанныеОкна.Свойство("Title") И НРег(ТекОкно.ПолныеДанныеОкна.Title) = НРег(ДопПараметры.ЗаголовокОкна) Тогда
			НужноеОкно = ТекОкно;
			Прервать;
		КонецЕсли;	
	КонецЦикла;
	
	Если НужноеОкно = Неопределено Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("У процесса с ID=%1 не найдено окно с заголовком <%2>."), XMLСтрока(ДопПараметры.ProcessId), ДопПараметры.ЗаголовокОкна);
		Строки = Новый Массив;
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("## Важно. Список окон доступных для скриншота может не совпадать с внутренним списком окон клиента тестирования."));
		ДобавитьВСообщениеОкнаДоступныеДляСкриншота(Строки, ОкнаДляСкриншотов);
		ТекстСообщения = ТекстСообщения + Символы.ПС + Ванесса._СтрСоединить(Строки, Символы.ПС);
		
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;	
	
	ДопПараметры.Вставить("НужноеОкно", НужноеОкно);
	ДопПараметры.Вставить("ОкнаДляСкриншотов", ОкнаДляСкриншотов);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ПолученСнимокОкнаДляВозвратаСкриншота", ЭтаФорма, ДопПараметры);
	Ванесса.ВнешняяКомпонентаДляСкриншотов.НачатьВызовПолучитьСнимокОкна(ОписаниеОповещения,
		НужноеОкно.ПолныеДанныеОкна.window);
		
КонецПроцедуры 

&НаКлиенте
Процедура ПолученСнимокОкнаДляВозвратаСкриншота(ДвоичныеДанные, ПараметрыВызова, ДополнительныеПараметры) Экспорт
	
	Если ДвоичныеДанные = Неопределено Тогда
		
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
				"Не удалось получить снимок экрана.
				|Проверьте настройки компоненты VanessaExt в разделе «Скриншоты».
				|Если используется клиент тестирования, попробуйте сначала активировать его окно."));
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;

	ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
		"# Скриншот клиента тестирования
		|
		|Скриншот клиента тестирования сделан успешно."));

	Строки = Новый Массив;
	Строки.Добавить(ТекстОтвета);
	ДобавитьВСообщениеОкнаДоступныеДляСкриншота(Строки, ДополнительныеПараметры.ОкнаДляСкриншотов);
	ТекстОтвета = ТекстОтвета + Символы.ПС + Ванесса._СтрСоединить(Строки, Символы.ПС);
	MCPОтправитьРезультатИнструментаСИзображением(ТекстОтвета, ДвоичныеДанные, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолученПолучитьСписокОконКлиентаТестированияДляВозвратаИхЗаголовков(РезультатВызова, ПараметрыВызова, ДопПараметры) Экспорт
	
	ОкнаДляСкриншотов = ПолучитьОкнаДляСкриншотов(РезультатВызова);
	
	Если ОкнаДляСкриншотов.Количество() = 0 Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("У процесса с ID=%1 не найдено окон для снятия скриншотов."), XMLСтрока(ДопПараметры.ProcessId));
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	Результат = Новый Массив;
	Результат.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Для снятия скриншотов найдено %1 окон:"), ОкнаДляСкриншотов.Количество()));
	Для Каждого ТекОкно Из ОкнаДляСкриншотов Цикл
		Если НЕ ТекОкно.ПолныеДанныеОкна.Свойство("Title") Тогда
			Продолжить;
		КонецЕсли;
		Результат.Добавить("  -" + ТекОкно.ПолныеДанныеОкна.Title);
	КонецЦикла;	
	
	ТекстСообщения = Ванесса._СтрСоединить(Результат, Символы.ПС);
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), ТекстСообщения);
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
	
КонецПроцедуры 

// Возвращает список окон клиента тестирования
&НаКлиенте
Функция ПолучитьСписокВнутреннихОконКлиентаТестирования(Запрос, ДополнительныеПараметры) Экспорт
	
	// Реальная проверка подключения клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	Попытка
		ТестовоеПриложение = Ванесса.ПолучитьТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	МассивОкон = ТестовоеПриложение.НайтиОбъекты(Тип("ТестируемоеОкноКлиентскогоПриложения"));
	
	Результат = Новый Массив;
	Результат.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В клиенте тестирования найдено %1 окон:"), МассивОкон.Количество()));
	
	Для Каждого ТекОкно Из МассивОкон Цикл
		Результат.Добавить("   -" + ТекОкно.Заголовок);
	КонецЦикла;
	
	Возврат Ванесса._СтрСоединить(Результат, Символы.ПС);
	
КонецФункции

// Универсальный инструмент для управления окнами клиента тестирования
// Поддерживает действия: activate (активизировать окно), close (закрыть окно), open_navigation (открыть навигационную ссылку)
&НаКлиенте
Функция УправлениеОкномКлиентаТестирования(Запрос, ДополнительныеПараметры) Экспорт
	
	Действие = Строка(ЗначениеПараметраИзЗапроса(Запрос, "action"));
	Если НЕ ЗначениеЗаполнено(Действие) Тогда
		ТекстОшибки = Ванесса.Локализовать("Параметр action обязателен. Допустимые значения: 'activate' (активизировать окно), 'close' (закрыть окно), 'open_navigation' (открыть навигационную ссылку).");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
	КонецЕсли;
	
	// Реальная проверка подключения клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	Попытка
		ТестовоеПриложение = Ванесса.ПолучитьТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	Если Действие = "activate" Тогда
		// Активизировать окно по заголовку
		ЗаголовокОкна = Строка(ЗначениеПараметраИзЗапроса(Запрос, "window_title"));
		Если НЕ ЗначениеЗаполнено(ЗаголовокОкна) Тогда
			ТекстОшибки = Ванесса.Локализовать("Для действия 'activate' обязателен параметр window_title");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
		КонецЕсли;	
		
		НужноеОкно = ТестовоеПриложение.НайтиОбъект(Тип("ТестируемоеОкноКлиентскогоПриложения"), ЗаголовокОкна);
		
		Если НужноеОкно = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Окно с заголовоком <%1> не найдено."), ЗаголовокОкна);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;	
		
		Попытка
			НужноеОкно.Активизировать();
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Не получилось активизировать указанное окно. Возможно оно заблокировано модальным окном или другим окном.");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Окно <%1> было успешно активизировано."), ЗаголовокОкна);
		
	ИначеЕсли Действие = "close" Тогда
		// Закрыть окно по заголовку или части заголовка
		ЗаголовокОкна = Строка(ЗначениеПараметраИзЗапроса(Запрос, "window_title"));
		Если НЕ ЗначениеЗаполнено(ЗаголовокОкна) Тогда
			ТекстОшибки = Ванесса.Локализовать("Для действия 'close' обязателен параметр window_title");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
		КонецЕсли;	
		
		НужноеОкно = ТестовоеПриложение.НайтиОбъект(Тип("ТестируемоеОкноКлиентскогоПриложения"), ЗаголовокОкна);
		
		Если НужноеОкно = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Окно с заголовоком <%1> не найдено."), ЗаголовокОкна);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;	
		
		Попытка
			НужноеОкно.Закрыть();
			Ванесса.sleep(1);
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Не получилось закрыть указанное окно. Возможно оно заблокировано или уже закрыто.");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
		НужноеОкно = ТестовоеПриложение.НайтиОбъект(Тип("ТестируемоеОкноКлиентскогоПриложения"), ЗаголовокОкна);
		Если НужноеОкно = Неопределено Тогда
			Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Окно <%1> было успешно закрыто."), ЗаголовокОкна);
		КонецЕсли;
		
		ОкноМодальногоДиалога = Ванесса.НайтиОкноМодальногоДиалога();
		Если ОкноМодальногоДиалога <> Неопределено Тогда
			Строки = Новый Массив;
			Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("После попытки закрытия окна <%1> появился модальный диалог. Это не ошибка. Чтобы закрыть исходное окно надо обработать модальный диалог."), ЗаголовокОкна));
			
			ДобавитьВОтветИнформациюОМодальномДиалоге(Строки, ОкноМодальногоДиалога);
			
			Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
		КонецЕсли;	
		
		НужноеОкно = ТестовоеПриложение.НайтиОбъект(Тип("ТестируемоеОкноКлиентскогоПриложения"), ЗаголовокОкна);
		Если НужноеОкно <> Неопределено Тогда
			ТекстОшибки = Ванесса.Локализовать("Не получилось закрыть указанное окно. Команда на закрытие окна была отправлена, но окно все ещё есть.");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;	
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Окно <%1> было успешно закрыто."), ЗаголовокОкна);
		
	ИначеЕсли Действие = "open_navigation" Тогда
		// Открыть навигационную ссылку
		НавигационнаяСсылка = Строка(ЗначениеПараметраИзЗапроса(Запрос, "navigation_link"));
		Если НЕ ЗначениеЗаполнено(НавигационнаяСсылка) Тогда
			ТекстОшибки = Ванесса.Локализовать("Для действия 'open_navigation' обязателен параметр navigation_link");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
		КонецЕсли;	
		
		Попытка
			Ванесса.ОткрытьНавигационнуюСсылку(НавигационнаяСсылка);
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Не получилось открыть навигационную ссылку: ") + НавигационнаяСсылка + ". " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;

		МассивСтрок = Новый Массив;
		МассивСтрок.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Команда открытия навигационной ссылки <%1> была выполнена."), НавигационнаяСсылка));
		Ванесса.sleep(1);
		ДобавитьВМассивСтрокДанныеОбАктивномОкне(МассивСтрок);
		
		Возврат Ванесса._СтрСоединить(МассивСтрок, Символы.ПС);
		
	Иначе
		ТекстОшибки = Ванесса.Локализовать("Неизвестное действие: ") + Действие + ". " + Ванесса.Локализовать("Допустимые: 'activate', 'close', 'open_navigation'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
	КонецЕсли;
	
КонецФункции

// Добавляет, читает и удаляет переменные контекста и глобального контекста
&НаКлиенте
Функция УправлениеПеременными(Запрос, ДополнительныеПараметры) Экспорт
	
	Действие = Строка(ЗначениеПараметраИзЗапроса(Запрос, "action"));
	Если НЕ ЗначениеЗаполнено(Действие) Тогда
		Действие = "get";
	КонецЕсли;
	
	ИмяПеременной = Строка(ЗначениеПараметраИзЗапроса(Запрос, "name"));
	ЗначениеПеременнойПараметр = Строка(ЗначениеПараметраИзЗапроса(Запрос, "value"));
	ПеременнаяГлобальная = Строка(ЗначениеПараметраИзЗапроса(Запрос, "is_global"));
	Если ЗначениеЗаполнено(ПеременнаяГлобальная) Тогда
		ПеременнаяГлобальная = Булево(ПеременнаяГлобальная);
	Иначе
		ПеременнаяГлобальная = Ложь;
	КонецЕсли;	
	
	ОбъектКонтекст = Ванесса.ОбъектКонтекст;
	ОбъектКонтекстСохраняемый = Ванесса.ОбъектКонтекстСохраняемый;
	
	Если Действие = "get" Тогда
		// Если имя указано — одна переменная, иначе — все
		Если ЗначениеЗаполнено(ИмяПеременной) Тогда
			Значение = Ванесса.ЗначениеПеременной(ИмяПеременной);
			
			Если ПеременнаяГлобальная Тогда
				Значение = ЗначениеИзСтруктуры(ОбъектКонтекстСохраняемый, ИмяПеременной);
			Иначе	
				Значение = ЗначениеИзСтруктуры(ОбъектКонтекст, ИмяПеременной);
			КонецЕсли;	
			
			ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
				"# Переменная: %1
				|
				|- Значение: %2"),
				ИмяПеременной,
				MCPПредставлениеЗначенияДляMarkdown(Значение));
			Возврат ТекстОтвета;
		Иначе
			// Все переменные — формируем Markdown
			Строки = Новый Массив;
			Строки.Добавить("# Переменные контекста");
			Строки.Добавить("");
			
			КоличествоПеременных = 0;
			
			Если ОбъектКонтекст <> Неопределено И ТипЗнч(ОбъектКонтекст) = Тип("Структура") Тогда
				Строки.Добавить("## Контекст ($var$)");
				Для Каждого КлючЗначение Из ОбъектКонтекст Цикл
					Если Ванесса.ЭтоСлужебнаяПеременаяКонтекста(КлючЗначение.Ключ) Тогда
						Продолжить;
					КонецЕсли;
					Строки.Добавить("- `" + КлючЗначение.Ключ + "` = " + MCPПредставлениеЗначенияДляMarkdown(КлючЗначение.Значение));
					КоличествоПеременных = КоличествоПеременных + 1;
				КонецЦикла;
			КонецЕсли;
			
			Если ОбъектКонтекстСохраняемый <> Неопределено И ТипЗнч(ОбъектКонтекстСохраняемый) = Тип("Структура") Тогда
				Строки.Добавить("");
				Строки.Добавить("## Сохраняемый контекст ($$var$$)");
				Для Каждого КлючЗначение Из ОбъектКонтекстСохраняемый Цикл
					Если Ванесса.ЭтоСлужебнаяПеременаяКонтекста(КлючЗначение.Ключ) Тогда
						Продолжить;
					КонецЕсли;
					Строки.Добавить("- `" + КлючЗначение.Ключ + "` = " + MCPПредставлениеЗначенияДляMarkdown(КлючЗначение.Значение));
					КоличествоПеременных = КоличествоПеременных + 1;
				КонецЦикла;
			КонецЕсли;
			
			Если КоличествоПеременных = 0 Тогда
				Строки.Добавить(Ванесса.Локализовать("Переменных нет."));
			КонецЕсли;
			
			Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
		КонецЕсли;
		
	ИначеЕсли Действие = "set" Тогда
		Если Не ЗначениеЗаполнено(ИмяПеременной) Тогда
			ТекстОшибки = Ванесса.Локализовать("Параметр name обязателен для action=set.");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
		КонецЕсли;
		
		Попытка
			Если ПеременнаяГлобальная Тогда
				ОбъектКонтекстСохраняемый.Вставить(ИмяПеременной, ЗначениеПеременнойПараметр);
			Иначе
				ОбъектКонтекст.Вставить(ИмяПеременной, ЗначениеПеременнойПараметр);
			КонецЕсли;	
			ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
				"# Переменная установлена
				|
				|- Имя: %1
				|- Значение: %2"),
				ИмяПеременной,
				MCPПредставлениеЗначенияДляMarkdown(ЗначениеПеременнойПараметр));
			Возврат ТекстОтвета;
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка установки переменной:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
	ИначеЕсли Действие = "delete" Тогда
		Если Не ЗначениеЗаполнено(ИмяПеременной) Тогда
			ТекстОшибки = Ванесса.Локализовать("Параметр name обязателен для action=delete.");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
		КонецЕсли;
		
		Попытка
			// Удаляем из обоих контекстов
			Если ОбъектКонтекст <> Неопределено И ТипЗнч(ОбъектКонтекст) = Тип("Структура") Тогда
				Попытка
					ОбъектКонтекст.Удалить(ИмяПеременной);
				Исключение
				КонецПопытки;
			КонецЕсли;
			
			ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
				"# Переменная удалена
				|
				|- Имя: %1"),
				ИмяПеременной);
			Возврат ТекстОтвета;
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка удаления:")+ " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
	Иначе
		ТекстОшибки = Ванесса.Локализовать("Неизвестное действие:") + " " + Действие + Ванесса.Локализовать(". Допустимые: get, set, delete.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
	КонецЕсли;
	
	
	Возврат Ванесса.Локализовать("Окно было успешно активизировано.");
	
КонецФункции

// Получает информацию из базы знаний
&НаКлиенте
Функция ПолучитьДанныеИзБазыЗнаний(Запрос, ДополнительныеПараметры) Экспорт
	
	ФорматОтвета = ЗначениеПараметраИзЗапроса(Запрос, "format");
	СтрокаПоиска = СокрЛП(ЗначениеПараметраИзЗапроса(Запрос, "search_string"));
	Попытка
		НомерВопроса = Число(ЗначениеПараметраИзЗапроса(Запрос, "question_number"));	
	Исключение
		НомерВопроса = Неопределено;
	КонецПопытки;
	
	Если НЕ ЗначениеЗаполнено(ФорматОтвета) Тогда
		ТекстСообщения = Ванесса.Локализовать("Не указан обязательный параметр 'format'");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если ФорматОтвета <> "short_info"
		И ФорматОтвета <> "questions_only"
		И ФорматОтвета <> "one_question"
		И ФорматОтвета <> "question_search"
		И ФорматОтвета <> "answers_search"
		И ФорматОтвета <> "all"
		Тогда
		ТекстСообщения = Ванесса.Локализовать("Передано недопустимое значение параметра 'format'");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если глКешБазыЗнаний = Неопределено Тогда
		глКешБазыЗнаний = ПолныйТекстБазыЗнанийСервер();
	КонецЕсли;
	
	Возврат ДанныеИзБазыЗнанийКлиент(ФорматОтвета, НомерВопроса, СтрокаПоиска);
	
КонецФункции

// Получает информацию о статистике использования шагов
&НаКлиенте
Функция ПолучитьДанныеОЧастоИспользуемыхШагах(Запрос, ДополнительныеПараметры) Экспорт
	
	Количество = ЗначениеПараметраИзЗапроса(Запрос, "limit");
	Если НЕ ЗначениеЗаполнено(Количество) Тогда
		Количество = 100;
	КонецЕсли;	
	
	Если глЧастоИспользуемыеШаги = Неопределено Тогда
		глЧастоИспользуемыеШаги = ПолучитьДанныеМакетаЧастоИспользуемыеШаги();
	КонецЕсли;
	
	Возврат ДанныеОЧастоИспользуемыхШагах(Количество);
	
КонецФункции

// Получает список расширений, установленных в базе
&НаКлиенте
Функция ПолучитьСписокРасширений(Запрос, ДополнительныеПараметры) Экспорт
	
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	Попытка
		// Получаем данные расширений с сервера
		ТекстОшибки = Неопределено;
		МассивДанныхРасширений = ПолучитьДанныеРасширенийКлиент(ТекстОшибки);
		Если ЗначениеЗаполнено(ТекстОшибки) Тогда
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		// Формируем массив строк на клиенте
		Строки = СформироватьМассивСтрокРасширений(МассивДанныхРасширений);
		
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
		
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка получения списка расширений:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Универсальный инструмент для работы с таблицей профилей клиентов тестирования
&НаКлиенте
Функция УправлениеПрофилямиКлиентовТестирования(Запрос, ДополнительныеПараметры) Экспорт
	
	Действие = Строка(ЗначениеПараметраИзЗапроса(Запрос, "action"));
	Если НЕ ЗначениеЗаполнено(Действие) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр action обязателен. Допустимые значения: 'get_list', 'add', 'edit'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если Действие = "get_list" Тогда
		Возврат ПолучитьСписокПрофилейКлиентовТестирования();
	ИначеЕсли Действие = "add" Тогда
		Возврат ДобавитьПрофильКлиентаТестирования(Запрос);
	ИначеЕсли Действие = "edit" Тогда
		Возврат РедактироватьПрофильКлиентаТестирования(Запрос);
	Иначе
		ТекстСообщения = Ванесса.Локализовать("Неизвестное действие:") + " " + Действие + ". " + Ванесса.Локализовать("Допустимые: 'get_list', 'add', 'edit'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
КонецФункции

// Получает список профилей клиентов тестирования
&НаКлиенте
Функция ПолучитьСписокПрофилейКлиентовТестирования()
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Список профилей клиентов тестирования"));
	Строки.Добавить("");
	
	Если Ванесса.ДанныеКлиентовТестирования.Количество() = 0 Тогда
		Строки.Добавить(Ванесса.Локализовать("Профили не настроены. Добавьте профиль на вкладке 'Клиенты тестирования' в настройках VA."));
	Иначе
		Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Найдено профилей: %1"), Ванесса.ДанныеКлиентовТестирования.Количество()));
		Строки.Добавить("");
		Для Каждого СтрокаПрофиля Из Ванесса.ДанныеКлиентовТестирования Цикл
			Строки.Добавить(Ванесса.Локализовать("## Профиль: ") + СтрокаПрофиля.Имя);
			Строки.Добавить("");
			Строки.Добавить(Ванесса.Локализовать("| Свойство | Значение |"));
			Строки.Добавить(Ванесса.Локализовать("|----------|----------|"));
			Строки.Добавить(Ванесса.Локализовать("| Имя |") + " " + СтрокаПрофиля.Имя + " |");
			Строки.Добавить(Ванесса.Локализовать("| Синоним |") + " " + СтрокаПрофиля.Синоним + " |");
			Строки.Добавить(Ванесса.Локализовать("| Строка подключения |") + " " + СтрокаПрофиля.ПутьКИнфобазе + " |");
			Строки.Добавить(Ванесса.Локализовать("| Дополнительные параметры |") + " " + СтрокаПрофиля.ДопПараметры + " |");
			Строки.Добавить(Ванесса.Локализовать("| Тип клиента |") + " " + СтрокаПрофиля.ТипКлиента + " |");
			Строки.Добавить(Ванесса.Локализовать("| Имя компьютера |") + " " + СтрокаПрофиля.ИмяКомпьютера + " |");
			Строки.Добавить(Ванесса.Локализовать("| Дескриптор окна |") + " " + СтрокаПрофиля.ДескрипторОкна + " |");
			Строки.Добавить(Ванесса.Локализовать("| PID |") + " " + СтрокаПрофиля.PIDКлиентаТестирования + " |");
			СтатусПодключения = ?(СтрокаПрофиля.Подключен,Ванесса.Локализовать("✅ подключен"),Ванесса.Локализовать("❌ не подключен"));
			Строки.Добавить(Ванесса.Локализовать("| Статус |") + " " + СтатусПодключения + " |");
			Строки.Добавить("");
		КонецЦикла;
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Добавляет профиль клиента тестирования
&НаКлиенте
Функция ДобавитьПрофильКлиентаТестирования(Запрос)
	
	ИмяПрофиля = Строка(ЗначениеПараметраИзЗапроса(Запрос, "name"));
	Если НЕ ЗначениеЗаполнено(ИмяПрофиля) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр name обязателен для добавления профиля.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Проверяем, нет ли уже профиля с таким именем
	Для Каждого СтрокаПрофиля Из Ванесса.ДанныеКлиентовТестирования Цикл
		Если НРег(СтрокаПрофиля.Имя) = НРег(ИмяПрофиля) ИЛИ НРег(СтрокаПрофиля.Синоним) = НРег(ИмяПрофиля) Тогда
			ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Профиль с именем '%1' уже существует."), ИмяПрофиля);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстСообщения);
		КонецЕсли;
	КонецЦикла;
	
	Попытка
		// Создаем новую строку в таблице профилей
		НоваяСтрока = Ванесса.ДанныеКлиентовТестирования.Добавить();
		НоваяСтрока.Имя = ИмяПрофиля;
		НоваяСтрока.Синоним = Строка(ЗначениеПараметраИзЗапроса(Запрос, "synonym"));
		НоваяСтрока.ПутьКИнфобазе = Строка(ЗначениеПараметраИзЗапроса(Запрос, "infobase_path"));
		НоваяСтрока.ДопПараметры = Строка(ЗначениеПараметраИзЗапроса(Запрос, "additional_parameters"));
		НоваяСтрока.ТипКлиента = Строка(ЗначениеПараметраИзЗапроса(Запрос, "client_type"));
		Если НЕ ЗначениеЗаполнено(НоваяСтрока.ТипКлиента) Тогда
			НоваяСтрока.ТипКлиента = "Тонкий";
		КонецЕсли;	
		НоваяСтрока.ИмяКомпьютера = Строка(ЗначениеПараметраИзЗапроса(Запрос, "computer_name"));
		НоваяСтрока.Подключен = Ложь;
		
		ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
			"# Профиль добавлен
			|
			|- Имя: %1
			|- Синоним: %2
			|
			|## Следующие шаги
			|- Используйте `connect_test_client` для подключения профиля"),
			ИмяПрофиля,
			НоваяСтрока.Синоним);
		Возврат ТекстОтвета;
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка добавления профиля:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Редактирует профиль клиента тестирования
&НаКлиенте
Функция РедактироватьПрофильКлиентаТестирования(Запрос)
	
	ИмяПрофиля = Строка(ЗначениеПараметраИзЗапроса(Запрос, "name"));
	Если НЕ ЗначениеЗаполнено(ИмяПрофиля) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр name обязателен для редактирования профиля.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Ищем профиль с таким именем
	НайденныйПрофиль = Неопределено;
	Для Каждого СтрокаПрофиля Из Ванесса.ДанныеКлиентовТестирования Цикл
		Если НРег(СтрокаПрофиля.Имя) = НРег(ИмяПрофиля) ИЛИ НРег(СтрокаПрофиля.Синоним) = НРег(ИмяПрофиля)  Тогда
			НайденныйПрофиль = СтрокаПрофиля;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если НайденныйПрофиль = Неопределено Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Профиль с именем '%1' не найден."), ИмяПрофиля);
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстСообщения);
	КонецЕсли;
	
	Попытка
		// Обновляем поля профиля
		ЗначениеСиноним = ЗначениеПараметраИзЗапроса(Запрос, "synonym");
		Если ЗначениеЗаполнено(ЗначениеСиноним) Тогда
			НайденныйПрофиль.Синоним = ЗначениеСиноним;
		КонецЕсли;
		
		ЗначениеПуть = ЗначениеПараметраИзЗапроса(Запрос, "infobase_path");
		Если ЗначениеЗаполнено(ЗначениеПуть) Тогда
			НайденныйПрофиль.ПутьКИнфобазе = ЗначениеПуть;
		КонецЕсли;
		
		ЗначениеДопПараметры = ЗначениеПараметраИзЗапроса(Запрос, "additional_parameters");
		Если ЗначениеЗаполнено(ЗначениеДопПараметры) Тогда
			НайденныйПрофиль.ДопПараметры = ЗначениеДопПараметры;
		КонецЕсли;
		
		ЗначениеТипКлиента = ЗначениеПараметраИзЗапроса(Запрос, "client_type");
		Если ЗначениеЗаполнено(ЗначениеТипКлиента) Тогда
			НайденныйПрофиль.ТипКлиента = ЗначениеТипКлиента;
		КонецЕсли;
		
		ЗначениеИмяКомпьютера = ЗначениеПараметраИзЗапроса(Запрос, "computer_name");
		Если ЗначениеЗаполнено(ЗначениеИмяКомпьютера) Тогда
			НайденныйПрофиль.ИмяКомпьютера = ЗначениеИмяКомпьютера;
		КонецЕсли;
		
		ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
			"# Профиль отредактирован
			|
			|- Имя: %1
			|- Синоним: %2
			|
			|## Следующие шаги
			|- Используйте `connect_test_client` для подключения профиля"),
			НайденныйПрофиль.Имя,
			НайденныйПрофиль.Синоним);
		Возврат ТекстОтвета;
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка редактирования профиля:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

&НаКлиенте
Функция ПолучитьДанныеРасширенийКлиент(ТекстОшибки)
	// Нельзя получать данные по расширениям напрямую из базы менеджера тестирования, т.к. он мог долго не перезапускаться и данные (возможно) могут отличаться от данных клиента тестирования.
	Возврат ПолучитьДанныеРасширенийСПомощьюРасшренияVAExtension(ТекстОшибки);
КонецФункции

&НаКлиенте
Функция ПолучитьДанныеРасширенийСПомощьюРасшренияVAExtension(ТекстОшибки)
	
	Текст = "
	|Результат = Новый Массив;
	|
	|Данные = РасширенияКонфигурации.Получить();
	|Для Каждого ТекРасширение Из Данные Цикл
	|	ДанныеРасширения = Новый Структура;
	|	ДанныеРасширения.Вставить(""Имя"", ТекРасширение.Имя);
	|	ДанныеРасширения.Вставить(""Версия"", ТекРасширение.Версия);
	|	ДанныеРасширения.Вставить(""Назначение"", Строка(ТекРасширение.Назначение));
	|	ДанныеРасширения.Вставить(""ОбластьДействия"", Строка(ТекРасширение.ОбластьДействия));
	|	ДанныеРасширения.Вставить(""Активно"", ТекРасширение.Активно);
	|	ДанныеРасширения.Вставить(""БезопасныйРежим"", Строка(ТекРасширение.БезопасныйРежим));
	|	ДанныеРасширения.Вставить(""ЗащитаОтОпасныхДействий"", Строка(ТекРасширение.ЗащитаОтОпасныхДействий));
	|	
	|	Результат.Добавить(ДанныеРасширения);
	|	
	|	ЗаписьJSON = Новый ЗаписьJSON;
	|	ЗаписьJSON.УстановитьСтроку();
	|	ЗаписатьJSON(ЗаписьJSON, Результат);
	|	ЗначениеДляВозврата = ЗаписьJSON.Закрыть();
	|	
	|КонецЦикла;
	|";
	
	Ванесса.ОткрытьНавигационнуюСсылку("e1cib/app/Обработка.VAExtension_ВыполнитьПроизвольныйКод");
	ТестовоеПриложение = Ванесса.ПолучитьТестовоеПриложение();
	НужнаяФорма = ТестовоеПриложение.НайтиОбъект(Тип("ТестируемаяФорма"),, "*.VAExtension_ВыполнитьПроизвольныйКод.*",5);
	Если НужнаяФорма = Неопределено Тогда
		ТекстОшибки = Ванесса.Локализовать("Клиент тестирования подключен, но не найдена форма 'VAExtension_ВыполнитьПроизвольныйКод' из служебного расширения.");
		Возврат Неопределено
	КонецЕсли;	
	
	РеквизитВыражениеВычислено = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ВыражениеВычислено", Истина);
	РеквизитКодДляВыполнения = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("КодДляВыполнения", Истина);
	РеквизитКодДляВыполнения.ВвестиТекст(Текст);
	
	АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	
	КнопкаВыполнитьКод = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяКнопкаФормы"),,"ФормаВыполнитьКодСервер");
	
	КнопкаЗакрытьФорму = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяКнопкаФормы"),,"ЗакрытьФорму");
	
	КнопкаВыполнитьКод.Нажать();
	
	БылаОшибка = Истина;
	Ванесса.ОжиданиеОкончанияОбработки(БылаОшибка, РеквизитВыражениеВычислено);
	
	Если БылаОшибка Тогда
		ТекстОшибки = Ванесса.Локализовать("Не получилось выполнить код для получения списка расширений на стороне клиента тестирования.");
		Возврат Неопределено
	КонецЕсли;
	
	РеквизитЗначениеДляВозврата = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ЗначениеДляВозврата", Истина);
	ПредставлениеРезультата = Ванесса.ПолучитьПредставлениеДанныхЭлементаФормы(РеквизитЗначениеДляВозврата);
	
	Данные = Ванесса.ПрочитатьОбъектJSON(ПредставлениеРезультата);
	
	Попытка
		КнопкаЗакрытьФорму.Нажать();
	Исключение
		Ванесса.ЗаписатьЛогВЖРИнформация("ПолучениеДанныхРасширений", ОписаниеОшибки());
	КонецПопытки;
	
	Возврат Данные;
	
КонецФункции

// Формирует массив строк для вывода информации о расширениях
&НаКлиенте
Функция СформироватьМассивСтрокРасширений(МассивДанныхРасширений)
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Список расширений, установленных в базе"));
	Строки.Добавить("");
	
	КоличествоРасширений = 0;
	Для Каждого ДанныеРасширения Из МассивДанныхРасширений Цикл
		КоличествоРасширений = КоличествоРасширений + 1;
		Строки.Добавить(Ванесса.Локализовать("## ") + КоличествоРасширений + ". " + ДанныеРасширения.Имя);
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("| Свойство | Значение |"));
		Строки.Добавить(Ванесса.Локализовать("|----------|----------|"));
		Строки.Добавить(Ванесса.Локализовать("| Имя |") + " " + ДанныеРасширения.Имя + " |");
		Строки.Добавить(Ванесса.Локализовать("| Версия |") + " " + ДанныеРасширения.Версия + " |");
		Строки.Добавить(Ванесса.Локализовать("| Назначение |") + " " + ДанныеРасширения.Назначение + " |");
		Строки.Добавить(Ванесса.Локализовать("| Активно |") + " " + MCPПредставлениеЗначенияДляMarkdown(ДанныеРасширения.Активно) + " |");
		Строки.Добавить(Ванесса.Локализовать("| Безопасный режим |") + " " + MCPПредставлениеЗначенияДляMarkdown(ДанныеРасширения.БезопасныйРежим) + " |");
		//Строки.Добавить(Ванесса.Локализовать("| Область действия |") + " " + ДанныеРасширения.ОбластьДействия + " |");
		Строки.Добавить("");
	КонецЦикла;
	
	Если КоличествоРасширений = 0 Тогда
		Строки.Добавить(Ванесса.Локализовать("Расширения не найдены."));
		Строки.Добавить("");
	Иначе
		Строки.Добавить(Ванесса.Локализовать("## Итого:") + " " + КоличествоРасширений + " " + Ванесса.Локализовать("расширений"));
		Строки.Добавить("");
	КонецЕсли;
	
	Возврат Строки;
	
КонецФункции

// Универсальный инструмент для управления командным интерфейсом
&НаКлиенте
Функция УправлениеКоманднымИнтерфейсом(Запрос, ДополнительныеПараметры) Экспорт
	
	Действие = Строка(ЗначениеПараметраИзЗапроса(Запрос, "action"));
	Если НЕ ЗначениеЗаполнено(Действие) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр action обязателен. Допустимые значения: 'get_section_panel', 'get_function_panel', 'click_section_panel', 'click_function_panel'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Проверка подключения клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	Если Действие = "get_section_panel" Тогда
		Возврат ПолучитьПанельРазделов(Запрос);
	ИначеЕсли Действие = "get_function_panel" Тогда
		Возврат ПолучитьПанельФункций(Запрос);
	ИначеЕсли Действие = "click_section_panel" Тогда
		Возврат НажатьКнопкуПанелиРазделов(Запрос);
	ИначеЕсли Действие = "click_function_panel" Тогда
		Возврат НажатьКнопкуПанелиФункций(Запрос);
	Иначе
		ТекстСообщения = Ванесса.Локализовать("Неизвестное действие:") + " " + Действие + ". " + Ванесса.Локализовать("Допустимые: 'get_section_panel', 'get_function_panel', 'click_section_panel', 'click_function_panel'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
КонецФункции

// Получает панель разделов (внутренняя реализация)
&НаКлиенте
Функция ПолучитьПанельРазделов(Запрос)
	
	ПанельРазделов = Ванесса.ПанельРазделов();
	Если ПанельРазделов = Неопределено Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Панель разделов пуста или недоступна."));
	КонецЕсли;
	СписокКоманд = ПанельРазделов.НайтиОбъекты();
	
	// Markdown формат
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Панель разделов"));
	Строки.Добавить("");
	
	Если СписокКоманд.Количество() = 0 Тогда
		Строки.Добавить(Ванесса.Локализовать("Панель разделов пуста."));
	Иначе
		Для Каждого Команда Из СписокКоманд Цикл
			Строки.Добавить(Ванесса.Локализовать("- Команда:") + " " + Команда.ТекстЗаголовка);
			//Если ЗначениеЗаполнено(Команда.НавигационнаяСсылка) Тогда
			//	Строки.Добавить("  " + "- НавигационнаяСсылка:" + " " + Команда.НавигационнаяСсылка);
			//КонецЕсли;
			// Навигационные ссылки не добавляются для панели разделов, т.к. для них не работает переход по навигационной ссылке
			Строки.Добавить("");
		КонецЦикла;
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Получает панель функций (внутренняя реализация)
&НаКлиенте
Функция ПолучитьПанельФункций(Запрос)
	
	ПанельФункций = Ванесса.ПанельФункций();
	
	Если ПанельФункций = Неопределено Тогда
		Возврат Ванесса.Локализовать("Панель функций пуста или недоступна. Возможно, не выбран раздел в панели разделов. Либо повторное нажатие на одну и туже команду панели разделов скрыло панель функций.");
	КонецЕсли;
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Панель функций"));
	Строки.Добавить("");
	
	ТекстОшибки = Неопределено;
	ДобавитьВМассивСтрокДанныеПанелиФункций(Строки, ТекстОшибки);
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;	
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Нажимает кнопку панели разделов (внутренняя реализация)
&НаКлиенте
Функция НажатьКнопкуПанелиРазделов(Запрос)
	
	Команда = Строка(ЗначениеПараметраИзЗапроса(Запрос, "command"));
	Если НЕ ЗначениеЗаполнено(Команда) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр command обязателен для действия click_section_panel.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ВозвратФункции = ЗначениеПараметраИзЗапроса(Запрос, "returnFunctionPanel");
	Если НЕ ЗначениеЗаполнено(ВозвратФункции) Тогда
		ВозвратФункции = Ложь;
	Иначе
		Попытка
			ВозвратФункции = Булево(ВозвратФункции);
		Исключение
			ВозвратФункции = Ложь;
		КонецПопытки;
	КонецЕсли;
	
	// Получаем панель разделов
	ПанельРазделов = Ванесса.ПанельРазделов();
	
	Если ПанельРазделов = Неопределено Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Панель разделов пуста или недоступна."));
	КонецЕсли;
	
	// Ищем и нажимаем кнопку
	Попытка
		ДопПараметры = Новый Структура;
		ДопПараметры.Вставить("КоличествоПопыток", 1);
		Ванесса.ВПанелиРазделовЯВыбираю(Команда, ДопПараметры);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Не удалось нажать на команду панели разделов.") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	// Формируем ответ
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("# Команда панели разделов нажата: %1"), Команда));
	Строки.Добавить("");
	
	Если ВозвратФункции Тогда
		ТекстОшибки = Неопределено;
		ДобавитьВМассивСтрокДанныеПанелиФункций(Строки, ТекстОшибки);
		// Возврат панели разделов в предыдущее состояние
		Ванесса.ВПанелиРазделовЯВыбираю(Команда, ДопПараметры);
		Если ЗначениеЗаполнено(ТекстОшибки) Тогда
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;	
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

&НаКлиенте
Процедура ДобавитьВМассивСтрокДанныеПанелиФункций(Строки, ТекстОшибки)
	Строки.Добавить(Ванесса.Локализовать("## Панель функций"));
	Строки.Добавить("");
	БылиЭлементы = Ложь;
	// Получаем панель функций после нажатия
	Попытка
		ПанельФункций = Ванесса.ПанельФункций();
		Если ПанельФункций <> Неопределено Тогда
			
			ДанныеПанелиФункций = ДанныеПанелиФункций(ПанельФункций);
			ЭлементыБезГруппы = ДанныеПанелиФункций.ЭлементыБезГруппы;
			КешГрупп = ДанныеПанелиФункций.КешГрупп;
			
			Если ЭлементыБезГруппы.Количество() > 0 Тогда
				Для Каждого Элем Из ЭлементыБезГруппы Цикл
					Строки.Добавить("- `" + Элем.ТекстЗаголовка + "`");
					БылиЭлементы = Истина;
				КонецЦикла;
				Строки.Добавить("");
			КонецЕсли;
			
			Для Каждого КлючЗначение Из КешГрупп Цикл
				Строки.Добавить(Ванесса.Локализовать("# Группа `") + КлючЗначение.Ключ + "`");
				ДанныеГруппы = КлючЗначение.Значение;
				Для Каждого Элем Из ДанныеГруппы Цикл
					Строки.Добавить("* `" + Элем.ТекстЗаголовка + "`");
					Строки.Добавить("  " + "- Навигацонная ссылка:" + " " + Элем.НавигационнаяСсылка);
					Строки.Добавить("");
					БылиЭлементы = Истина;
				КонецЦикла;	
			КонецЦикла;	
			
		КонецЕсли;
		
		Если НЕ БылиЭлементы Тогда
			Строки.Добавить(Ванесса.Локализовать("Панель функций пуста."));
		КонецЕсли;	
		
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Не удалось нажать на команду панели функций.") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Функция ДанныеПанелиФункций(ПанельФункций)
	
	ЭлементыБезГруппы = Новый Массив;
	КешГрупп = Новый Соответствие;

	Результат = Новый Структура;
	Результат.Вставить("ЭлементыБезГруппы", ЭлементыБезГруппы);
	Результат.Вставить("КешГрупп", КешГрупп);
	
	СписокКомандФункций = ПанельФункций.НайтиОбъекты();
	Если СписокКомандФункций.Количество() = 0 Тогда
	Иначе
		КешНавигационныхССылок = Новый Соответствие;
		
		Для Каждого КомандаФункции Из СписокКомандФункций Цикл
			Если ТипЗнч(КомандаФункции) = Тип("ТестируемаяГруппаКомандногоИнтерфейса") Тогда
				Если КешГрупп[КомандаФункции.ТекстЗаголовка] = Неопределено Тогда
					ДанныеГруппы = Новый Массив;
					КешГрупп.Вставить(КомандаФункции.ТекстЗаголовка, ДанныеГруппы);
				КонецЕсли;
				
				ДанныеГруппы = КешГрупп[КомандаФункции.ТекстЗаголовка];
				
				ЭлементыГруппы = КомандаФункции.НайтиОбъекты();
				Для Каждого Элем Из ЭлементыГруппы Цикл
					Если ЗначениеЗаполнено(Элем.НавигационнаяСсылка) Тогда
						Если КешНавигационныхССылок[Элем.НавигационнаяСсылка] <> Неопределено Тогда
							Продолжить;
						КонецЕсли;
						КешНавигационныхССылок.Вставить(Элем.НавигационнаяСсылка, Элем);
					КонецЕсли;	
					ДанныеГруппы.Добавить(Элем);
				КонецЦикла;	
			КонецЕсли;
		КонецЦикла;
		
		// собираем элемены без группы
		Для Каждого КомандаФункции Из СписокКомандФункций Цикл
			Если ТипЗнч(КомандаФункции) = Тип("ТестируемаяГруппаКомандногоИнтерфейса") Тогда
				Продолжить;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(КомандаФункции.НавигационнаяСсылка) Тогда
				Если КешНавигационныхССылок[КомандаФункции.НавигационнаяСсылка] <> Неопределено Тогда
					Продолжить;
				КонецЕсли;
			КонецЕсли;	
			
			ЭлементыБезГруппы.Добавить(КомандаФункции);
			
		КонецЦикла;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

// Нажимает кнопку панели функций (внутренняя реализация)
&НаКлиенте
Функция НажатьКнопкуПанелиФункций(Запрос)
	
	Команда = Строка(ЗначениеПараметраИзЗапроса(Запрос, "command"));
	Если НЕ ЗначениеЗаполнено(Команда) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр command обязателен для действия click_function_panel.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Группа = Строка(ЗначениеПараметраИзЗапроса(Запрос, "group"));
	
	// Получаем панель функций
	ПанельФункций = Неопределено;
	Попытка
		ПанельФункций = Ванесса.ПанельФункций();
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Не удалось получить панель функций.") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	Если ПанельФункций = Неопределено Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Панель функций пуста или недоступна. Возможно, не выбран раздел в панели разделов."));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Группа) Тогда
		//Ищем группу
		
		ЭлементыГруппа = ПанельФункций.НайтиОбъекты(Тип("ТестируемаяГруппаКомандногоИнтерфейса"),Группа);
		Если ЭлементыГруппа.Количество() = 0 Тогда
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В панели функций не найдена группа '%1'"), Группа));
		КонецЕсли;
		
		Если ЭлементыГруппа.Количество() > 1 Тогда
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В панели функций найдено %1 групп с заголовоком '%2'"), XMLСтрока(ЭлементыГруппа.Количество()), Группа));
		КонецЕсли;
		
		ЭлементФункция = ЭлементыГруппа[0].НайтиОбъект(,Команда);
		Если ЭлементФункция = Неопределено Тогда
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В панели функций в группе '%1' не найдена команда '%2'"), Группа, Команда));
		КонецЕсли;
		
		Попытка
			ЭлементФункция.Нажать();
		Исключение
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В панели функций не получлось нажать на команду '%1'"), Команда)) + Символы.ПС + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		КонецПопытки;
	Иначе	
		ЭлементФункция = ПанельФункций.НайтиОбъект(,Команда);
		Если ЭлементФункция = Неопределено Тогда
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В панели функций не найдена команда '%1'"), Команда));
		КонецЕсли;
		
		Попытка
			ЭлементФункция.Нажать();
		Исключение
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В панели функций не получлось нажать на команду '%1'"), Команда)) + Символы.ПС + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		КонецПопытки;
	КонецЕсли;	
	
	Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("# Команда панели функций нажата: %1"), Команда);
	
КонецФункции

// Получает информацию о строке сценария. Если в строке находится шаг - то возвращает подробную информацию о шаге.
&НаКлиенте
Функция ПолучитьПодробнуюИнформациюОШагеВСтрокеСценария(Запрос, ДополнительныеПараметры) Экспорт
	
	НомерСтроки = Число(ЗначениеПараметраИзЗапроса(Запрос, "lineNumber"));	
	Если НЕ ЗначениеЗаполнено(НомерСтроки) Тогда
		ТекстСообщения = Ванесса.Локализовать("Не указан обязательный параметр 'lineNumber'");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	СтрокаДерева = Ванесса.СтрокаДереваПоНомеруСтрокиФичи(НомерСтроки);
	
	ЭкземплярVanessaEditor = Ванесса.ПолучитьVanessaEditor();
	Если СтрокаДерева = Неопределено Тогда
		Попытка
			ТекстСтроки = ЭкземплярVanessaEditor.getLineContent(НомерСтроки);
			ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В строке %1 не найден шаг.
			|Текст в строке: %2"), XMLСтрока(НомерСтроки), ТекстСтроки);
			Возврат ТекстСообщения;
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
	КонецЕсли;
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("### Данные строки"));
	Строки.Добавить("");
	ТекстСтроки = СокрЛ(ЭкземплярVanessaEditor.getLineContent(НомерСтроки));
	Строки.Добавить(Ванесса.Локализовать("Текст в строке:") + " " + ТекстСтроки);
	Строки.Добавить(Ванесса.Локализовать("Шаг в строке:") + " " + СтрокаДерева.Имя);
	Строки.Добавить(Ванесса.Локализовать("Снипет:") + " " + СтрокаДерева.Снипет);
	Строки.Добавить(Ванесса.Локализовать("Тип данных в строке:") + " " + СтрокаДерева.Тип);
	Если ЗначениеЗаполнено(СтрокаДерева.ДопТип) Тогда
		Строки.Добавить(Ванесса.Локализовать("Уточнение типа:") + " " + СтрокаДерева.ДопТип);
	КонецЕсли;	
	
	Если СтрокаДерева.Тип = "Шаг" Тогда
		СтрокаДляПоиска = ПолучитьСтрокуДляПоискаШагаПоСнипету(СтрокаДерева.Снипет);
		Отбор = Новый Структура();
		Отбор.Вставить("СтрокаДляПоиска", СтрокаДляПоиска);
		СтрокиТаблицы = Ванесса.ТаблицаИзвестныхStepDefinition.НайтиСтроки(Отбор);
		СтрокаТаблицаИзвестныхStepDefinition = Неопределено;
		Если СтрокиТаблицы.Количество() > 0 Тогда
			СтрокаТаблицаИзвестныхStepDefinition = СтрокиТаблицы[0];
		КонецЕсли;	
		Если СтрокаТаблицаИзвестныхStepDefinition = Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("В таблице известных шагов не найден шаг:") + " " + СтрокаДерева.Имя);
		Иначе	
			Строки.Добавить(Ванесса.Локализовать("## Данные шага:"));
			Строки.Добавить(Ванесса.Локализовать("  " + "- Тип шага:") + " " + СтрокаТаблицаИзвестныхStepDefinition.ТипШага);
			ИмяФайла = СтрокаТаблицаИзвестныхStepDefinition.ИмяФайла;
			Если НЕ Ванесса.ЭтоLinuxКлиент() Тогда
				ИмяФайла = СтрЗаменить(ИмяФайла, "/", "\");
			КонецЕсли;	
			Строки.Добавить(Ванесса.Локализовать("  " + "- Имя файла где находится реализация шага:") + " " + ИмяФайла);
			Строки.Добавить(Ванесса.Локализовать("# Описание шага:"));
			Строки.Добавить(СтрокаТаблицаИзвестныхStepDefinition.ОписаниеШага);
		КонецЕсли;	
	КонецЕсли;	
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Добавляет, читает и удаляет точки останова
&НаКлиенте
Функция УправлениеТочкамиОстанова(Запрос, ДополнительныеПараметры) Экспорт
	
	Если НЕ Ванесса.Объект.ИспользоватьРедакторVanessaEditor Тогда
		ТекстОшибки = Ванесса.Локализовать("Работать с точками останова можно только когда используется основной редактор Vanessa Automation, а сейчас он выключен.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;	
	
	Действие = Строка(ЗначениеПараметраИзЗапроса(Запрос, "action"));
	Если НЕ ЗначениеЗаполнено(Действие) Тогда
		Действие = "toggle";
	КонецЕсли;
	
	Если Действие = "list" Тогда
		Попытка
			МассивБрейкпоинтов = Ванесса.БрейкпоинтыVanessaEditor();
			Строки = Новый Массив;
			Строки.Добавить(Ванесса.Локализовать("# Точки останова"));
			Строки.Добавить("");
			Если МассивБрейкпоинтов <> Неопределено И ТипЗнч(МассивБрейкпоинтов) = Тип("Массив") И МассивБрейкпоинтов.Количество() > 0 Тогда
				Строки.Добавить(Ванесса.Локализовать("Количество:") + " " + Формат(МассивБрейкпоинтов.Количество(), "ЧГ=0; ЧН=0"));
				Для Каждого ДанныеТочкиОстнова Из МассивБрейкпоинтов Цикл
					Строки.Добавить("- " + ДанныеТочкиОстнова.НомерСтроки);
				КонецЦикла;
			Иначе
				Строки.Добавить(Ванесса.Локализовать("Точек останова нет."));
				Строки.Добавить("");
				// Контекст: показать открытые файлы
				Если Не Ванесса.ОтсутствуетVanessaEditor() Тогда
					Попытка
						Редактор = Ванесса.ПолучитьVanessaEditor();
						СписокВкладок = Редактор.tabsGetAll();
						Если ТипЗнч(СписокВкладок) = Тип("Массив") И СписокВкладок.Количество() > 0 Тогда
							Строки.Добавить("## Открытые файлы");
							Для Каждого Вкладка Из СписокВкладок Цикл
								Строки.Добавить("- " + Строка(Вкладка));
							КонецЦикла;
						Иначе
							Строки.Добавить(Ванесса.Локализовать("Файлы не открыты. Загрузите фичи (`load_features`) и откройте сценарий."));
						КонецЕсли;
					Исключение
						Строки.Добавить(Ванесса.Локализовать("Загрузите фичи (`load_features`) и откройте сценарий для работы с точками останова."));
					КонецПопытки;
				Иначе
					Строки.Добавить(Ванесса.Локализовать("Редактор не доступен. Загрузите фичи (`load_features`) для инициализации."));
				КонецЕсли;
				Строки.Добавить("");
				Строки.Добавить(Ванесса.Локализовать("## Как добавить"));
				Строки.Добавить(Ванесса.Локализовать("- Используйте `manage_breakpoints(action='toggle', line=N)` для переключения точки останова на строке N."));
			КонецЕсли;
			
			Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
	ИначеЕсли Действие = "remove_all" Тогда
		Попытка
			МассивБрейкпоинтов = Ванесса.Брейкпоинты();
			Количество = 0;
			Если МассивБрейкпоинтов <> Неопределено И ТипЗнч(МассивБрейкпоинтов) = Тип("Массив") Тогда
				Количество = МассивБрейкпоинтов.Количество();
			КонецЕсли;
			
			// Удаляем все точки останова через API редактора
			Если НЕ Ванесса.ОтсутствуетVanessaEditor() Тогда
				Ванесса.УдалитьВсеТочкиОстановаРедактора();
			КонецЕсли;
			
			ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
				"# Точки останова удалены
				|
				|- Удалено: %1"),
				Формат(Количество, "ЧГ=0; ЧН=0"));
			Возврат ТекстОтвета;
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
	ИначеЕсли Действие = "toggle" Тогда
		Попытка
			
			Если НЕ Ванесса.ЭтоВкладкаEdit(Ванесса.VanessaTabs.current) Тогда
				ТекстОшибки = Ванесса.Локализовать("В редакторе не открыт фича файл.");
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;	
			
			НомерСтроки = Число(ЗначениеПараметраИзЗапроса(Запрос, "line"));
			РедакторДоступен = НЕ Ванесса.ОтсутствуетVanessaEditor();
			
			Если НомерСтроки <> Неопределено И РедакторДоступен Тогда
				Редактор = Ванесса.ПолучитьVanessaEditor();
				Редактор.toggleBreakpoint(НомерСтроки);
			ИначеЕсли НомерСтроки <> Неопределено И Не РедакторДоступен Тогда
				ТекстОшибки = Ванесса.Локализовать("Редактор Monaco недоступен. Загрузите фичи (`load_features`) для инициализации редактора.");
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			Иначе
				Ванесса.ОбработатьУстановкуСнятиеБрейкпоинта();
			КонецЕсли;
			
			Если НомерСтроки <> Неопределено Тогда
				ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
					"# Брейкпоинт переключён
					|
					|- Строка: %1"),
					Формат(НомерСтроки, "ЧГ=0; ЧН=0"));
			Иначе
				ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
					"# Брейкпоинт переключён
					|
					|- Строка: текущая"));
			КонецЕсли;
			Возврат ТекстОтвета;
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
	Иначе
		ТекстОшибки = Ванесса.Локализовать("Неизвестное действие:") + " " + Действие + ". " + Ванесса.Локализовать("Допустимые: toggle, remove_all, list.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;
	
КонецФункции

// Выполняет сценарий или все сценарии
&НаКлиенте
Функция ВыполнитьСценарий(Запрос, ДополнительныеПараметры) Экспорт
	
	Если Ванесса.Объект.ИдетВыполнениеСценариев Тогда
		ТекстСообщения = Ванесса.Локализовать("Сценарии уже выполняются. Дождитесь завершения или вызовите stopExecution.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстСообщения);
	КонецЕсли;
	
	Режим = "all";
	Значение = Строка(ЗначениеПараметраИзЗапроса(Запрос, "mode"));
	Если ЗначениеЗаполнено(Значение) Тогда
		Режим = Значение;
	КонецЕсли;	
	
	// Параметр filePath: загрузить фичу и открыть в редакторе перед запуском
	ПутьКФайлу = Строка(ЗначениеПараметраИзЗапроса(Запрос, "filePath"));
	Попытка
		НомерСтроки = Число(ЗначениеПараметраИзЗапроса(Запрос, "linenumber"));
	Исключение
		НомерСтроки = Неопределено;
	КонецПопытки;

	Идентификатор = Запрос.Идентификатор;
	Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
	
	ДопПараметрыВызоваMCP = Новый Структура;
	ДопПараметрыВызоваMCP.Вставить("Тип", "ВыполнитьСценарий");
	ДопПараметрыВызоваMCP.Вставить("ПутьКФайлу", ПутьКФайлу);
	ДопПараметрыВызоваMCP.Вставить("Режим", Режим);
	ДопПараметрыВызоваMCP.Вставить("Идентификатор", Идентификатор);
	ДопПараметрыВызоваMCP.Вставить("НомерСтроки", НомерСтроки);
	
	ПродолжитьВыполнитьСценарий1(ПутьКФайлу, ДопПараметрыВызоваMCP);
	
КонецФункции

// Останавливает выполнение сценариев
&НаКлиенте
Функция ОстановитьВыполнениеСценариев(Запрос, ДополнительныеПараметры) Экспорт
	
	БылоВыполнение = Ванесса.Объект.ИдетВыполнениеСценариев;
	
	Попытка
		Ванесса.КомандаОстановитьСценарии();
	Исключение
	КонецПопытки;
	
	Если Не БылоВыполнение Тогда
		ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
			"# Остановка выполнения
			|
			|- Остановлено: да
			|- Было активное выполнение: %1
			|
			|Сценарии не выполнялись. Используйте `run_scenario` для запуска."),
			MCPПредставлениеЗначенияДляMarkdown(БылоВыполнение));
	Иначе
		ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
			"# Остановка выполнения
			|
			|- Остановлено: да
			|- Было активное выполнение: %1
			|
			|## Следующие шаги
			|- Получите результаты: `get_test_results`"),
			MCPПредставлениеЗначенияДляMarkdown(БылоВыполнение));
	КонецЕсли;
		
	Ванесса.СообщитьПользователю(Ванесса.Локализовать("Выполнена остановка выполнения сценариев"));
	
	Возврат ТекстОтвета;
	
КонецФункции

// Получает результат выполнения тестов
&НаКлиенте
Функция ПолучитьРезультатВыполненияТестов(Запрос, ДополнительныеПараметры) Экспорт
	
	Результат = Неопределено;
	ИдСценария = Строка(ЗначениеПараметраИзЗапроса(Запрос, "scenarioId"));

	Если ЗначениеЗаполнено(ИдСценария) Тогда
		Попытка
			Результат = Ванесса.ПолучитьРезультатПрохожденияТестовСценария(ИдСценария);
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка получения результата:") + " " + ОписаниеОшибки();
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
	Иначе
		// Последний результат
		
		МассивРезультатПрохожденияТестовСценария = Ванесса.ПолучитьМассивРезультатПрохожденияТестовСценария();	
		
		Попытка
			Если МассивРезультатПрохожденияТестовСценария <> Неопределено 
				И МассивРезультатПрохожденияТестовСценария.Количество() > 0 Тогда
				Результат = МассивРезультатПрохожденияТестовСценария[МассивРезультатПрохожденияТестовСценария.Количество() - 1];
			КонецЕсли;
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	Если Результат = Неопределено Тогда
		
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
				"# Результаты тестов
				|
				|Нет результатов выполнения.
				|
				|## Следующие шаги
				|- Загрузите фичи: `load_features`
				|- Запустите сценарий: `run_scenario`
				|- Затем повторите: `get_test_results`"));
		
		Возврат ТекстСообщения;
	КонецЕсли;
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
		"# Результаты тестов
		|
		|- Статус: %1"),
		MCPПредставлениеЗначенияДляMarkdown(MCPБезопасноеСвойство(Результат, "Статус"))));
	
	ВремяНач = MCPБезопасноеСвойство(Результат, "ВремяНачала");
	ВремяКон = MCPБезопасноеСвойство(Результат, "ВремяОкончания");
	Если ВремяНач <> Неопределено И ВремяКон <> Неопределено Тогда
		Строки.Добавить(Ванесса.Локализовать("- Длительность:") + " " + Формат(ВремяКон - ВремяНач, "ЧГ=0; ЧН=0") + Ванесса.Локализовать(" мс"));
	КонецЕсли;
	
	ОписаниеОшибкиРез = MCPБезопасноеСвойство(Результат, "ОписаниеОшибки");
	Если ЗначениеЗаполнено(ОписаниеОшибкиРез) Тогда
		Строки.Добавить(Ванесса.Локализовать("- Ошибка:") + " " + ОписаниеОшибкиРез);
	КонецЕсли;
	Строки.Добавить("");
	
	ШагиРезультаты = MCPБезопасноеСвойство(Результат, "РезультатПрохожденияТестовШагов");
	Если ШагиРезультаты <> Неопределено И ТипЗнч(ШагиРезультаты) = Тип("Массив") Тогда
		Строки.Добавить(Ванесса.Локализовать("## Шаги (") + Формат(ШагиРезультаты.Количество(), "ЧГ=0; ЧН=0") + ")");
		Номер = 0;
		Для Каждого ШагРез Из ШагиРезультаты Цикл
			Номер = Номер + 1;
			ИмяШага = MCPПредставлениеЗначенияДляMarkdown(MCPБезопасноеСвойство(ШагРез, "Имя"));
			СтатусШага = MCPПредставлениеЗначенияДляMarkdown(MCPБезопасноеСвойство(ШагРез, "Статус"));
			
			ВремяНачШ = MCPБезопасноеСвойство(ШагРез, "ВремяНачала");
			ВремяКонШ = MCPБезопасноеСвойство(ШагРез, "ВремяОкончания");
			ВремяШага = "";
			Если ВремяНачШ <> Неопределено И ВремяКонШ <> Неопределено Тогда
				ВремяШага = " (" + Формат(ВремяКонШ - ВремяНачШ, "ЧГ=0; ЧН=0") + " " + Ванесса.Локализовать("мс)");
			КонецЕсли;
			
			ОшибкаШага = ТекстОшибкиИзОписанияОшибки(MCPБезопасноеСвойство(ШагРез, "ОписаниеОшибки"));
			
			Строки.Добавить(Формат(Номер, "ЧГ=0; ЧН=0") + ". " + ИмяШага + " → **" + СтатусШага + "**" + ВремяШага);
			Если ЗначениеЗаполнено(ОшибкаШага) Тогда
				Строки.Добавить(Ванесса.Локализовать("   " + "- Ошибка:") + " " + ОшибкаШага);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);

КонецФункции

// Возвращает текущую дату на сервере
&НаКлиенте
Функция ПолучитьТекущуюДатуНаСервере1С(Запрос, ДополнительныеПараметры) Экспорт
	Возврат ПолучитьТекущуюДатуНаСервере1СMCP_Сервер();
КонецФункции

// Возвращает состояние Vanessa Automation
&НаКлиенте
Функция ПолучитьСостояниеVanessaAutomation(Запрос, ДополнительныеПараметры) Экспорт

	Результат = Новый Структура;
	Результат.Вставить("isRunning", Ванесса.Объект.ИдетВыполнениеСценариев = Истина);
	Результат.Вставить("featureDirectory", Ванесса.Объект.КаталогФич);
	Результат.Вставить("КаталогПроекта", Ванесса.Объект.КаталогПроекта);
	
	// Реальная проверка подключения клиента тестирования
	Попытка
		АктивноеОкноVA = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
		Значение = АктивноеОкноVA <> Неопределено;
		ТекущиеДанныеКлиентаТестирования = Ванесса.ТекущиеДанныеКлиентаТестирования();
		Если ТекущиеДанныеКлиентаТестирования = Неопределено Тогда
			Значение = Ложь;
		КонецЕсли;	
		Результат.Вставить("testClientConnected", Значение);
		Если Значение Тогда
			ТекущиеДанныеКлиентаТестирования = Ванесса.ТекущиеДанныеКлиентаТестирования();
			Если ТекущиеДанныеКлиентаТестирования <> Неопределено Тогда
				Результат.Вставить("ИмяПрофиляКлиентаТестирования", ТекущиеДанныеКлиентаТестирования.Имя);
			КонецЕсли;	
		КонецЕсли;	
	Исключение
		Результат.Вставить("testClientConnected", Ложь);
	КонецПопытки;
	
	Попытка
		СостояниеVA = Ванесса.ПолучитьСостояниеVanessaAutomation();
	Исключение
		СостояниеVA = Неопределено;
	КонецПопытки;
	
	Если СостояниеVA <> Неопределено Тогда
		
		Если СостояниеVA.Свойство("ТекущаяФича")
			И ТипЗнч(СостояниеVA.ТекущаяФича) = Тип("Структура")
			И НЕ ЗначениеЗаполнено(СостояниеVA.ТекущаяФича.ПолныйПуть)
			И Ванесса.Объект.ИспользоватьРедакторVanessaEditor
			И Ванесса.ЭтоВкладкаEdit(Ванесса.VanessaTabs.current)
			Тогда
			// Значит фича была загружена, но ещё не запускалась. Сообщим об этом.
			ТекстСообщения = Ванесса.Локализовать("В редактор был загржен фича файл, но ещё ни разу не запускался. Для получения подробной информации о загруженном файле нужно вызвать get_editor_state.
				|Имя загруженного feature файла:" + " " + Ванесса.Объект.КаталогФич);
			Возврат ТекстСообщения;
		КонецЕсли;	
		
		// Текущая фича
		Если СостояниеVA.Свойство("ТекущаяФича") И ТипЗнч(СостояниеVA.ТекущаяФича) = Тип("Структура") Тогда
			Фича = Новый Структура;
			Фича.Вставить("name", MCPБезопасноеСвойство(СостояниеVA.ТекущаяФича, "Имя"));
			Фича.Вставить("fullPath", MCPБезопасноеСвойство(СостояниеVA.ТекущаяФича, "ПолныйПуть"));
			Фича.Вставить("language", MCPБезопасноеСвойство(СостояниеVA.ТекущаяФича, "ЯзыкФичи"));
			Фича.Вставить("tags", MCPБезопасноеСвойство(СостояниеVA.ТекущаяФича, "МассивТегов"));
			Результат.Вставить("currentFeature", Фича);
		Иначе
			Результат.Вставить("currentFeature", Неопределено);
		КонецЕсли;
		
		// Текущий сценарий
		Если СостояниеVA.Свойство("ТекущийСценарий") И ТипЗнч(СостояниеVA.ТекущийСценарий) = Тип("Структура") Тогда
			Сценарий = Новый Структура;
			Сценарий.Вставить("name", MCPБезопасноеСвойство(СостояниеVA.ТекущийСценарий, "Имя"));
			Сценарий.Вставить("id", MCPБезопасноеСвойство(СостояниеVA.ТекущийСценарий, "ИдСценария"));
			Сценарий.Вставить("result", MCPБезопасноеСвойство(СостояниеVA.ТекущийСценарий, "РезультатПрохожденияТестовСценария"));
			Сценарий.Вставить("tags", MCPБезопасноеСвойство(СостояниеVA.ТекущийСценарий, "МассивТегов"));
			Сценарий.Вставить("retryNumber", MCPБезопасноеСвойство(СостояниеVA.ТекущийСценарий, "НомерПопыткиВыполненияСценария"));
			Результат.Вставить("currentScenario", Сценарий);
		Иначе
			Результат.Вставить("currentScenario", Неопределено);
		КонецЕсли;
		
		// Текущий шаг
		Если СостояниеVA.Свойство("ТекущийШаг") И ТипЗнч(СостояниеVA.ТекущийШаг) = Тип("Структура") Тогда
			Шаг = Новый Структура;
			Шаг.Вставить("name", MCPБезопасноеСвойство(СостояниеVA.ТекущийШаг, "Имя"));
			Шаг.Вставить("paramValues", MCPБезопасноеСвойство(СостояниеVA.ТекущийШаг, "ЗначенияПараметров"));
			Шаг.Вставить("lineNumber", MCPБезопасноеСвойство(СостояниеVA.ТекущийШаг, "НомерСтрокиВФиче"));
			Результат.Вставить("currentStep", Шаг);
		Иначе
			Результат.Вставить("currentStep", Неопределено);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат MCPСформироватьMarkdownСостоянияVA(Результат);
	
КонецФункции

// Выполняет синтакс контроль
&НаКлиенте
Функция СинтаксКонтроль(Запрос, ДополнительныеПараметры) Экспорт
	
	ПутьКФайлу = Строка(ЗначениеПараметраИзЗапроса(Запрос, "filePath"));
	
	СтрокаСообщения = Ванесса.Локализовать("Выполнение синтакс контроля.
	|filePath=%1");
	СтрокаСообщения = Ванесса.ПодставитьПараметрыВСтроку(СтрокаСообщения, ПутьКФайлу);
	Ванесса.СообщитьПользователю(СтрокаСообщения);
	Ванесса.ЗаписатьЛогВЖРИнформация("ПоискШаговПоСтрокеMCP", СтрокаСообщения);
	
	ОткрытиеФичаФайлаВРедакторе(ПутьКФайлу, Запрос, "СинтаксКонтроль");	
	
КонецФункции

// Открывает фича файл
&НаКлиенте
Функция ОткрытьФичаФайл(Запрос, ДополнительныеПараметры) Экспорт
	
	ПутьКФайлу = Строка(ЗначениеПараметраИзЗапроса(Запрос, "filePath"));
	ОткрытиеФичаФайлаВРедакторе(ПутьКФайлу, Запрос, "ОткрытьФичаФайл");
	
КонецФункции

// Загружает или перезагружает .feature файлы
&НаКлиенте
Функция ОткрытьЗагрузитьИлиПерезагрузитьФичаФайл(Запрос, ДополнительныеПараметры) Экспорт
	
	ПутьКФайлу = Строка(ЗначениеПараметраИзЗапроса(Запрос, "path"));
	
	ДопПараметрыВызоваMCP = Новый Структура;
	ДопПараметрыВызоваMCP.Вставить("ИсходныйПутьКФайлу", ПутьКФайлу);
	
	Если НЕ ЗначениеЗаполнено(ПутьКФайлу) Тогда
		ТекстОшибки = Неопределено;
		ПутьКФайлу = ПутьКФайлуИзТекущегоФайлаВРедакторе(ТекстОшибки);
		Если ЗначениеЗаполнено(ТекстОшибки) Тогда
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;	
	КонецЕсли;	
	
	ОткрытиеФичаФайлаВРедакторе(ПутьКФайлу, Запрос, "ОткрытьЗагрузитьИлиПерезагрузитьФичаФайл", ДопПараметрыВызоваMCP);
	
КонецФункции

// Получает состояние редактора
&НаКлиенте
Функция ПолучитьСостояниеРедактора(Запрос, ДополнительныеПараметры) Экспорт
	
	ВключитьСодержимое = Истина;
	ЗначениеИзЗапроса = ЗначениеПараметраИзЗапроса(Запрос, "includeContent");
	Если ЗначениеИзЗапроса <> Неопределено Тогда
		ВключитьСодержимое = ЗначениеИзЗапроса = Истина;
	КонецЕсли;
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Состояние редактора"));
	Строки.Добавить("");
	
	// Вкладки
	Строки.Добавить(Ванесса.Локализовать("## Открытые вкладки"));
	Если Ванесса.VanessaTabs <> Неопределено Тогда
		Попытка
			КоличествоВкладок = Ванесса.VanessaTabs.count();
			Строки.Добавить(Ванесса.Локализовать("Количество:") + " " + Формат(КоличествоВкладок, "ЧГ=0; ЧН=0"));
			Строки.Добавить("");
			
			Для Инд = 0 По КоличествоВкладок - 1 Цикл
				Вкладка = Ванесса.VanessaTabs.tabStack[Инд];
				ИмяФайла = Вкладка.filename;
				Изменена = Вкладка.modified;
				Текущая = (Ванесса.VanessaTabs.current <> Неопределено И НРег(Ванесса.VanessaTabs.current.filename) = НРег(ИмяФайла));
				
				Префикс = "";
				Если Текущая Тогда
					Префикс = "**→** ";
				КонецЕсли;
				
				Суффикс = "";
				Если Изменена = Истина Тогда
					Суффикс = " " + Ванесса.Локализовать("*(изменена)*");
				КонецЕсли;
				
				Строки.Добавить("- " + Префикс + ИмяФайла + Суффикс);
			КонецЦикла;
		Исключение
			Строки.Добавить(Ванесса.Локализовать("ошибка чтения вкладок:") + " " + ОписаниеОшибки());
		КонецПопытки;
	Иначе
		Строки.Добавить(Ванесса.Локализовать("Редактор не инициализирован. Загрузите фичи: `load_features`."));
	КонецЕсли;
	Строки.Добавить("");
	
	// Текущий файл и позиция
	Строки.Добавить(Ванесса.Локализовать("## Активный документ"));
	Если Не Ванесса.ОтсутствуетVanessaEditor() Тогда
		Попытка
			Редактор = Ванесса.ПолучитьVanessaEditor();
			
			// Позиция курсора
			Попытка
				Позиция = Редактор.getPosition();
				Если Позиция <> Неопределено Тогда
					Строки.Добавить(Ванесса.Локализовать("- Строка:") + " " + Формат(Позиция.lineNumber, "ЧГ=0; ЧН=0"));
				КонецЕсли;
			Исключение
			КонецПопытки;
			
			// Выбранный сценарий
			СтрокаДерева = Ванесса.СтрокаДереваПоНомеруСтрокиФичи(Позиция.lineNumber);
			ЕстьВыбранныйСценарий = Ложь;
			Если СтрокаДерева <> Неопределено Тогда
				ЭтоПример = Ложь;
				ЭтоШагКонтекста = Ложь;
				СтрокаСценария = Ванесса.НайтиСтрокуСценарияЧерезРодителя(СтрокаДерева, ЭтоПример, ЭтоШагКонтекста);
				Если СтрокаСценария <> Неопределено Тогда
					ЕстьВыбранныйСценарий = Истина;
					Строки.Добавить("");
					Строки.Добавить(Ванесса.Локализовать("### Выбранный сценарий:") + " " + СтрокаСценария.Имя);
				КонецЕсли;	
			КонецЕсли;	
			
			Если НЕ ЕстьВыбранныйСценарий Тогда
				Строки.Добавить("");
				Строки.Добавить(Ванесса.Локализовать("### Выбранный сценарий:") + " " + Ванесса.Локализовать("Нет выбранного сценария. Можно запустить все сценарии на выполнение с помощью"));
			КонецЕсли;
			
			// Все сценарии текущей вкладки
			ВсеСценарии = Ванесса.ПолучитьИменаСценариевПоТекущейФиче();
			Строки.Добавить("");
			Строки.Добавить(Ванесса.Локализовать("### Всего сценариев в файле:") + " " + ВсеСценарии.Количество());
			Строки.Добавить(Ванесса.Локализовать("### Все сценарии из файла:"));
			Для Каждого ТекСценарий Из ВсеСценарии Цикл
				Строки.Добавить("- " + ТекСценарий.Имя);
			КонецЦикла;	
			
			// Содержимое
			Если ВключитьСодержимое Тогда
				Строки.Добавить("");
				Строки.Добавить(Ванесса.Локализовать("### Содержимое"));
				Строки.Добавить("```gherkin");
				Попытка
					Содержимое = Редактор.getContent();
					Если ЗначениеЗаполнено(Содержимое) Тогда
						Строки.Добавить(Содержимое);
					Иначе
						Строки.Добавить(Ванесса.Локализовать("(пустой документ)"));
					КонецЕсли;
				Исключение
					Строки.Добавить(Ванесса.Локализовать("(ошибка чтения:") + " " + ОписаниеОшибки() + ")");
				КонецПопытки;
				Строки.Добавить("```");
			КонецЕсли;
		Исключение
			Строки.Добавить(Ванесса.Локализовать("ошибка доступа к редактору:") + " " + ОписаниеОшибки());
		КонецПопытки;
	Иначе
		Строки.Добавить(Ванесса.Локализовать("Редактор не открыт"));
	КонецЕсли;
	Строки.Добавить("");
	
	// Ошибки
	Строки.Добавить(Ванесса.Локализовать("## Ошибки в редакторе"));
	Попытка
		Если Ванесса.ДанныеОшибокVanessaEditor <> Неопределено И ТипЗнч(Ванесса.ДанныеОшибокVanessaEditor) = Тип("Соответствие") Тогда
			КоличествоОшибок = 0;
			Для Каждого КлючЗначение Из Ванесса.ДанныеОшибокVanessaEditor Цикл
				КоличествоОшибок = КоличествоОшибок + 1;
			КонецЦикла;
			Строки.Добавить(Ванесса.Локализовать("Количество:") + " " + Формат(КоличествоОшибок, "ЧГ=0; ЧН=0"));
			Если КоличествоОшибок > 0 Тогда
				Для Каждого КлючЗначение Из Ванесса.ДанныеОшибокVanessaEditor Цикл
					Строки.Добавить(Ванесса.Локализовать("- Строка") + " " + Формат(КлючЗначение.Значение.НомерСтрокиВФиче, "ЧГ=0; ЧН=0") + ". Текст ошибки: " + КлючЗначение.Значение.КраткйТекстОшибки);
					Строки.Добавить("  " + Ванесса.Локализовать("Имя feature файла где возникла эта ошибка:") + " " + КлючЗначение.Значение.ИмяФичи);
				КонецЦикла;
			КонецЕсли;
		Иначе
			Строки.Добавить(Ванесса.Локализовать("нет ошибок"));
		КонецЕсли;
	Исключение
		Строки.Добавить(Ванесса.Локализовать("ошибка чтения:") + " " + ОписаниеОшибки());
	КонецПопытки;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Выбирает и делает сценарий текущим. После этого можно вызывать выполнение текущего сценария
&НаКлиенте
Функция ВыбратьСценарий(Запрос, ДополнительныеПараметры) Экспорт
	
	ИмяСценария = ЗначениеПараметраИзЗапроса(Запрос, "name");
	Если НЕ ЗначениеЗаполнено(ИмяСценария) Тогда
		ТекстСообщения = Ванесса.Локализовать("Не указан обязательный параметр 'name'");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;	
	
	СценарииФичи = Ванесса.ПолучитьИменаСценариевПоТекущейФиче();
	
	Для Каждого ТекСценарий Из СценарииФичи Цикл
		Если НРег(ТекСценарий.Имя) = НРег(ИмяСценария) Тогда
			
			Ванесса.СделатьСтрокуДереваТекущейПоНомеруСтрокиФичи(ТекСценарий.НомерСтрокиВФиче);
			СтрокаДерева = Ванесса.СтрокаДереваПоНомеруСтрокиФичи(ТекСценарий.НомерСтрокиВФиче);
			Ванесса.МодульРедакторТекста().АктивизироватьСтрокуРедактораПоИдСтрокиИНомеруСтрокиФичи(СтрокаДерева.ИдСтроки, ТекСценарий.НомерСтрокиВФиче);
			
			Идентификатор = Запрос.Идентификатор;
			Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
			ПодключитьОбработчикОжидания("ЗавершитьВыборСценария", 0.1, Истина);
			Возврат Неопределено;
		КонецЕсли;	
	КонецЦикла;
	
	ТекстСообщения = Ванесса.Локализовать("Не найден сценарий:") + " " + ИмяСценария;
	Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	
КонецФункции

// Выбирает и делает шаг текущим. После этого можно вызывать выполнение с текущего шага
&НаКлиенте
Функция ВыбратьШаг(Запрос, ДополнительныеПараметры) Экспорт
	
	НомерСтроки = ЗначениеПараметраИзЗапроса(Запрос, "lineNumber");
	Если НЕ ЗначениеЗаполнено(НомерСтроки) Тогда
		ТекстСообщения = Ванесса.Локализовать("Не указан обязательный параметр 'lineNumber'");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;	
	
	ТекстОшибки = Неопределено;
	СделатьШагФичиТекущим(НомерСтроки, Запрос.Идентификатор, ТекстОшибки);
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;	
	ПодключитьОбработчикОжидания("ЗавершитьВыборШага", 0.1, Истина);
	
КонецФункции

// Вызывается при успешном завершении загрузки фичи/фич
&НаКлиенте
Процедура ОповеститьMCP_ОЗавершенииЗагрузкиФичи(ПутьКФайлу, Тип, ДопПараметрыВызоваMCP) Экспорт
	
	Если Тип = "СинтаксКонтроль" Тогда
		ЗавершитьПроверкуСинтаксиса(ПутьКФайлу);
	ИначеЕсли Тип = "ОткрытьФичаФайл" Тогда
		ЗавершитьОткрытиеФичаФайла(ПутьКФайлу);
	ИначеЕсли Тип = "ОткрытьЗагрузитьИлиПерезагрузитьФичаФайл" Тогда
		ЗавершитьОткрытьЗагрузитьИлиПерезагрузитьФичаФайл(ПутьКФайлу, ДопПараметрыВызоваMCP);
	ИначеЕсли Тип = "ВыполнитьОдинШагВФичаФайле" Тогда
		ПродолжитьВыполнениеШагаВРедакторе(ПутьКФайлу, ДопПараметрыВызоваMCP);
	ИначеЕсли Тип = "ВыполнитьСценарий" Тогда
		ПродолжитьВыполнитьСценарий1(ПутьКФайлу, ДопПараметрыВызоваMCP);
	ИначеЕсли Тип = "ЗагрузитьФичуДляВыполненияСценария" Тогда
		// Проверяем, есть ли режим после загрузки (для reloadAndRunFromLine)
		Если ДопПараметрыВызоваMCP <> Неопределено И ДопПараметрыВызоваMCP.Свойство("РежимПослеЗагрузки") Тогда
			Если ДопПараметрыВызоваMCP.РежимПослеЗагрузки = "reloadAndRunFromLine" Тогда
				// Устанавливаем текущий шаг по номеру строки
				НомерСтроки = ДопПараметрыВызоваMCP.НомерСтроки;
				
				Текст = Новый ЧтениеТекста;
				Текст.Открыть(ДопПараметрыВызоваMCP.ИсходныйПутьКФайлу, "UTF-8");
				ТекстФайла = Текст.Прочитать();
				Текст.Закрыть();
				
				МассивСтрокФайла = Ванесса._СтрРазделить(ТекстФайла, Символы.ПС);
				
				НашлиШаг = Ложь;
				Пока Истина Цикл
					СтрокаДерева = Ванесса.СтрокаДереваПоНомеруСтрокиФичи(НомерСтроки);
					Если СтрокаДерева <> Неопределено И СтрокаДерева.Тип = "Шаг" Тогда
						ЭтоПример = Ложь;
						ЭтоШагКонтекста = Ложь;
						СтрокаСценария  = Ванесса.НайтиСтрокуСценарияЧерезРодителя(СтрокаДерева, ЭтоПример, ЭтоШагКонтекста);
						
						Если ЭтоШагКонтекста Тогда
							ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В файле <%1> первым шагом, который можно выполнить, начиная со строки <%2> и ниже, оказался шаг контекста в строке <%3>. Но нельзя запускаться выполнение сценария с определенного шага и при этом передавать номер строки, где располагается секция Контекст, т.к. секция Контекст не является самостоятельным сценарием."), ДопПараметрыВызоваMCP.ИсходныйПутьКФайлу, ДопПараметрыВызоваMCP.НомерСтроки, НомерСтроки);
							МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстОшибки);
							Возврат;
						КонецЕсли;	
						
						НашлиШаг = Истина;
						Прервать;
					КонецЕсли;
					
					НомерСтроки = НомерСтроки + 1;
					Если НомерСтроки > МассивСтрокФайла.Количество() Тогда
						Прервать;
					КонецЕсли;
					
				КонецЦикла;
				
				Если НЕ НашлиШаг Тогда
					ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В файле <%1> нет шагов для выполнения, начиная со строки <%2> и ниже."), ДопПараметрыВызоваMCP.ИсходныйПутьКФайлу, ДопПараметрыВызоваMCP.НомерСтроки);
					МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстОшибки);
					Возврат;
				КонецЕсли;	
				
				ТекстОшибки = Неопределено;
				СделатьШагФичиТекущим(НомерСтроки, Ванесса.ИдентификаторЗадачиMCP(), ТекстОшибки);
				Если ЗначениеЗаполнено(ТекстОшибки) Тогда
					МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстОшибки);
					Возврат;
				КонецЕсли;	
				ПодключитьОбработчикОжидания("ЗавершитьВыборШагаДляВыполненияСценарияСТекущегоШага", 0.1, Истина);
				Возврат;
			КонецЕсли;
		КонецЕсли;
		ПродолжитьВыполнитьСценарий2(ПутьКФайлу, ДопПараметрыВызоваMCP);
	ИначеЕсли Тип = "ЗагрузитьФичаФайлИВыполнитьСценарии" Тогда
		Проблемы = Ванесса.ПолучитьПроблемыПоТекущейФиче();
		Если Проблемы.Количество() > 0 Тогда
			МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ОшибкаПроверкиСинтаксиса(Проблемы));
			Возврат;
		КонецЕсли;
		Ванесса.КомандаВыполнитьСценарии();
	Иначе
		ТекстИсключения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не известный тип оповещения MCP после загрузки фичи <%1>"), Тип);
		ВызватьИсключение ТекстИсключения;
	КонецЕсли;	
	
КонецПроцедуры

// Вызывается при успешном завершении выполнения одной строки фичи
&НаКлиенте
Функция ОповеститьMCP_ОУспешномЗавершенииВыполненияОднойСтрокиФичи(Тип, ДопПараметрыВызоваMCP) Экспорт
	
	Если Тип = "ВыполнитьОдинШагВФичаФайле" Тогда
		ЗавершитьУспешноеВыполнениеШагаВРедакторе(ДопПараметрыВызоваMCP);
	Иначе
		ТекстИсключения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не известный тип оповещения MCP после выполнения одной строки фичи <%1>"), Тип);
		ВызватьИсключение ТекстИсключения;
	КонецЕсли;	
	
КонецФункции

// Вызывается при окончании выполнения сценариев
&НаКлиенте
Функция ОповеститьMCP_ОЗавершенииВыполненияСценариев(Тип, ДопПараметрыВызоваMCP) Экспорт
	
	Если Тип = "selected" Тогда
		ЗавершитьВыполненияСценария(Тип, ДопПараметрыВызоваMCP);
	ИначеЕсли Тип = "all" Тогда
		ЗавершитьВыполненияСценария(Тип, ДопПараметрыВызоваMCP);
	ИначеЕсли Тип = "reloadAndRun" Тогда
		ЗавершитьВыполненияСценария(Тип, ДопПараметрыВызоваMCP);
	ИначеЕсли Тип = "reloadAndRunFromLine" Тогда
		ЗавершитьВыполненияСценария(Тип, ДопПараметрыВызоваMCP);
	ИначеЕсли Тип = "fromCurrentStep" Тогда
		ЗавершитьВыполненияСценария(Тип, ДопПараметрыВызоваMCP);
	Иначе
		ТекстИсключения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не известный тип оповещения MCP при завершении выполнения сценариев <%1>"), Тип);
		ВызватьИсключение ТекстИсключения;
	КонецЕсли;	
	
КонецФункции

// Вызывается при ошибке при выполнении одной строки фичи
&НаКлиенте
Функция ОповеститьMCP_ОбОшибкеПриВыполненияОднойСтрокиФичи(Тип, ДопПараметрыВызоваMCP, РезультатПрохожденияШага) Экспорт
	
	Если Тип = "ВыполнитьОдинШагВФичаФайле" Тогда
		ЗавершитьВыполнениеШагаВРедактореСОшибкой(ДопПараметрыВызоваMCP, РезультатПрохожденияШага);
	Иначе
		ТекстИсключения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не известный тип оповещения MCP <%1>"), Тип);
		ВызватьИсключение ТекстИсключения;
	КонецЕсли;	
	
КонецФункции

// Вызывается при ошибке выполнения выделенного сценария
&НаКлиенте
Функция ОповеститьMCP_ОбОшибкеВыполненияВыделенногоСценария(ДопПараметрыВызоваMCP, ТекстОшибки) Экспорт
	
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), ТекстОшибки);
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
	
КонецФункции

// Вызывается при завершении выполнения шага строкой в MCPИнструментВыполнитьОдинШагТекстом
&НаКлиенте
Процедура ЗавершениеВыполнениеШагаИзСтроки() Экспорт
	МодульМсп_Сервер = МодульМсп_Сервер();
	ТекстСообщения = Ванесса.Локализовать("Шаг выполнен успешно");
	МодульМсп_Сервер.ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), ТекстСообщения);
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
КонецПроцедуры

// Возврашает общий модуль из расширения MCP
// Если установлен внешний фасад (альтернативный MCP транспорт, плагин MCPRagSearch) - возвращает его.
&НаКлиенте
Функция МодульМсп_Сервер() Экспорт
	Если ВнешнийФасадМсп <> Неопределено Тогда
		Возврат ВнешнийФасадМсп;
	КонецЕсли;
	Возврат Вычислить("Мсп_Сервер");
КонецФункции

// Устанавливает внешний фасад Мсп_Сервер (Неопределено - вернуться к штатному модулю расширения).
&НаКлиенте
Процедура УстановитьВнешнийФасадМсп(Фасад) Экспорт
	ВнешнийФасадМсп = Фасад;
КонецПроцедуры

// Сохраняет табличный документ в файл
&НаКлиенте
Функция СохранитьТабличныйДокументВФайл(Запрос, ДополнительныеПараметры) Экспорт
	
	// Получаем параметр file_name (обязательный)
	ИмяФайла = Строка(ЗначениеПараметраИзЗапроса(Запрос, "file_name"));
	Если НЕ ЗначениеЗаполнено(ИмяФайла) Тогда
		ТекстСообщения = Ванесса.Локализовать("Необходимо указать параметр file_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр form_element_name (обязательный)
	ИмяЭлементаФормы = Строка(ЗначениеПараметраИзЗапроса(Запрос, "form_element_name"));
	Если НЕ ЗначениеЗаполнено(ИмяЭлементаФормы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Необходимо указать параметр form_element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр format (по умолчанию 'mxl')
	Формат = Строка(ЗначениеПараметраИзЗапроса(Запрос, "format"));
	Если НЕ ЗначениеЗаполнено(Формат) Тогда
		Формат = "mxl";
	Иначе
		Формат = НРег(СокрЛП(Формат));
	КонецЕсли;
	
	// Проверка допустимого значения формата
	Если Формат <> "mxl" И Формат <> "xlsx" И Формат <> "pdf" Тогда
		ТекстСообщения = Ванесса.Локализовать("Недопустимое значение format. Допустимые значения: 'mxl', 'xlsx', 'pdf'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	// Получаем табличный документ из активной формы
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	Попытка
		ТабличныйДокумент = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку(ИмяЭлементаФормы, Истина,,,ДопПараметры);
	Исключение
		ТекстОшибки = ОписаниеОшибки();
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	// Сохраняем табличный документ в файл
	Попытка
		// ИндексФильтра начинается с нуля
		ДопПараметры = Новый Структура;
		ДопПараметры.Вставить("РасширениеФайла", Формат);
		ДопПараметры.Вставить("ИмяФайлаДляСохранения", ИмяФайла);
		Если Формат = "pdf" Тогда
			ДопПараметры.Вставить("ИндексФильтра", 2);
		ИначеЕсли Формат = "xlsx" Тогда
			ДопПараметры.Вставить("ИндексФильтра", 7);
		КонецЕсли;	
		ТабДок = Ванесса.ПолучитьТабличныйДокументTestClient(ТабличныйДокумент, ДопПараметры);
		
		ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
			"# Табличный документ %1 сохранен
			|
			|- Файл: %2
			|- Формат: %3"),ИмяЭлементаФормы, ИмяФайла,Формат);
		Возврат ТекстОтвета;
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка сохранения табличного документа:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Возвращает данные указанного элемента формы
&НаКлиенте
Функция ПолучитьДанныеЭлементаФормы(Запрос, ДополнительныеПараметры) Экспорт
	
	// Получаем параметр element_name (обязательный)
	ИмяЭлемента = Строка(ЗначениеПараметраИзЗапроса(Запрос, "element_name"));
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	// Получаем элемент формы
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	Попытка
		ЭлементФормы = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку(ИмяЭлемента, Истина,,,ДопПараметры);
	Исключение
		ТекстОшибки = ОписаниеОшибки();
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
	Если ЭлементФормы = Неопределено Тогда
		ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Элемент формы '%1' не найден."), ИмяЭлемента);
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;
	
	// Получаем данные элемента
	Попытка
		ДанныеЭлемента = Новый Структура;
		ДанныеЭлемента.Вставить("Имя", ИмяЭлемента);
		
		// Получаем заголовок элемента
		Заголовок = Неопределено;
		Попытка
			Заголовок = ЭлементФормы.ТекстЗаголовка;
		Исключение
		КонецПопытки;
		ДанныеЭлемента.Вставить("Заголовок", Заголовок);
		
		// Получаем тип элемента
		ТипЭлемента = Неопределено;
		Попытка
			ТипЭлемента = Строка(ТипЗнч(ЭлементФормы));
		Исключение
		КонецПопытки;
		ДанныеЭлемента.Вставить("Тип", ТипЭлемента);
		
		// Получаем вид элемента
		ВидЭлемента = Неопределено;
		Попытка
			ВидЭлемента = Строка(ЭлементФормы.Вид);
		Исключение
		КонецПопытки;
		ДанныеЭлемента.Вставить("Вид", ВидЭлемента);
		
		// Получаем значение элемента
		ЗначениеЭлемента = Неопределено;
		ТекстРедактирования = Неопределено;
		Попытка
			ДопПараметры = Новый Структура;
			ДопПараметры.Вставить("УдалятьПустыеСтрокиИзМакета", Ложь);
			СостояниеФормыTestClient = Ванесса.ПолучитьСостояниеЭлементаФормыTestClient(ЭлементФормы, ДопПараметры);
			Если ТипЗнч(ЭлементФормы) = Тип("ТестируемаяТаблицаФормы") Тогда
				ЗначениеЭлемента = Ванесса.ПолучитьСтрокуСТЧФормаTestClient(СостояниеФормыTestClient.ЗначениеТЧ, Неопределено);
			ИначеЕсли СостояниеФормыTestClient.Вид = ВидПоляФормы.ПолеТабличногоДокумента Тогда
				ЗначениеЭлемента = Ванесса.ПолучитьСтрокуТабличныйДокументФормаTestClient(СостояниеФормыTestClient.ТабДок);
			Иначе	
				ЗначениеЭлемента = СостояниеФормыTestClient.ПредставлениеДанных;
				Если СостояниеФормыTestClient.Свойство("ТекстРедактирования") Тогда
					ТекстРедактирования = СостояниеФормыTestClient.ТекстРедактирования;
				КонецЕсли;	
			КонецЕсли;	
		Исключение
		КонецПопытки;
		ДанныеЭлемента.Вставить("ТекстРедактирования", ТекстРедактирования);
		ДанныеЭлемента.Вставить("Значение", ЗначениеЭлемента);
		
		Если ТипЗнч(ЭлементФормы) = Тип("ТестируемаяКнопкаФормы") Тогда
			// Получаем значение пометки
			Пометка = Неопределено;
			Попытка
				Пометка = ЭлементФормы.ТекущееПометка();
			Исключение
			КонецПопытки;
			ДанныеЭлемента.Вставить("Пометка", Пометка);
		КонецЕсли;	
		
		// Получаем представление значения
		ПредставлениеЗначения = Неопределено;
		Попытка
			ПредставлениеЗначения = Ванесса.ПолучитьПредставлениеДанныхЭлементаФормы(ЭлементФормы);
		Исключение
		КонецПопытки;
		ДанныеЭлемента.Вставить("Представление", ПредставлениеЗначения);
		
		// Получаем доступность
		ДоступностьЭлемента = Неопределено;
		Попытка
			ДоступностьЭлемента = ЭлементФормы.ТекущаяДоступность();
		Исключение
		КонецПопытки;
		ДанныеЭлемента.Вставить("Доступность", ДоступностьЭлемента);
		
		// Получаем видимость
		ВидимостьЭлемента = Неопределено;
		Попытка
			ВидимостьЭлемента = ЭлементФормы.ТекущаяВидимость();
		Исключение
		КонецПопытки;
		ДанныеЭлемента.Вставить("Видимость", ВидимостьЭлемента);
		
		// Получаем только просмотр
		ТолькоПросмотрЭлемента = Неопределено;
		Попытка
			ТолькоПросмотрЭлемента = ЭлементФормы.ТекущееТолькоПросмотр();
		Исключение
		КонецПопытки;
		ДанныеЭлемента.Вставить("ТолькоПросмотр", ТолькоПросмотрЭлемента);
		
		// Формируем результат в формате Markdown
		Строки = Новый Массив;
		Строки.Добавить(Ванесса.Локализовать("# Данные элемента формы"));
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("## Основная информация"));
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("- Имя:") + " " + ДанныеЭлемента.Имя);
		Если ДанныеЭлемента.Заголовок <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- Заголовок:") + " " + ДанныеЭлемента.Заголовок);
		КонецЕсли;
		Если ДанныеЭлемента.Вид <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- Вид:") + " " + ДанныеЭлемента.Вид);
		КонецЕсли;
		Если ДанныеЭлемента.Тип <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- Тип:") + " " + ДанныеЭлемента.Тип);
		КонецЕсли;
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("## Значение элемента формы"));
		Строки.Добавить("");
		Если ДанныеЭлемента.Значение <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- Значение:") + " " + MCPПредставлениеЗначенияДляMarkdown(ДанныеЭлемента.Значение));
		КонецЕсли;
		Если ДанныеЭлемента.Представление <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- Представление:") + " " + ДанныеЭлемента.Представление);
		КонецЕсли;
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("## Состояние"));
		Строки.Добавить("");
		Если ДанныеЭлемента.Доступность <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- Доступность:") + " " + MCPПредставлениеЗначенияДляMarkdown(ДанныеЭлемента.Доступность));
		КонецЕсли;
		Если ДанныеЭлемента.Видимость <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- Видимость:") + " " + MCPПредставлениеЗначенияДляMarkdown(ДанныеЭлемента.Видимость));
		КонецЕсли;
		Если ДанныеЭлемента.ТолькоПросмотр <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- ТолькоПросмотр:") + " " + MCPПредставлениеЗначенияДляMarkdown(ДанныеЭлемента.ТолькоПросмотр));
		КонецЕсли;
		Если ДанныеЭлемента.Свойство("Пометка") И ДанныеЭлемента.Пометка <> Неопределено Тогда
			Строки.Добавить(Ванесса.Локализовать("- Пометка:") + " " + MCPПредставлениеЗначенияДляMarkdown(ДанныеЭлемента.Пометка));
		КонецЕсли;
		
		Если ТипЗнч(ЭлементФормы) = Тип("ТестируемаяТаблицаФормы") Тогда
			// Получаем значение пометки
			ТекущийРежимРедактирование = Неопределено;
			Попытка
				ТекущийРежимРедактирование = ЭлементФормы.ТекущийРежимРедактирование();
			Исключение
			КонецПопытки;
			Если ТекущийРежимРедактирование <> Неопределено Тогда
				Строки.Добавить("");
				Строки.Добавить(Ванесса.Локализовать("- Таблица в режиме редактирования:") + " " + MCPПредставлениеЗначенияДляMarkdown(ТекущийРежимРедактирование));
			КонецЕсли;	
		КонецЕсли;	
		
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка получения данных элемента формы:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции	

// Универсальный инструмент для управления элементами формы
&НаКлиенте
Функция УправлениеЭлементамиФормы(Запрос, ДополнительныеПараметры) Экспорт
	
	// Получаем параметр action (обязательный)
	Действие = Строка(ЗначениеПараметраИзЗапроса(Запрос, "action"));
	Если НЕ ЗначениеЗаполнено(Действие) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр action обязателен. Допустимые значения: 'click_button', 'input_text', 'open_dropdown', 'get_dropdown_state', 'select_from_dropdown', 'set_value', 'open_choice_form', 'set_radio_button', 'set_checkbox', 'unset_checkbox', 'toggle_checkbox', 'click_hyperlink', 'simulate_tab', 'activate_column', 'navigate_next_column', 'navigate_previous_column', 'start_edit_row', 'end_edit_row', 'navigate_to_row', 'navigate_next_row', 'navigate_previous_row', 'select_row', 'get_selected_rows', 'create_new', 'switch_tab', 'toggle_group', 'activate_header_element', 'clear_field'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр element_name (обязательный для большинства действий)
	ИмяЭлемента = Строка(ЗначениеПараметраИзЗапроса(Запрос, "element_name"));
	
	// Получаем параметр table_name (необязательный)
	ИмяТаблицы = Строка(ЗначениеПараметраИзЗапроса(Запрос, "table_name"));
	
	// Получаем параметр value (необязательный)
	Значение = Строка(ЗначениеПараметраИзЗапроса(Запрос, "value"));
	
	ЗначениеПереходаКСтроке = Строка(ЗначениеПараметраИзЗапроса(Запрос, "row_criteria"));
	
	// Получаем параметр hyperlink_title (необязательный)
	ЗаголовокГиперссылки = Строка(ЗначениеПараметраИзЗапроса(Запрос, "hyperlink_title"));
	Если НЕ ЗначениеЗаполнено(ЗаголовокГиперссылки) Тогда
		ЗаголовокГиперссылки = Неопределено;
	КонецЕсли;	
	
	// Получаем параметр hyperlink_index (необязательный)
	Попытка
		ИндексГиперссылки = Число(ЗначениеПараметраИзЗапроса(Запрос, "hyperlink_index"));
	Исключение
		ИндексГиперссылки = Неопределено;
	КонецПопытки;
	
	// Получаем параметр tab_direction (необязательный)
	НаправлениеПерехода = Строка(ЗначениеПараметраИзЗапроса(Запрос, "tab_direction"));
	Если НЕ ЗначениеЗаполнено(НаправлениеПерехода) Тогда
		НаправлениеПерехода = "next";
	КонецЕсли;
	
	// Получаем параметр exact_match (необязательный)
	ТочноеСовпадение = ЗначениеПараметраИзЗапроса(Запрос, "exact_match");
	Если ТочноеСовпадение = Неопределено Тогда
		ТочноеСовпадение = Ложь;
	Иначе
		Попытка
			ТочноеСовпадение = Булево(ТочноеСовпадение);
		Исключение
			ТочноеСовпадение = Ложь;
		КонецПопытки;
	КонецЕсли;
	
	// Получаем параметр group_name (необязательный, для очистки полей в группах)
	ИмяГруппы = Строка(ЗначениеПараметраИзЗапроса(Запрос, "group_name"));
	Если НЕ ЗначениеЗаполнено(ИмяГруппы) Тогда
		ИмяГруппы = Неопределено;
	КонецЕсли;
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	Идентификатор = Запрос.Идентификатор;
	Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
	
	// Выполняем действие в зависимости от параметра action
	Если Действие = "click_button" Тогда
		Результат = НажатьКнопкуФормы(ИмяЭлемента, ИмяТаблицы);
	ИначеЕсли Действие = "input_text" Тогда
		Результат = ВвестиТекстВПоле(ИмяЭлемента, Значение, ИмяТаблицы);
	ИначеЕсли Действие = "open_dropdown" Тогда
		Результат = ОткрытьВыпадающийСписок(ИмяЭлемента, ИмяТаблицы, Истина);
	ИначеЕсли Действие = "get_dropdown_state" Тогда
		Результат = ОткрытьВыпадающийСписок(ИмяЭлемента, ИмяТаблицы, Ложь);
	ИначеЕсли Действие = "select_from_dropdown" Тогда
		Результат = ВыбратьИзВыпадающегоСписка(ИмяЭлемента, Значение, ИмяТаблицы);
	ИначеЕсли Действие = "set_value" Тогда
		Результат = УстановитьЗначениеВПоле(ИмяЭлемента, Значение, ИмяТаблицы, ТочноеСовпадение);
	ИначеЕсли Действие = "open_choice_form" Тогда
		Результат = ОткрытьФормуВыбора(ИмяЭлемента, ИмяТаблицы);
	ИначеЕсли Действие = "set_radio_button" Тогда
		Результат = УстановитьПереключатель(ИмяЭлемента, Значение);
	ИначеЕсли Действие = "set_checkbox" Тогда
		Результат = УстановитьФлаг(ИмяЭлемента, ИмяТаблицы);
	ИначеЕсли Действие = "unset_checkbox" Тогда
		Результат = СнятьФлаг(ИмяЭлемента, ИмяТаблицы);
	ИначеЕсли Действие = "toggle_checkbox" Тогда
		Результат = ПереключитьФлаг(ИмяЭлемента, ИмяТаблицы);
	ИначеЕсли Действие = "click_hyperlink" Тогда
		Результат = НажатьГиперссылку(ИмяЭлемента, ЗаголовокГиперссылки, ИндексГиперссылки);
	ИначеЕсли Действие = "simulate_tab" Тогда
		Результат = ЭмуляцияПереходаTAB(НаправлениеПерехода);
	ИначеЕсли Действие = "activate_column" Тогда
		Результат = АктивизироватьКолонкуТаблицы(ИмяТаблицы, Значение);
	ИначеЕсли Действие = "navigate_next_column" Тогда
		Результат = ПереходКСледующейКолонке(ИмяТаблицы);
	ИначеЕсли Действие = "navigate_previous_column" Тогда
		Результат = ПереходКПредыдущейКолонке(ИмяТаблицы);
	ИначеЕсли Действие = "start_edit_row" Тогда
		Результат = НачатьРедактированиеСтроки(ИмяТаблицы);
	ИначеЕсли Действие = "end_edit_row" Тогда
		Результат = ЗакончитьРедактированиеСтроки(ИмяТаблицы);
	ИначеЕсли Действие = "navigate_to_row" Тогда
		Результат = ПереходКСтрокеТаблицы(ИмяТаблицы, ЗначениеПереходаКСтроке);
	ИначеЕсли Действие = "navigate_next_row" Тогда
		Результат = ПереходКСледующейСтрокеТаблицы(ИмяТаблицы);
	ИначеЕсли Действие = "navigate_previous_row" Тогда
		Результат = ПереходКПредыдущейСтрокеТаблицы(ИмяТаблицы);
	ИначеЕсли Действие = "select_row" Тогда
		Результат = ВыбратьСтрокуТаблицы(ИмяТаблицы);
	ИначеЕсли Действие = "get_selected_rows" Тогда
		Результат = ПолучитьВыделенныеСтрокиТаблицы(ИмяТаблицы);
	ИначеЕсли Действие = "create_new" Тогда
		Результат = СоздатьНовый(ИмяЭлемента, ИмяТаблицы);
	ИначеЕсли Действие = "switch_tab" Тогда
		Результат = ПереключитьсяНаЗакладку(ИмяЭлемента);
	ИначеЕсли Действие = "toggle_group" Тогда
		Результат = РазвернутьСвернутьГруппу(ИмяЭлемента, Значение);
	ИначеЕсли Действие = "activate_header_element" Тогда
		Результат = АктивизироватьЭлементШапки(ИмяЭлемента);
	ИначеЕсли Действие = "clear_field" Тогда
		Результат = ОчиститьПоле(ИмяЭлемента, ИмяТаблицы);
	ИначеЕсли Действие = "clear_message_window" Тогда
		Результат = ОчиститьОкноСообщенийПользователю();
	ИначеЕсли Действие = "select_from_form_dropdown" Тогда
		Результат = ВыбратьИзСпискаВыбораФормы(Значение);
	Иначе
		ТекстСообщения = Ванесса.Локализовать("Неизвестное действие: ") + Действие + ". " + Ванесса.Локализовать("Допустимые: 'click_button', 'input_text', 'open_dropdown', 'get_dropdown_state', 'select_from_dropdown', 'set_value', 'open_choice_form', 'set_radio_button', 'set_checkbox', 'unset_checkbox', 'toggle_checkbox', 'click_hyperlink', 'simulate_tab', 'activate_column', 'navigate_next_column', 'navigate_previous_column', 'start_edit_row', 'end_edit_row', 'navigate_to_row', 'navigate_next_row', 'navigate_previous_row', 'select_row', 'get_selected_rows', 'create_new', 'switch_tab', 'toggle_group', 'activate_header_element', 'clear_field', 'clear_message_window'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если ТипЗнч(Результат) = Тип("Строка") Тогда
		Попытка
			МассивСтрок = Новый Массив;
			МассивСтрок.Добавить(Результат);
			ДобавитьВМассивСтрокДанныеОбАктивномОкне(МассивСтрок);
			Результат = Ванесса._СтрСоединить(МассивСтрок, Символы.ПС);
		Исключение
		КонецПопытки;
		
		Возврат Результат;
	Иначе	
		Возврат Результат;
	КонецЕсли;
	
КонецФункции

// Внутренняя функция для нажатия кнопки формы
&НаКлиенте
Функция НажатьКнопкуФормы(ИмяЭлемента, ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'click_button' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			// Поиск в таблице
			Ванесса.ВОткрытойФормеВТЧЯНажимаюНаКнопкуСЗаголовком(ИмяТаблицы, ИмяЭлемента, Истина, ДопПараметры);
		Иначе
			ДопПараметры.Вставить("ДополнитьОшибкуПоискаКнопки", Истина);
			Кнопка = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку(ИмяЭлемента, Истина,,,ДопПараметры);

			Если Кнопка = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Кнопка '%1' не найдена."), ИмяЭлемента);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
			Кнопка.Нажать();
		КонецЕсли;
		
		Если ПослеНажатияНаКнопкуНадоСделатьПаузу(ИмяЭлемента) Тогда
			Ванесса.sleep(1);
		КонецЕсли;
		
		Строки = Новый Массив;
		Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Кнопка '%1' была успешно нажата."), ИмяЭлемента));
		
		Попытка
			ОкноМодальногоДиалога = Ванесса.НайтиОкноМодальногоДиалога();
			Если ОкноМодальногоДиалога <> Неопределено Тогда
				Строки.Добавить("");
				Строки.Добавить(Ванесса.Локализовать("После нажатия кнопки появился модальный диалог."));
				
				ДобавитьВОтветИнформациюОМодальномДиалоге(Строки, ОкноМодальногоДиалога);
			КонецЕсли;	
		Исключение
		КонецПопытки;
		
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	Исключение
		
		ТекстОшибки = Ванесса.Локализовать("Ошибка нажатия кнопки:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для ввода текста в поле
&НаКлиенте
Функция ВвестиТекстВПоле(ИмяЭлемента, Значение, ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'input_text' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Значение) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'input_text' необходимо указать параметр value.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			// Поиск в таблице
			ВыделеноБольшеОднойСтроки = Ложь;
			Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
			Попытка
				ВыделенныеСтроки = Таблица.ПолучитьВыделенныеСтроки();
				Если ВыделенныеСтроки.Количество() > 1 Тогда
					ВыделеноБольшеОднойСтроки = Истина;
				КонецЕсли;
			Исключение
			КонецПопытки;
			
			Если ВыделеноБольшеОднойСтроки Тогда
				ТекстОшибки = Ванесса.Локализовать("В таблице выделно больше одной строки. Редактирование не возможно.");
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
			Ванесса.ВОткрытойФормеВТЧВПолеСЗаголовкомЯВвожуТекст(ИмяТаблицы, ИмяЭлемента, Значение, Истина, ДопПараметры);
		Иначе
			Поле = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку(ИмяЭлемента, Истина,,,ДопПараметры);
			Поле.ВвестиТекст(Значение);
		КонецЕсли;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Текст '%1' был успешно введен в поле '%2'."), Значение, ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка ввода текста:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для открытия выпадающего списка
&НаКлиенте
Функция ОткрытьВыпадающийСписок(ИмяЭлемента, ИмяТаблицы, ОткрыватьСписок)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'open_dropdown' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			// Поиск в таблице
			Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
			Если Таблица = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' не найдена."), ИмяТаблицы);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
			Поле = Таблица.НайтиОбъект(,,ИмяЭлемента);
			Если Поле = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Поле '%1' не найдено в таблице '%2'."), ИмяЭлемента, ИмяТаблицы);
				Ванесса.ДополнитьОшибку_НетПоляТаблицыMCP(Таблица, ТекстОшибки);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
			Если ОткрыватьСписок Тогда
				Поле.Активизировать();
				
				Если НЕ Таблица.ТекущийРежимРедактирование() Тогда
					Таблица.ИзменитьСтроку();
				КонецЕсли;
			КонецЕсли;
		Иначе
			Поле = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку(ИмяЭлемента, Истина,,,ДопПараметры);
		КонецЕсли;
		
		Если Поле = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Поле '%1' не найдено."), ИмяЭлемента);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		Если ОткрыватьСписок Тогда
			Если НЕ Поле.ВыпадающийСписокОткрыт() Тогда
				Поле.ОткрытьВыпадающийСписок();
			КонецЕсли;	
			Поле.ОжидатьФормированияВыпадающегоСписка();
		КонецЕсли;	
		
		Попытка
			ПредставлениеСпискаВыбора = Ванесса.ДополнитьПредставлениеСпискаВыбора(Поле.ПолучитьПредставлениеСпискаВыбора());
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Не вышло получить представление открытого списка выбора <%1>.");
			ТекстОшибки = СтрЗаменить(ТекстОшибки,"%1",ИмяЭлемента);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
		Строки = Новый Массив;
		Если ОткрыватьСписок Тогда
			Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Выпадающий список поля '%1' был успешно открыт."), ИмяЭлемента));
			Строки.Добавить(Ванесса.Локализовать("Содержимое списка:"));
		Иначе
			Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Содержимое списка '%1'."), ИмяЭлемента));
		КонецЕсли;	
		Для Каждого Элем Из ПредставлениеСпискаВыбора Цикл
			Строки.Добавить("  -" + Элем.ОтображаемыйТекст);
		КонецЦикла;	
		
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка открытия выпадающего списка:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для выбора из выпадающего списка
&НаКлиенте
Функция ВыбратьИзВыпадающегоСписка(ИмяЭлемента, Значение, ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'select_from_dropdown' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Значение) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'select_from_dropdown' необходимо указать параметр value.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Поле = Неопределено;
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			// Поиск в таблице
			Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
			Если Таблица = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' не найдена."), ИмяТаблицы);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
			Поле = Таблица.НайтиОбъект(,,ИмяЭлемента);
			Если Поле = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Поле '%1' не найдено в таблице '%2'."), ИмяЭлемента, ИмяТаблицы);
				Ванесса.ДополнитьОшибку_НетПоляТаблицыMCP(Таблица, ТекстОшибки);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
		Иначе
			Поле = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку(ИмяЭлемента, Истина,,,ДопПараметры);
		КонецЕсли;
		
		Если Поле = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Поле '%1' не найдено."), ИмяЭлемента);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		Если НЕ Поле.ВыпадающийСписокОткрыт() Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Нельзя сделать выбор из выпадающего списка '%1' т.к. он не открыт."), ИмяЭлемента);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		Поле.ВыполнитьВыборИзВыпадающегоСписка(Значение);
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Значение '%1' было успешно выбрано из выпадающего списка поля '%2'."), Значение, ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка выбора из списка:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Ванесса.ДополнитьОшибку_ЗначениеОткрытогоСписка(Поле, ТекстОшибки);
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для установки значения в поле
&НаКлиенте
Функция УстановитьЗначениеВПоле(ИмяЭлемента, Значение, ИмяТаблицы, ТочноеСовпадение)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'set_value' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Значение) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'set_value' необходимо указать параметр value.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			Если ТочноеСовпадение Тогда
				Ванесса.ВТаблицеИзВыпадающегоСпискаЯВыбираюТочноеЗначение(ИмяТаблицы, ИмяЭлемента, Значение, Истина);
			Иначе	
				Ванесса.ВОткрытойФормеВТЧИзВыпадающегоСпискаСЗаголовкомЯВыбираю(ИмяТаблицы, ИмяЭлемента, Значение, Истина);
			КонецЕсли;	
		Иначе
			Если ТочноеСовпадение Тогда
				Ванесса.ИзВыпадающегоСпискаЯВыбираюТочноеЗначение(ИмяЭлемента,Значение,Истина,ДопПараметры);
			Иначе
				// выбор по строке
				Ванесса.ВОткрытойФормеИзВыпадающегоСпискаСЗаголовкомЯВыбираю(ИмяЭлемента,Значение,Истина,ДопПараметры);
			КонецЕсли;	
		КонецЕсли;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Значение '%1' было успешно установлено в поле '%2'."), Значение, ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка установки значения:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для открытия формы выбора
&НаКлиенте
Функция ОткрытьФормуВыбора(ИмяЭлемента, ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'open_choice_form' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			Ванесса.ВОткрытойФормеВТЧЯНажимаюКнопкуВыбораУРеквизитаСЗаголовком(ИмяТаблицы, ИмяЭлемента, Истина, ДопПараметры);
		Иначе
			Ванесса.ВОткрытойФормеЯНажимаюКнопкуВыбораУПоляСЗаголовком(ИмяЭлемента, Истина, ДопПараметры);
		КонецЕсли;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Форма выбора для поля '%1' была успешно открыта."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка открытия формы выбора:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для установки переключателя
&НаКлиенте
Функция УстановитьПереключатель(ИмяЭлемента, Значение)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'set_radio_button' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Ванесса.ВОткрытойФормеЯМеняюЗначениеПереключателяСЗаголовкомНа(ИмяЭлемента, Значение, Истина);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Переключатель '%1' был успешно установлен."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка установки переключателя:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для установки флага
&НаКлиенте
Функция УстановитьФлаг(ИмяЭлемента, ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'set_checkbox' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			Ванесса.ВОткрытойФормеВТЧЯУстанавливаюФлагСЗаголовком(ИмяТаблицы, ИмяЭлемента, Истина);
		Иначе
			Ванесса.ВОткрытойФормеЯУстанавливаюФлагСЗаголовком(ИмяЭлемента, Истина);
		КонецЕсли;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Флаг '%1' был успешно установлен."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка установки флага:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для снятия флага
&НаКлиенте
Функция СнятьФлаг(ИмяЭлемента, ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'unset_checkbox' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			Ванесса.ВОткрытойФормеВТЧЯСнимаюФлагСЗаголовком(ИмяТаблицы, ИмяЭлемента, Истина);
		Иначе
			Ванесса.ВОткрытойФормеЯСнимаюФлагСЗаголовком(ИмяЭлемента, Истина);
		КонецЕсли;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Флаг '%1' был успешно снят."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка снятия флага:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для переключения флага
&НаКлиенте
Функция ПереключитьФлаг(ИмяЭлемента, ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'toggle_checkbox' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			Ванесса.ВОткрытойФормеВТЧЯИзменяюФлагСЗаголовком(ИмяТаблицы, ИмяЭлемента, Истина);
		Иначе
			Ванесса.ВОткрытойФормеЯИзменяюФлагСЗаголовком(ИмяЭлемента, Истина);
		КонецЕсли;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Флаг '%1' был успешно переключен на противоположное значение."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка переключения флага:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для нажатия гиперссылки
&НаКлиенте
Функция НажатьГиперссылку(ИмяЭлемента, ЗаголовокГиперссылки, ИндексГиперссылки)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'click_hyperlink' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ИндексГиперссылки = Неопределено И ЗаголовокГиперссылки = Неопределено Тогда
			// Значит это обычная гиперссылка
			Ванесса.ВОткрытойФормеЯНажимаюНаГиперссылкуСЗаголовком(ИмяЭлемента, Истина);
		ИначеЕсли ИндексГиперссылки <> Неопределено Тогда	
			// Значит это номер гиперссылки в форматированной строке.
			Ванесса.УПоляЯНажимаюГиперссылку(ИмяЭлемента, ИндексГиперссылки, Истина, Ложь);
		ИначеЕсли ЗаголовокГиперссылки <> Неопределено Тогда	
			// Значит это номер гиперссылки в форматированной строке.
			Ванесса.УПоляЯНажимаюГиперссылку(ИмяЭлемента, ЗаголовокГиперссылки, Истина, Ложь);
		КонецЕсли;	
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Гиперссылка в поле '%1' была успешно нажата."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка нажатия гиперссылки:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для эмуляции перехода TAB
&НаКлиенте
Функция ЭмуляцияПереходаTAB(НаправлениеПерехода)
	
	Попытка
		Если НаправлениеПерехода = "previous" Тогда
			Ванесса.ЯПерехожуКПредыдущемуРеквизиту();
			Возврат Ванесса.Локализовать("Был выполнен переход к предыдущему элементу (эмуляция SHIFT+TAB).");
		Иначе
			Ванесса.ЯПерехожуКСледующемуРеквизиту();
			Возврат Ванесса.Локализовать("Был выполнен переход к следующему элементу (эмуляция TAB).");
		КонецЕсли;
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка эмуляции перехода TAB:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для активизации колонки таблицы
&НаКлиенте
Функция АктивизироватьКолонкуТаблицы(ИмяТаблицы, ИмяКолонки)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'activate_column' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ИмяКолонки) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'activate_column' необходимо указать параметр column_name (значение).");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Ванесса.ВОткрытойФормеВТЧЯАктивизируюПоле(ИмяТаблицы, ИмяКолонки, Истина);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Колонка '%1' в таблице '%2' была успешно активизирована."), ИмяКолонки, ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка активизации колонки таблицы:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для перехода к следующей колонке (TAB)
&НаКлиенте
Функция ПереходКСледующейКолонке(ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'navigate_next_column' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Ванесса.ВТаблицеЯПерехожуКСледующейЯчейке(ИмяТаблицы);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Выполнен переход к следующей колонке в таблице '%1' (эмуляция TAB)."), ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка перехода к следующей колонке:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для перехода к предыдущей колонке (SHIFT+TAB)
&НаКлиенте
Функция ПереходКПредыдущейКолонке(ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'navigate_previous_column' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Ванесса.ВТаблицеЯПерехожуКПредыдущейЯчейке(ИмяТаблицы);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Выполнен переход к предыдущей колонке в таблице '%1' (эмуляция SHIFT+TAB)."), ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка перехода к предыдущей колонке:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для начала редактирования строки
&НаКлиенте
Функция НачатьРедактированиеСтроки(ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'start_edit_row' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
		Если Таблица = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' не найдена."), ИмяТаблицы);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		// Начало редактирования строки
		Если НЕ Таблица.ТекущийРежимРедактирование() Тогда
			Таблица.ИзменитьСтроку();
		Иначе	
			Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' уже в режиме редактирования."), ИмяТаблицы);
		КонецЕсли;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В таблице '%1' начато редактирование строки."), ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка начала редактирования строки:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для окончания редактирования строки
&НаКлиенте
Функция ЗакончитьРедактированиеСтроки(ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'end_edit_row' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Ванесса.ЯЗавершаюРедактированиеСтроки(ИмяТаблицы);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В таблице '%1' окончено редактирование строки."), ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка окончания редактирования строки:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для перехода к строке таблицы
&НаКлиенте
Функция ПереходКСтрокеТаблицы(ИмяТаблицы, RowCriteria)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'navigate_to_row' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(RowCriteria) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'navigate_to_row' необходимо указать параметр row_criteria (значение).");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
		Если Таблица = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' не найдена."), ИмяТаблицы);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		ПоляТаблицы = Таблица.НайтиОбъекты(Тип("ТестируемоеПолеФормы"));
		КешЗаголовковПолей = Новый Соответствие;
		Для Каждого ТекПоле Из ПоляТаблицы Цикл
			Если НЕ ЗначениеЗаполнено(ТекПоле.ТекстЗаголовка) Тогда
				Продолжить;
			КонецЕсли;
			КешЗаголовковПолей.Вставить(НРег(ТекПоле.ТекстЗаголовка), Истина);
		КонецЦикла;
		
		// Парсим row_criteria в формате "Колонка=Значение;Колонка2=Значение2"
		Отбор = Новый Структура;
		СтрокиПар = СтрРазделить(RowCriteria, ";");
		Для Каждого ТекСтрока Из СтрокиПар Цикл
			ТекСтрока = СокрЛП(ТекСтрока);
			Если ПустаяСтрока(ТекСтрока) Тогда
				Продолжить;
			КонецЕсли;
			Индекс = Найти(ТекСтрока, "=");
			Если Индекс > 0 Тогда
				ИмяКолонки = СокрЛП(Лев(ТекСтрока, Индекс - 1));
				ЗначениеКолонки = СокрЛП(Сред(ТекСтрока, Индекс + 1));
				Отбор.Вставить(ИмяКолонки, ЗначениеКолонки);
			КонецЕсли;
		КонецЦикла;
		
		Если Отбор.Количество() = 0 Тогда
			ТекстОшибки = Ванесса.Локализовать("Не удалось распознать параметры для перехода к строке. Формат: 'Колонка=Значение;Колонка2=Значение2'.");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстОшибки);
		КонецЕсли;
		
		ПоляКоторыхНет = Новый Массив;
		Для Каждого КлючЗначения Из Отбор Цикл
			Если КешЗаголовковПолей[НРег(КлючЗначения.Ключ)] = Неопределено Тогда
				ПоляКоторыхНет.Добавить(КлючЗначения.Ключ);
			КонецЕсли;
		КонецЦикла;
		
		Если ПоляКоторыхНет.Количество() > 0 Тогда
			Строки = Новый Массив;
			Строки.Добавить(Ванесса.Локализовать("Для перехода к строке были указаны колонки которых нет в таблице:"));
			Для Каждого Элем Из ПоляКоторыхНет Цикл
				Строки.Добавить("  -" + Элем);
			КонецЦикла;
			
			Строки.Добавить("");
			Строки.Добавить(Ванесса.Локализовать("Колонки, которые допустимо указывать для перехода:"));
			Для Каждого ТекПоле Из ПоляТаблицы Цикл
				Если НЕ ЗначениеЗаполнено(ТекПоле.ТекстЗаголовка) Тогда
					Продолжить;
				КонецЕсли;
				Строки.Добавить("  -" + ТекПоле.ТекстЗаголовка);
			КонецЦикла;
			
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса._СтрСоединить(Строки, Символы.ПС));
		КонецЕсли;
		
		Колонки = Новый Структура;
		Значения = Новый Структура;
		Сч = 0;
		Для Каждого КлючЗначения Из Отбор Цикл
			Сч = Сч + 1;
			Колонки.Вставить("Кол" + XMLСтрока(Сч), КлючЗначения.Ключ);
			Значения.Вставить("Кол" + XMLСтрока(Сч), КлючЗначения.Значение);
		КонецЦикла;
		
		ТабПарам = Новый Массив;
		ТабПарам.Добавить(Колонки);
		ТабПарам.Добавить(Значения);
		
		ДопПараметры = Новый Структура;
		ДопПараметры.Вставить("ПолучатьЗначениеТЧ", Ложь);
		Ванесса.ВТаблицеЯПерехожуКСтроке(ИмяТаблицы, ТабПарам,,,ДопПараметры);
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В таблице '%1' выполнена переход к строке с указанными параметрами."), ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка перехода к строке таблицы:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для выбора текущей строки
&НаКлиенте
Функция ВыбратьСтрокуТаблицы(ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'select_row' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Ванесса.ВТаблицеЯВыбираюТекущуюСтроку(ИмяТаблицы);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В таблице '%1' выбрана текущая строка."), ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка выбора строки таблицы:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для перехода к следующей строке таблицы
&НаКлиенте
Функция ПереходКСледующейСтрокеТаблицы(ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'navigate_next_row' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
		Если Таблица = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' не найдена."), ИмяТаблицы);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		Попытка
			Таблица.ПерейтиКСледующейСтроке();
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Невозможно перейти к следующей строке. Возможно, вы находитесь на последней строке таблицы.");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В таблице '%1' выполнен переход к следующей строке."), ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка перехода к следующей строке таблицы:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для перехода к предыдущей строке таблицы
&НаКлиенте
Функция ПереходКПредыдущейСтрокеТаблицы(ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'navigate_previous_row' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
		Если Таблица = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' не найдена."), ИмяТаблицы);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		Попытка
			Таблица.ПерейтиКПредыдущейСтроке();
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Невозможно перейти к предыдущей строке. Возможно, вы находитесь на первой строке таблицы.");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В таблице '%1' выполнен переход к предыдущей строке."), ИмяТаблицы);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка перехода к предыдущей строке таблицы:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для получения выделенных строк таблицы
&НаКлиенте
Функция ПолучитьВыделенныеСтрокиТаблицы(ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяТаблицы) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'get_selected_rows' необходимо указать параметр table_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
		Если Таблица = Неопределено Тогда
			ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' не найдена."), ИмяТаблицы);
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		
		ВыделенныеСтроки = Таблица.ПолучитьВыделенныеСтроки();
		
		Строки = Новый Массив;
		Строки.Добавить(Ванесса.Локализовать("# Данные выделенных строк таблицы '%1'") + " " + ИмяТаблицы);
		Строки.Добавить("");
		
		ЗначениеЭлемента = Ванесса.ПолучитьСтрокуСТЧФормаTestClient(ВыделенныеСтроки, Неопределено);
		Строки.Добавить(ЗначениеЭлемента);
		
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("Общее количество строк в таблице: ") + Формат(ВыделенныеСтроки.Количество(), "ЧГ=0; ЧН=0"));
		
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка получения данных строки таблицы:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для создания нового элемента
&НаКлиенте
Функция СоздатьНовый(ИмяЭлемента, ИмяТаблицы)
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
			Поле = Таблица.НайтиОбъект(,,ИмяЭлемента);
			Если Поле = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Поле '%1' не найдено в таблице '%2'."), ИмяЭлемента, ИмяТаблицы);
				Ванесса.ДополнитьОшибку_НетПоляТаблицыMCP(Таблица, ТекстОшибки);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			Поле.Активизировать();
			Если НЕ Таблица.ТекущийРежимРедактирование() Тогда
				Таблица.ИзменитьСтроку();
			КонецЕсли;
			
			Ванесса.ЯНажимаюНаКнопкуСоздатьПоля(ИмяЭлемента, Истина);
		Иначе
			Ванесса.ЯНажимаюНаКнопкуСоздатьПоля(ИмяЭлемента, Истина);
		КонецЕсли;
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("У поля '%1' вызвана команда создания нового элемента."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка создания нового элемента:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для переключения на закладку
&НаКлиенте
Функция ПереключитьсяНаЗакладку(ИмяЭлемента)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'switch_tab' необходимо указать параметр element_name (имя группы/закладки).");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Ванесса.ВОткрытойФормеЯПерехожуКЗакладкеСЗаголовком(ИмяЭлемента, Истина);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Закладка '%1' была успешно активизирована."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка переключения на закладку:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для разворачивания/сворачивания группы
&НаКлиенте
Функция РазвернутьСвернутьГруппу(ИмяЭлемента, Действие)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'toggle_group' необходимо указать параметр element_name (имя группы).");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Параметр value обязателен: 'expand' - развернуть, 'collapse' - свернуть
	Если НЕ ЗначениеЗаполнено(Действие) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'toggle_group' необходимо указать параметр value: 'expand' (развернуть) или 'collapse' (свернуть).");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ДействиеСтрок = СокрЛП(Нрег(Действие));
	Если НЕ (ДействиеСтрок = "expand" ИЛИ ДействиеСтрок = "collapse") Тогда
		ТекстСообщения = Ванесса.Локализовать("Недопустимое значение параметра value для действия 'toggle_group'. Допустимые значения: 'expand' (развернуть), 'collapse' (свернуть).");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Если ДействиеСтрок = "expand" Тогда
			// Явное развертывание
			Ванесса.ВОткрытойФормеЯРазворачиваюГруппуСЗаголовком(ИмяЭлемента, Истина);
			Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Группа '%1' была развернута."), ИмяЭлемента);
		Иначе
			// Явное сворачивание
			Ванесса.ВОткрытойФормеЯСворачиваюГруппуСЗаголовком(ИмяЭлемента, Истина);
			Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Группа '%1' была свернута."), ИмяЭлемента);
		КонецЕсли;
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка выполнения действи с группой:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для активизации элемента шапки
&НаКлиенте
Функция АктивизироватьЭлементШапки(ИмяЭлемента)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'activate_header_element' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		Ванесса.ЯАктивизируюПоле(ИмяЭлемента, Истина);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Элемент шапки '%1' был успешно активизирован."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка активизации элемента шапки:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для очистки поля (шапка или таблица)
&НаКлиенте
Функция ОчиститьПоле(ИмяЭлемента, ИмяТаблицы)
	
	Если НЕ ЗначениеЗаполнено(ИмяЭлемента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'clear_field' необходимо указать параметр element_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		Если ЗначениеЗаполнено(ИмяТаблицы) Тогда
			// Поиск в таблице
			Таблица = Ванесса.НайтиТЧПоИмени(ИмяТаблицы);
			Если Таблица = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Таблица '%1' не найдена."), ИмяТаблицы);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
			Поле = Таблица.НайтиОбъект(,,ИмяЭлемента);
			Если Поле = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Поле '%1' не найдено в таблице '%2'."), ИмяЭлемента, ИмяТаблицы);
				Ванесса.ДополнитьОшибку_НетПоляТаблицыMCP(Таблица, ТекстОшибки);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
			// Активизируем поле и очищаем его
			Поле.Активизировать();
			
			Если НЕ Таблица.ТекущийРежимРедактирование() Тогда
				Таблица.ИзменитьСтроку();
			КонецЕсли;
			
			// Очищаем значение поля
			Поле.Очистить();
		Иначе
			// Поиск в шапке формы
			Поле = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку(ИмяЭлемента, Истина,,,ДопПараметры);
			Если Поле = Неопределено Тогда
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Поле '%1' не найдено."), ИмяЭлемента);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;
			
			// Очищаем значение поля
			Поле.Очистить();
		КонецЕсли;
		
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Поле '%1' было успешно очищено."), ИмяЭлемента);
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка очистки поля:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Функция ИмяПровайдера()
	Возврат "VanessaAutomation";
КонецФункции	

&НаКлиенте
Функция ЗначениеПараметраИзЗапроса(Запрос, ИмяПараметра)
	
	Значение = "";
	Запрос.Аргументы.Свойство(ИмяПараметра, Значение);
	Возврат Значение;
	
КонецФункции	

&НаКлиенте
Процедура MCPИнструментПолучитьВсеШаги(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
    //Свойства.Вставить("text", МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", "Текст для обработки"));
    Поля = Новый Массив;
    //Поля.Добавить("text");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, Поля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_all_steps_in_json_format",
        Ванесса.Локализовать("Возвращает все доступные шаги Vanessa Automation в формате массива json. Преполагается, что результат будет сохранен в файл и далее будет обрабатываться."),
        Схема,
        Новый ОписаниеОповещения("ВсеДоступныеШагиMCP", Вычислить("ЭтотОбъект"))),
		ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментНайтиШагиПоКлючевымСловам(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	//Представление шага
	Описание_search_name = Ванесса.Локализовать("Поисковый запрос без учета регистра. Строка ищется как подстрока в представлении шага.
	|Для альтернатив по принципу ИЛИ используйте разделитель '|', например 'первая часть|вторая часть'. Если параметр не указан — возвращаются все шаги.");
    Свойства.Вставить("search_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_search_name));
	
	Описание_exclude_name = Ванесса.Локализовать("Поисковый запрос без учета регистра. Шаг будет исключен из результата поиска, если в представлении шага будет найдено указанное значение.
	|Для альтернатив по принципу ИЛИ используйте разделитель '|', например 'первая часть|вторая часть'. Если параметр не указан — возвращаются все шаги.");
    Свойства.Вставить("exclude_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_exclude_name));
	
	//Описание шага
	Описание_search_description = Ванесса.Локализовать("Поисковый запрос без учета регистра. Строка ищется как подстрока в описании шага.
	|Для альтернатив по принципу ИЛИ используйте разделитель '|', например 'первая часть|вторая часть'. Если параметр не указан — возвращаются все шаги.");
    Свойства.Вставить("search_description", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_search_description));
	
	Описание_exclude_description = Ванесса.Локализовать("Поисковый запрос без учета регистра. Шаг будет исключен из результата поиска, если в описании шага будет найдено указанное значение.
	|Для альтернатив по принципу ИЛИ используйте разделитель '|', например 'первая часть|вторая часть'. Если параметр не указан — возвращаются все шаги.");
    Свойства.Вставить("exclude_description", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_exclude_description));
	
	//Тип шага
	Описание_search_type = Ванесса.Локализовать("Поисковый запрос без учета регистра. Строка ищется как подстрока в типе шага.
	|Для альтернатив по принципу ИЛИ используйте разделитель '|', например 'первая часть|вторая часть'. Если параметр не указан — возвращаются все шаги.");
    Свойства.Вставить("search_type", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_search_type));
	
	Описание_exclude_type = Ванесса.Локализовать("Поисковый запрос без учета регистра. Шаг будет исключен из результата поиска, если в типе шага будет найдено указанное значение.
	|Для альтернатив по принципу ИЛИ используйте разделитель '|', например 'первая часть|вторая часть'. Если параметр не указан — возвращаются все шаги.");
    Свойства.Вставить("exclude_type", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_exclude_type));
	
	// Фильтры по условиям и циклам
	Описание_loops_only = Ванесса.Локализовать("Если передано значение true, то будут отбираться только шаги, которые являются циклами. Значение по умолчанию false. Необязательный.");
    Свойства.Вставить("loops_only", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("boolean", Описание_loops_only));
	Описание_conditions_only = Ванесса.Локализовать("Если передано значение true, то будут отбираться только шаги, которые всегда являются условиями. Значение по умолчанию false. Необязательный.");
    Свойства.Вставить("conditions_only", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("boolean", Описание_conditions_only));
	
	// Количество шагов в выдаче
	Описание_limit = Ванесса.Локализовать("Максимальное количество результатов. 0 — вернуть все результаты. По умолчанию 50.");
    Свойства.Вставить("limit", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("number", Описание_limit));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "search_for_steps_by_keywords",
        Ванесса.Локализовать("Ищет шаги Vanessa Automation по переданной строке поиска и возвращает их в формате Markdown.
		|Поиск производится по: представлению шагов, описании шагов и типу шагов.
		|В любой параметр можно передавать несколько значений, разделенных вертикальной чертой |.
		|Пример поиска шагов в представлении которых встречается слово ""кнопка"": search_name = ""кнопка|кнопки|кнопку"".
		|Пример исключения шагов в представлении которых встречается слово ""поле"": exclude_name = ""поле|поля"".
		
		|Пример поиска шагов в описании которых встречается слово ""клиент тестирования"": search_description = ""клиент тестирования"".
		|Пример исключения шагов в описании которых встречается слово ""гиперссылка"": exclude_description = ""гиперссылка|гиперссылки"".
		
		|Пример поиска шагов в типе которых встречается строка ""Объекты конфигурации"": search_type = ""Объекты конфигурации"".
		|Пример исключения шагов в описании которых встречается строка ""Переменные среды"": exclude_type = ""Переменные среды"".
		|"),
        Схема,
        Новый ОписаниеОповещения("ПоискШаговПоСтроке", Вычислить("ЭтотОбъект"))),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьИмяЗаголовокАктивногоОкнаИФормы(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_type = Ванесса.Локализовать("Параметр type. Может принимать значения: window_caption, navigation_link, form_name, form_caption");
    Свойства.Вставить("type", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_type));
	
    ТребуемыеПоля = Новый Массив;
    ТребуемыеПоля.Добавить("type");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_active_window_data",
        Ванесса.Локализовать("В зависимости от параметра type возвращает важные свойства активного окна клиента тестирования:
		|- если type=window_caption - возвращает самое важное значение активного окна: его заголовок. По заголовку окна можно найти окно и получить его данные, можно активизировать окно, можно закрыть окно.                   
		|- если type=navigation_link - возвращает навигационную ссылку активного окна. Навигационная ссылка потом может быть использована для открытия окна по навигационной ссылке. Также навигационная ссылка помогает понять с каким объектом метаданных связано активное окно.
		|- если type=form_name - возвращает имя фромы, подчиненной активному окну. Имя формы позволяет понять с каким объектом метаданных связано активное окно, т.к. в имени формы чаще всего содержится имя объекта метаданных.
		|- если type=form_caption - возвращает заголовок формы, подчиненной активному окну. Чаще всего такой заголовок формы совпадает с заголовком окна-владельца и он нужен редко.
		|"),
        Схема,
        Новый ОписаниеОповещения("ПолучитьИмяЗаголовокАктивногоОкнаИФормы", Вычислить("ЭтотОбъект"))),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьИнформациюОТекущейФорме(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_format = Ванесса.Локализовать("Формат вывода: 'full' (дерево + Gherkin + Сообщения), 'gherkin' (только Gherkin-шаги), 'elements' (только дерево элементов, выводятся все элементы формы), 'buttons' (только кнопки), 'messages' (только текст окна сообщений пользвателю). По умолчанию 'full'.");
    Свойства.Вставить("format", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_format));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_form_analysis",
        Ванесса.Локализовать("Возвращает информацию о текущей форме тестируемого приложения: дерево элементов или состояние формы как Gherkin-шаги. Требует подключённого клиента тестирования.
		|Важно обращать на флаг Видимости у элемента формы и групп, в которые он входит. Если у элемента Видимость=Истина, а у группы Видимость=Ложь, то скорее всего элемент формы не виден пользователю и он не может с ним взаимодействовать."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьИнформациюОТекущейФорме", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментВыполнитьОдинШагТекстом(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_stepText = Ванесса.Локализовать("Полный текст шага для выполнения. Обязательный параметр.");
    Свойства.Вставить("stepText", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_stepText));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("stepText");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "execute_step_from_text",
        Ванесса.Локализовать("Выполняет произвольный текст, в котором содержится шаг Vanessa Automation. Параметр stepText обязателен.
		|Нельзя вызывать шаги, которые в себе содержат строку 'в течение' т.к. они работают асинхронно и Vanessa Automation не будет ждать завершения выполнения такого шага при работе инструмента execute_step_from_text - такие шаги нужно выполнять только в рамках feature файла."),
        Схема,
        Новый ОписаниеОповещения("ВыполнитьОдинШагТекстом", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПодключитьКлиентТестирования(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_profileName = Ванесса.Локализовать("Имя профиля клиента тестирования.");
    Свойства.Вставить("profileName", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_profileName));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("profileName");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "connect_test_client",
        Ванесса.Локализовать("Подключает клиент тестирования 1С по имени профиля. Необходимо для работы get_form_analysis, execute_feature_step и запуска сценариев, которым требуется клиент тестирвания для работы."),
        Схема,
        Новый ОписаниеОповещения("ПодключитьКлиентТестирования", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментВыполнитьОдинШагВФичаФайле(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_filePath = Ванесса.Локализовать("Путь к .feature файлу. Если параметр не указан, то команда будет выполняться в текущей вкладке редактора в уже открытом файле.");
    Свойства.Вставить("filePath", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_filePath));
	
	Описание_lineNumber = Ванесса.Локализовать("Номер строки в файле (начиная с 1).");
    Свойства.Вставить("lineNumber", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("integer", Описание_lineNumber));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("lineNumber");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "execute_feature_step",
        Ванесса.Локализовать("Выполняет шаг из .feature файла по номеру строки. Валидирует что файл открыт в редакторе, активирует вкладку при необходимости."),
        Схема,
        Новый ОписаниеОповещения("ВыполнитьОдинШагВФичаФайле", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьСписокВнутреннихОконКлиентаТестирования(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_window_list_testclient",
        Ванесса.Локализовать("Получает список внутренних окон клиента тестирования.
		|Результат можно использовать для управления окнами с помощью инструмента window_management (активизировать, закрыть, открыть навигационную ссылку), либо для получения данных из текущего окна get_active_window_data.
		|Результат нельзя использовать для получения скриншота окна, т.к. список внутренних окон клиента тестирования и список окон с точки зрения операционной системы - это разные вещи."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьСписокВнутреннихОконКлиентаТестирования", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментУправлениеОкномКлиентаТестирования(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_action = Ванесса.Локализовать("Действие: 'activate' (активизировать окно по заголовку), 'close' (закрыть окно по заголовку или части заголовка), 'open_navigation' (открыть навигационную ссылку). Обязательный параметр.");
    Свойства.Вставить("action", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_action));
	
	Описание_window_title = Ванесса.Локализовать("Заголовок внутреннего окна клиента тестирования. Обязательный параметр для действий 'activate' и 'close'. Можно использовать символы * для поиска по частичному совпадению (например, '*Окно*' найдет все окна, содержащие слово 'Окно').");
    Свойства.Вставить("window_title", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_window_title));
	
	Описание_navigation_link = Ванесса.Локализовать("Навигационная ссылка для открытия. Обязательный параметр для действия 'open_navigation'.");
    Свойства.Вставить("navigation_link", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_navigation_link));

    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("action");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "window_management",
        Ванесса.Локализовать("Универсальный инструмент для управления окнами клиента тестирования.
		|Поддерживаемые действия (параметр action):
		|- 'activate' - активизировать окно по заголовку (требуется window_title)
		|- 'close' - закрыть окно по заголовку или части заголовка (требуется window_title)
		|- 'open_navigation' - открыть навигационную ссылку (требуется navigation_link)
		|
		|Для активизации окна: укажите action='activate' и window_title с полным заголовком окна или используйте символы * для поиска по частичному совпадению (например, '*Окно*').
		|Для закрытия окна: укажите action='close' и window_title (можно часть заголовка для поиска или используйте символы * для поиска по частичному совпадению).
		|Для открытия формы: укажите action='open_navigation' и navigation_link с навигационной ссылкой."),
        Схема,
        Новый ОписаниеОповещения("УправлениеОкномКлиентаТестирования", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментУправлениеПеременными(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_action = Ванесса.Локализовать("Действие: 'get' (по умолчанию), 'set', 'delete'.");
    Свойства.Вставить("action", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_action));
	
	Описание_name = Ванесса.Локализовать("Имя переменной (обязательно для set и delete).");
    Свойства.Вставить("name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_name));
	
	Описание_value = Ванесса.Локализовать("Значение переменной (обязательно для set).");
    Свойства.Вставить("value", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_value));

	Описание_is_global = Ванесса.Локализовать("Флаг, является ли переменная глобальной. Значение по умолчанию - false.");
    Свойства.Вставить("is_global", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("boolean", Описание_is_global));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "manage_variables",
        Ванесса.Локализовать("Чтение, установка и удаление переменных тестового контекста.
		|Действия с переменными: get (по умолчанию), set, delete.
		|Переменные позволяют хранить произвольные значения в памяти Vanessa Automation.
		|Переменные бывают локальные и глобальные. Локальные существуют только во время выполнения сценария. Перед началом выполнения сценария все локальные переменные удаляются.
		|Глобальные переменные существуют пока работает Vanessa Automation и не удаляются перед выполнением сценария."),
        Схема,
        Новый ОписаниеОповещения("УправлениеПеременными", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьПодробнуюИнформациюОШагеВСтрокеСценария(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_lineNumber = Ванесса.Локализовать("Номер строки. Параметр обязательный");
    Свойства.Вставить("lineNumber", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("integer", Описание_lineNumber));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("lineNumber");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_info_about_line_scenario",
        Ванесса.Локализовать("Получает подробную информацию о указанной строке файла открытого в редакторе.
		|Если в строке находится шаг сценария, то будет возвращена подробная информация о шаге."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьПодробнуюИнформациюОШагеВСтрокеСценария", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьДанныеИзБазыЗнаний(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_format = Ванесса.Локализовать("Формат получения ответа:
	|'short_info'. Рекомендуется вызвать хотя бы один раз с значением format=short_info, чтобы получить короткую информацию о базе знаний: сколько в ней есть вопросов, каков размер всей базы знаний (количество символов).
	|'questions_only'. Возвращает только текст вопросов и их номера, чтобы потом можно было получить ответ по номеру вопроса.
	|'one_question'. Возвращает ответ по номеру вопроса. В параметре question_number нужно передать номер вопроса.
	|'question_search'. Выполняет поиск по вопросам. В параметр 'search_string' передаётся строка для поиска. Можно использовать разделитель для поиска по нескольким значениям. Поиск не чувствителен к регистру. Пример строки поиска: 'кнопка|кнопку'.
	|'answers_search'. Выполняет поиск по ответам. В параметр 'search_string' передаётся строка для поиска. Можно использовать разделитель для поиска по нескольким значениям. Поиск не чувствителен к регистру. Пример строки поиска: 'кнопка|кнопку'.
	|'all'. Возвращает всю базу знаний в виде текста. Перед получением всей базы рекомендуется выполнить поиск с помощью question_search, answers_search или получить все вопросы с помощью questions_only.");
    Свойства.Вставить("format", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_format));
	
	Описание_question_number = Ванесса.Локализовать("Номер вопроса");
    Свойства.Вставить("question_number", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("number", Описание_question_number));
	Описание_search_string = Ванесса.Локализовать("Строка поиска");
    Свойства.Вставить("search_string", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_search_string));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("format");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_data_from_knowledge_base",
        Ванесса.Локализовать("Получает информацию из базы знаний, в которой хранится информация о часто возникающих вопросах и как их решить.
		|Данные в базе знаний хранятся в формате Вопрос - ответ.
		|Чтобы экономить токены можно получать данные порциями.
		|Можно получить только тексты вопросов, чтобы потом получить ответ только на нужный вопрос.
		|Можно выполнить поиск по вопросам и оветам. Поиск работает без учёта регистра букв."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьДанныеИзБазыЗнаний", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьДанныеОЧастоИспользуемыхШагах(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_limit = Ванесса.Локализовать("Сколько шагов вернуть. Параметр необязательный. Значение по умолчанию 100.");
    Свойства.Вставить("limit", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("number", Описание_limit));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "frequently_used_steps",
        Ванесса.Локализовать("Возвращает информацию о том какие шаги чаще используются статистике больших проектов.
		|В начале идут часто используемые шаги, далее идут более редко используемые шаги.
		|Возвращает представление шага, описание шага, тип шага.
		|Также возвращает информацию сколько всего шагов есть в накопленной статистике."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьДанныеОЧастоИспользуемыхШагах", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Универсальный инструмент для работы с командным интерфейсом
&НаКлиенте
Процедура MCPИнструментУправлениеКоманднымИнтерфейсом(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_action = Ванесса.Локализовать("Действие: 'get_section_panel' (получить панель разделов), 'get_function_panel' (получить панель функций), 'click_section_panel' (нажать кнопку панели разделов), 'click_function_panel' (нажать кнопку панели функций). Обязательный параметр.");
    Свойства.Вставить("action", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_action));
	
	Описание_command = Ванесса.Локализовать("Имя команды (кнопки) для нажатия. Обязательный параметр для действий click_section_panel и click_function_panel.");
    Свойства.Вставить("command", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_command));
	
	Описание_group = Ванесса.Локализовать("Имя группы команды в панели функций. Необязательный параметр для действия click_function_panel. Параметр надо передавать если имена команд в панели функций не уникальны.");
    Свойства.Вставить("group", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_group));
	
	Описание_returnFunctionPanel = Ванесса.Локализовать("Если true, то после нажатия кнопки раздела будет возвращено содержимое панели функций. При этом панель разделов после чтения данных будет возвращена в предыдущее состояние. По умолчанию false. Используется для действия click_section_panel.");
    Свойства.Вставить("returnFunctionPanel", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("boolean", Описание_returnFunctionPanel));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("action");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "manage_command_interface",
        Ванесса.Локализовать("Универсальный инструмент для работы с командным интерфейсом 1С:Предприятия.
		|Поддерживаемые действия (параметр action):
		|- 'get_section_panel' - получить панель разделов (первый уровень)
		|- 'get_function_panel' - получить панель функций (второй уровень). У функций есть навигационная ссылка, которую потом можно использовать для прямого вызова функции.
		|- 'click_section_panel' - нажать кнопку панели разделов
		|- 'click_function_panel' - нажать кнопку панели функций. После нажатия на кнопку панели функций нужно проверить, что открылось нужное окно клиента тестирования с помощью инструмента `get_window_list_testclient`.
		|
		|Для click_section_panel:
		|- command (обязательно) - имя кнопки для нажатия
		|- returnFunctionPanel (необязательно) - вернуть содержимое панели функций после нажатия
		|
		|Для click_function_panel:
		|- command (обязательно) - имя кнопки для нажатия
		|- group (необязательно) - имя группы, если команды не уникальны
		|
		|Требует подключённого клиента тестирования."),
        Схема,
        Новый ОписаниеОповещения("УправлениеКоманднымИнтерфейсом", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Получает список расширений, установленных в базе
&НаКлиенте
Процедура MCPИнструментПолучитьСписокРасширений(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_extension_list",
        Ванесса.Локализовать("Получает список всех расширений установленных в клиент тестирования. Клиент тестирования должен быть подключен.
		|Возвращает информацию о каждом расширении.
		|Поля: Имя, Версия, Назначение;
		|Флаги: Активно, БезопасныйРежим."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьСписокРасширений", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Универсальный инструмент для работы с таблицей профилей клиентов тестирования
// Получает важные данные об окружении
&НаКлиенте
Функция ПолучитьДанныеОбОкружении(Запрос, ДополнительныеПараметры) Экспорт
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Данные об окружении"));
	Строки.Добавить("");
	
	// Текущая дата и время
	Строки.Добавить(Ванесса.Локализовать("- Текущая дата и время:") + " " + Формат(ТекущаяДата(), "ДЛФ:DD"));
	Строки.Добавить("");
	
	СистемнаяИнформация = Новый СистемнаяИнформация;
	
	// Версия платформы
	Попытка
		Строки.Добавить(Ванесса.Локализовать("## Информация о платформе"));
		Строки.Добавить(Ванесса.Локализовать("- Платформа:") + " " + СистемнаяИнформация.ВерсияПриложения);
	Исключение
		Строки.Добавить(Ванесса.Локализовать("! Не удалось получить информацию об информационной базе: ") + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки()));
	КонецПопытки;
	
	// Данные об операционной системе
	Строки.Добавить("");
	Строки.Добавить(Ванесса.Локализовать("## Операционная система"));
	Строки.Добавить(Ванесса.Локализовать("- Данные ОС:") + " " + СистемнаяИнформация.ТипПлатформы);
	
	// Данные о VA (обычная или Single)
	Строки.Добавить("");
	Строки.Добавить(Ванесса.Локализовать("## Vanessa Automation"));
	Если Объект.ВерсияПоставки = "single" Тогда
		Строки.Добавить("- Это Vanessa Automation Single");
	Иначе	
		Строки.Добавить("- Это обычная Vanessa Automation");
	КонецЕсли;
	Строки.Добавить(Ванесса.Локализовать("- Версия:") + " " + Ванесса.ВерсияVanessaAutomation());
	
	// Информация о внешней компоненте
	Строки.Добавить("");
	Строки.Добавить(Ванесса.Локализовать("## Внешняя компонента VanessaExt"));
	Строки.Добавить(Ванесса.Локализовать("- Включено использование внешней компоненты VanessaExt:") + " " + Ванесса.Объект.ИспользоватьКомпонентуVanessaExt);
	
	// Информация о базе менеджера тестирования
	Строки.Добавить("");
	Строки.Добавить(Ванесса.Локализовать("## Менеджер тестирования"));
	Строки.Добавить(Ванесса.Локализовать("- Строка соединения с информационной базой:") + " " + СтрокаСоединенияИнформационнойБазы());
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Возвращает информацию о доступных типах голосовых уведомлений
&НаКлиенте
Функция ГолосовыеУведомления(Запрос, ДополнительныеПараметры) Экспорт
	
	ТипУведомления = Строка(ЗначениеПараметраИзЗапроса(Запрос, "notification_type"));
	
	Если НЕ ЗначениеЗаполнено(ТипУведомления) Тогда
		ТекстСообщения = Ванесса.Локализовать("Параметр notification_type обязателен. Допустимые значения: 'task_completed', 'decision_required', 'task_cannot_be_completed', 'error_occurred'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр языка озвучки (по умолчанию 'ru')
	Язык = Строка(ЗначениеПараметраИзЗапроса(Запрос, "language"));
	Если НЕ ЗначениеЗаполнено(Язык) Тогда
		Язык = "ru";
	Иначе
		Язык = НРег(СокрЛП(Язык));
		Если Язык <> "ru" И Язык <> "en" Тогда
			ТекстСообщения = Ванесса.Локализовать("Некорректное значение параметра language. Допустимые значения: 'ru' (русский), 'en' (английский).");
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
		КонецЕсли;
	КонецЕсли;
	
	// Проверяем допустимое значение
	ДопустимыеТипы = Новый Массив;
	ДопустимыеТипы.Добавить("task_completed");
	ДопустимыеТипы.Добавить("decision_required");
	ДопустимыеТипы.Добавить("task_cannot_be_completed");
	ДопустимыеТипы.Добавить("error_occurred");
	
	Если ДопустимыеТипы.Найти(ТипУведомления) = Неопределено Тогда
		ТекстСообщения = Ванесса.Локализовать("Недопустимое значение notification_type. Допустимые значения: 'task_completed', 'decision_required', 'task_cannot_be_completed', 'error_occurred'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Если Ванесса.ЭтоLinux Тогда
		ТекстСообщения = Ванесса.Локализовать("Голосовое уведомление не работает под linux.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстСообщения);
	КонецЕсли;
	
	Если НЕ Ванесса.Объект.ИспользоватьКомпонентуVanessaExt Тогда
		ТекстСообщения = Ванесса.Локализовать("Чтобы была возможность выполнить голосовое уведомление нужно в Vanessa Automation включить использование внешней компоненты VanessaExt.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстСообщения);
	КонецЕсли;	
	
	Если КаталогСФайламиОзвучки = Неопределено Тогда
		ДвоичныеДанныеГолос = ДвоичныеДанныеГолосСервер();	
		ЧтениеZipФайла = Новый ЧтениеZipФайла(ДвоичныеДанныеГолос.ОткрытьПотокДляЧтения());
		ВременныйКаталог = ПолучитьИмяВременногоФайла("");
		Ванесса.СоздатьКаталогКомандаСистемы(ВременныйКаталог);
		ЧтениеZipФайла.ИзвлечьВсе(ВременныйКаталог);
		КаталогСФайламиОзвучки = ВременныйКаталог;
	КонецЕсли;	 
	
	ИмяФайла = ТипУведомления + "_" + Язык + ".mp3";
	ПолноеИмяФайла = КаталогСФайламиОзвучки + "\" + ИмяФайла;
	Ванесса.ВнешняяКомпонентаДляСкриншотов.НачатьВызовВоспроизвестиМедиа(Ванесса.ОписаниеПустогоОбработчика(3), ПолноеИмяФайла, ПолноеИмяФайла);
	
	// Формируем ответ в формате Markdown
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Голосовое уведомление выполнено"));
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

&НаКлиенте
Процедура MCPИнструментПолучитьДанныеОбОкружении(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_environment_data",
        Ванесса.Локализовать("Получает важные данные об окружении:
		|- Текущая дата и время
		|- Версия платформы 1С:Предприятие
		|- Данные об операционной системе
		|- Какая используется VA - обычная или Single
		|- Включено ли использование внешней компоненты VanessaExt
		|- В какой базе запущен менеджер тестирования"),
        Схема,
        Новый ОписаниеОповещения("ПолучитьДанныеОбОкружении", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Универсальный инструмент для голосовых уведомлений
&НаКлиенте
Процедура MCPИнструментГолосовыеУведомления(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_notification_type = Ванесса.Локализовать("Тип уведомления: 'task_completed' (задание выполнено), 'decision_required' (нужно принять решение), 'task_cannot_be_completed' (задание не может быть выполнено), 'error_occurred' (возникла ошибка при выполнении задания). Обязательный параметр.");
    Свойства.Вставить("notification_type", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_notification_type));
	
	Описание_language = Ванесса.Локализовать("Язык озвучки: 'ru' (русский, по умолчанию), 'en' (английский). Необязательный параметр.");
    Свойства.Вставить("language", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_language));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("notification_type");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "voice_notification",
        Ванесса.Локализовать("Возвращает информацию о доступных типах голосовых уведомлений для ИИ агента.
		|Полезно в тех случаях, если ИИ агент работает в отдельной виртуальной машине, выполняет какое-то длинное задание и пользователю удобно
		|получать голосовое уведомление о: задании выполненном, необходимости принять решение, ошибке при выполнении задания.
		|Возвращает список доступных типов уведомлений и их описание в формате Markdown."),
        Схема,
        Новый ОписаниеОповещения("ГолосовыеУведомления", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментУправлениеПрофилямиКлиентовТестирования(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_action = Ванесса.Локализовать("Действие: 'get_list' (получить список профилей), 'add' (добавить профиль), 'edit' (редактировать профиль). Обязательный параметр.");
    Свойства.Вставить("action", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_action));
	
	Описание_name = Ванесса.Локализовать("Имя профиля. Обязательный параметр для add и edit.");
    Свойства.Вставить("name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_name));
	
	Описание_synonym = Ванесса.Локализовать("Синоним профиля. Необязательный параметр для add и edit.");
    Свойства.Вставить("synonym", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_synonym));
	
	Описание_infobase_path = Ванесса.Локализовать("Строка подключения к базе. Необязательный параметр для add и edit.");
    Свойства.Вставить("infobase_path", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_infobase_path));
	
	Описание_additional_parameters = Ванесса.Локализовать("Дополнительные параметры подключения. Необязательный параметр для add и edit.");
    Свойства.Вставить("additional_parameters", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_additional_parameters));
	
	Описание_client_type = Ванесса.Локализовать("Тип клиента (Тонкий (основной вариант), Толстый, Web, МобильныйКлиент, МобильноеПриложение, МобильныйКлиентАвтономный, ОбычноеПриложение). Необязательный параметр для add и edit. Значение по умолчанию 'Тонкий'.");
    Свойства.Вставить("client_type", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_client_type));
	
	Описание_computer_name = Ванесса.Локализовать("Имя компьютера. Необязательный параметр для add и edit.");
    Свойства.Вставить("computer_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_computer_name));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("action");
	ТребуемыеПоля.Добавить("name");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "manage_test_client_profiles",
        Ванесса.Локализовать("Универсальный инструмент для работы с таблицей профилей клиентов тестирования Vanessa Automation.
		|Поддерживаемые действия (параметр action):
		|- 'get_list' - получить список всех профилей клиентов тестирования
		|- 'add' - добавить новый профиль в таблицу
		|- 'edit' - редактировать существующий профиль
		|
		|Для add и edit:
		|- name (обязательно) - имя профиля
		|- synonym - синоним профиля.
		|- infobase_path - строка подключения к базе
		|- additional_parameters - дополнительные параметры подключения
		|- client_type - тип клиента (Тонкий (основной вариант), Толстый, Web, МобильныйКлиент, МобильноеПриложение, МобильныйКлиентАвтономный, ОбычноеПриложение).
		|- computer_name - имя компьютера
		|
		|Уточнение. Если параметры клиента тестирования изменились, то это может потребовать перезапуска клиента тестирования.
		|Подробная информация про то как устроена таблица клиентов тестирования есть в базе знаний, инструмент get_data_from_knowledge_base."),
        Схема,
        Новый ОписаниеОповещения("УправлениеПрофилямиКлиентовТестирования", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментУправлениеТочкамиОстанова(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_action = Ванесса.Локализовать("Действие: 'toggle', 'remove_all', 'list'. По умолчанию 'toggle'.");
    Свойства.Вставить("action", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_action));
	
	Описание_line = Ванесса.Локализовать("Номер строки для toggle (если не указан — текущая строка курсора).");
    Свойства.Вставить("line", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("number", Описание_line));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "manage_breakpoints",
        Ванесса.Локализовать("Управление точками останова в Gherkin-сценариях. Действия: toggle (по умолчанию), remove_all, list."),
        Схема,
        Новый ОписаниеОповещения("УправлениеТочкамиОстанова", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
	КонецПроцедуры
	
&НаКлиенте
Процедура MCPИнструментПолучитьСписокОконКлиентаТестированияДляСнятияСкриншотов(ПараметрыСозданияMCP)
	
	Свойства = Новый Структура;
	ТребуемыеПоля = Новый Массив;
	Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
	    "get_window_list_os",
	    Ванесса.Локализовать("Возвращает список окон доступных для снятия скриншота с точки зрения операционной системы. Нужно вызвать перед get_window_screenshot_os."),
	    Схема,
	    Новый ОписаниеОповещения("ПолучитьСписокОконКлиентаТестированияДляСнятияСкриншотов", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьСкриншотОкнаКлиентаТестирования(ПараметрыСозданияMCP)
	
	Свойства = Новый Структура;
	Описание_window_title = Ванесса.Локализовать("Заголовок окна, с которого нужно снять скриншот.");
	Свойства.Вставить("window_title", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_window_title));
	Описание_filename = Ванесса.Локализовать("Имя файла куда сохранить скриншот в формате png. Необязательный параметр. Если имя файла не указано, то файл будет создан во временном каталоге.");
	Свойства.Вставить("file_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_filename));
	Описание_color_mode = Ванесса.Локализовать("Режим цвета скриншота: 'grayscale' (черно-белый, по умолчанию) или 'color' (цветной).");
	Свойства.Вставить("color_mode", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_color_mode));
	
	ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("window_title");
	Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
	    "get_window_screenshot_os",
	    Ванесса.Локализовать("Делает скриншот окна клиента тестирования и возвращает изображение, для последующего анализа скриншота с помощью компьютерного зрения.
		|Параметр window_title определяет с какого окна нужно снять скриншот, т.к. у одного клиента тестирования может быть несколько окон. Параметр обязательный.
		|Чтобы получить список доступных окон для снятия скриншота надо вызвать get_window_screenshot_os.
		|Параметр color_mode определяет режим цвета: 'grayscale' (черно-белый, по умолчанию) или 'color' (цветной).
		|Если клиент тестирования не подключён или не запущен, возвращает ошибку с подсказкой сначала вызвать connect_test_client."),
	    Схема,
	    Новый ОписаниеОповещения("ПолучитьСкриншотОкнаКлиентаТестирования", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментВыполнитьСценарий(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_mode = Ванесса.Локализовать("Режим запуска: 'all' (по умолчанию — все сценарии загруженного feature файла или feature файлов), 'reloadAndRun' (перезагрузить и запустить выбранный), 'selected' (выбранный в редакторе), 'fromCurrentStep' (с текущего шага), 'reloadAndRunFromLine' (перезагрузить фича файл и выполнить сценарий с указанной строки).");
    Свойства.Вставить("mode", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_mode));
	
	Описание_filePath = Ванесса.Локализовать("Путь к .feature файлу. Если указан — файл будет загружен в дерево и открыт в редакторе перед запуском.");
    Свойства.Вставить("filePath", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_filePath));
	
	Описание_lineNumber = Ванесса.Локализовать("Номер строки в файле (Нельзя передавать случайное число. Надо передать реальный номер строки, в которой находится шаг сценария, который можно выполнить). Используется только для режима 'reloadAndRunFromLine'.");
    Свойства.Вставить("lineNumber", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("integer", Описание_lineNumber));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "run_scenario",
        Ванесса.Локализовать("Запускает выполнение сценариев. Отправляет прогресс по каждому шагу (step N/M). Возвращает итоговый результат после завершения.
		|Если указан filePath — загрузит фичу, откроет в редакторе и запустит.
		|Режимы: all (по умолчанию — все сценарии.
		|Если просят выполнить все сценарии надо использовать режим all),
		|reloadAndRun (перезагрузить и запустить фича файл с начала),
		|selected (выбранный в редакторе, перед этим надо вызвать select_scenario),
		|fromCurrentStep (с текущего шага, перед этим нужно вызвать select_step),
		|reloadAndRunFromLine (перезагрузить фича файл и выполнить сценарий со строки указанной в lineNumber).
		|"),
        Схема,
        Новый ОписаниеОповещения("ВыполнитьСценарий", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментОстановитьВыполнениеСценариев(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "stop_scenario",
        Ванесса.Локализовать("Посылает команду Vanessa Automation, что нужно остановить выполнение сценариев. На обработку команды может понадобится время."),
        Схема,
        Новый ОписаниеОповещения("ОстановитьВыполнениеСценариев", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьРезультатВыполненияТестов(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_scenarioId = Ванесса.Локализовать("ID сценария в дереве. Если не указан — последний выполненный.");
    Свойства.Вставить("scenarioId", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_scenarioId));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_test_results",
        Ванесса.Локализовать("Подробные результаты выполнения сценария: статус каждого шага, время, ошибки. Если scenarioId не указан — результат последнего сценария."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьРезультатВыполненияТестов", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьТекущуюДату(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
    //Свойства.Вставить("text", МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", "Текст для обработки"));
    Поля = Новый Массив;
    //Поля.Добавить("text");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, Поля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_current_date",
        Ванесса.Локализовать("Возвращает текущую дату и время сервера 1С:Предприятие."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьТекущуюДатуНаСервере1С", Вычислить("ЭтотОбъект"))),
	ИмяПровайдера());
		
КонецПроцедуры
	
&НаКлиенте
Процедура MCPИнструментСостояниеVanessaAutomation(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
    //Свойства.Вставить("text", МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", "Текст для обработки"));
    Поля = Новый Массив;
    //Поля.Добавить("text");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, Поля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_VanessaAutomation_state",
        Ванесса.Локализовать("Возвращает текущее состояние Vanessa Automation в формате Markdown: выполняется ли сценарий, текущая фича (имя, путь, язык, теги), текущий сценарий (имя, статус, теги), текущий шаг (текст, статус, ошибка), подключен ли клиент тестирования.
		|Используйте для получения контекста перед написанием или анализом тестов."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьСостояниеVanessaAutomation", Вычислить("ЭтотОбъект"))),
    	ИмяПровайдера());
		
КонецПроцедуры
	
&НаСервереБезКонтекста
Функция ПолучитьТекущуюДатуНаСервере1СMCP_Сервер()
	
	Возврат Строка(ТекущаяДата());
	
КонецФункции

&НаКлиенте
Функция MCPБезопасноеСвойство(ИсходнаяСтруктура, ИмяСвойства)
	
	Если ТипЗнч(ИсходнаяСтруктура) = Тип("Структура") Тогда
		Значение = Неопределено;
		Если ИсходнаяСтруктура.Свойство(ИмяСвойства, Значение) Тогда
			Возврат Значение;
		КонецЕсли;
	КонецЕсли;
	Возврат Неопределено;
	
КонецФункции

&НаКлиенте
Функция MCPСформироватьMarkdownСостоянияVA(Данные)
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Состояние Vanessa Automation"));
	Строки.Добавить("");
	Строки.Добавить(Ванесса.Локализовать("- Выполнение сценариев:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["isRunning"]));
	Строки.Добавить(Ванесса.Локализовать("- Клиент тестирования подключен:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["testClientConnected"]));
	Если Данные["testClientConnected"] Тогда
		Строки.Добавить(Ванесса.Локализовать("  " + "- Имя профиля клиента тестирования:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["ИмяПрофиляКлиентаТестирования"]));
	КонецЕсли;	
	Строки.Добавить(Ванесса.Локализовать("- Каталог фич:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["featureDirectory"]));
	Строки.Добавить(Ванесса.Локализовать("- Каталог проекта:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["КаталогПроекта"]));
	Строки.Добавить("");

	Строки.Добавить(Ванесса.Локализовать("## Текущая фича"));
	Строки.Добавить(MCPСформироватьMarkdownРазделСвойств(Данные["currentFeature"]));
	Строки.Добавить("");

	Строки.Добавить(Ванесса.Локализовать("## Текущий сценарий"));
	Строки.Добавить(MCPСформироватьMarkdownРазделСвойств(Данные["currentScenario"]));
	Строки.Добавить("");

	Строки.Добавить(Ванесса.Локализовать("## Текущий шаг"));
	Строки.Добавить(MCPСформироватьMarkdownРазделСвойств(Данные["currentStep"]));
	
	// Подсказки, если ничего не загружено
	ФичаПуста = Данные["currentFeature"] = Неопределено
		ИЛИ (ТипЗнч(Данные["currentFeature"]) = Тип("Структура")
			И (НЕ Данные["currentFeature"].Свойство("name")
				ИЛИ Данные["currentFeature"]["name"] = Неопределено
				ИЛИ СокрЛП(Строка(Данные["currentFeature"]["name"])) = ""));
	СценарийПуст = Данные["currentScenario"] = Неопределено
		ИЛИ (ТипЗнч(Данные["currentScenario"]) = Тип("Структура")
			И (НЕ Данные["currentScenario"].Свойство("name")
				ИЛИ Данные["currentScenario"]["name"] = Неопределено
				ИЛИ СокрЛП(Строка(Данные["currentScenario"]["name"])) = ""));
	Если ФичаПуста И СценарийПуст Тогда
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("## Следующие шаги"));
		Строки.Добавить(Ванесса.Локализовать("- Загрузите фичи: `load_features`"));
		Строки.Добавить(Ванесса.Локализовать("- Подключите клиента тестирования: `connect_test_client`"));
	КонецЕсли;

	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);

КонецФункции

&НаКлиенте
Функция MCPПредставлениеЗначенияДляMarkdown(Значение)
	Если Значение = Неопределено Тогда
		Возврат Ванесса.Локализовать("не указано");
	КонецЕсли;

	Если ТипЗнч(Значение) = Тип("Булево") Тогда
		Если Значение Тогда
			Возврат Ванесса.Локализовать("да");
		КонецЕсли;
		Возврат Ванесса.Локализовать("нет");
	КонецЕсли;

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Если Значение.Количество() = 0 Тогда
			Возврат Ванесса.Локализовать("не указано");
		КонецЕсли;

		Строки = Новый Массив;
		Для Каждого Элемент Из Значение Цикл
			Строки.Добавить(MCPПредставлениеЗначенияДляMarkdown(Элемент));
		КонецЦикла;
		Возврат Ванесса._СтрСоединить(Строки, ", ");
	КонецЕсли;

	Если ТипЗнч(Значение) = Тип("Структура") Тогда
		Строки = Новый Массив;
		Для Каждого КлючЗначение Из Значение Цикл
			Строки.Добавить(КлючЗначение.Ключ + ": " + MCPПредставлениеЗначенияДляMarkdown(КлючЗначение.Значение));
		КонецЦикла;
		Если Строки.Количество() = 0 Тогда
			Возврат Ванесса.Локализовать("не указано");
		КонецЕсли;
		Возврат Ванесса._СтрСоединить(Строки, "; ");
	КонецЕсли;

	Если ТипЗнч(Значение) = Тип("Соответствие") Тогда
		Строки = Новый Массив;
		Для Каждого КлючЗначение Из Значение Цикл
			Строки.Добавить(MCPПредставлениеЗначенияДляMarkdown(КлючЗначение.Ключ) + ": " + MCPПредставлениеЗначенияДляMarkdown(КлючЗначение.Значение));
		КонецЦикла;
		Если Строки.Количество() = 0 Тогда
			Возврат Ванесса.Локализовать("не указано");
		КонецЕсли;
		Возврат Ванесса._СтрСоединить(Строки, "; ");
	КонецЕсли;

	Возврат Строка(Значение);

КонецФункции

&НаКлиенте
Функция MCPСформироватьMarkdownРазделСвойств(ДанныеРаздела)
	Если ДанныеРаздела = Неопределено Тогда
		Возврат Ванесса.Локализовать("Данные отсутствуют.");
	КонецЕсли;

	Если ТипЗнч(ДанныеРаздела) <> Тип("Структура") Тогда
		Возврат MCPПредставлениеЗначенияДляMarkdown(ДанныеРаздела);
	КонецЕсли;

	Строки = Новый Массив;
	Для Каждого КлючЗначение Из ДанныеРаздела Цикл
		Строки.Добавить("- " + КлючЗначение.Ключ + ": " + MCPПредставлениеЗначенияДляMarkdown(КлючЗначение.Значение));
	КонецЦикла;

	Если Строки.Количество() = 0 Тогда
		Возврат Ванесса.Локализовать("Данные отсутствуют.");
	КонецЕсли;

	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);

КонецФункции

&НаКлиенте
Функция ПоискШаговПоСтрокеMCP(search_name, exclude_name, search_description, exclude_description, search_type, exclude_type, limit, ТолькоЦиклы, ТолькоУсловия)
	МодульМсп_Сервер = МодульМсп_Сервер();
	
	Массив_search_name = MCPПодготовитьВариантыТочногоПоиска(search_name);
	Массив_exclude_name = MCPПодготовитьВариантыТочногоПоиска(exclude_name);
	Массив_search_description = MCPПодготовитьВариантыТочногоПоиска(search_description);
	Массив_exclude_description = MCPПодготовитьВариантыТочногоПоиска(exclude_description);
	Массив_search_type = MCPПодготовитьВариантыТочногоПоиска(search_type);
	Массив_exclude_type = MCPПодготовитьВариантыТочногоПоиска(exclude_type);
	
	СтрокаСообщения = Ванесса.Локализовать("Поиск шагов с отбором:
	|search_name=%1
	|exclude_name=%2
	|search_description=%3
	|exclude_description=%4
	|search_type=%5
	|exclude_type=%6
	|limit=%7");
	СтрокаСообщения = Ванесса.ПодставитьПараметрыВСтроку(СтрокаСообщения, search_name, exclude_name, search_description, exclude_description, search_type, exclude_type, limit);
	Ванесса.СообщитьПользователю(СтрокаСообщения);
	Ванесса.ЗаписатьЛогВЖРИнформация("ПоискШаговПоСтрокеMCP", СтрокаСообщения);
	
	Лимит = 50;
	Если limit <> Неопределено Тогда
		Попытка
			ПромЛимит = Число(limit);
			Если ПромЛимит <> 0 Тогда
				Лимит = ПромЛимит;
			КонецЕсли;	
		Исключение
		КонецПопытки;
	КонецЕсли;

	Если Лимит < 0 Тогда
		Возврат МодульМсп_Сервер.ОшибкаИнструмента("INVALID_PARAMS", Ванесса.Локализовать("Параметр limit не может быть отрицательным. Используйте 0, чтобы вернуть все результаты."));
	КонецЕсли;
	
	МассивШагов = Новый Массив;
	МножествоКатегорий = Новый Соответствие;
	Счетчик = 0;
	
	Для Каждого СтрокаТаблицы Из Ванесса.ТаблицаИзвестныхStepDefinition Цикл
		
		Если ТолькоЦиклы Тогда
			Если НРег(СтрокаТаблицы.ТипШагаВДереве) <> "цикл" Тогда
				Продолжить;
			КонецЕсли;	
		КонецЕсли;	
		Если ТолькоУсловия Тогда
			Если НРег(СтрокаТаблицы.ТипШагаВДереве) <> "условие" Тогда
				Продолжить;
			КонецЕсли;	
		КонецЕсли;	
		
		Если НЕ ЗначениеЗаполнено(СтрокаТаблицы.ТипШага) Тогда
			// Значит это служебный шаг
			Продолжить;
		КонецЕсли;	
		Если НЕ ЗначениеЗаполнено(СтрокаТаблицы.ОписаниеШага) Тогда
			// Значит это служебный шаг
			Продолжить;
		КонецЕсли;	
		Если Найти(НРег(СтрокаТаблицы.ТипШага), "устаревшее") Тогда
			// Значит это устаревший шаг
			Продолжить;
		КонецЕсли;	
		
		// Исключения:
		// Имя, описание, тип
		Если Массив_exclude_name.Количество() > 0 И НайденоВхождениеЗначенияВМассиве(Массив_exclude_name, НРег(СтрокаТаблицы.ПредставлениеТеста)) Тогда
			Продолжить;
		КонецЕсли;
		Если Массив_exclude_description.Количество() > 0 И НайденоВхождениеЗначенияВМассиве(Массив_exclude_description, НРег(СтрокаТаблицы.ОписаниеШага)) Тогда
			Продолжить;
		КонецЕсли;
		Если Массив_exclude_type.Количество() > 0 И НайденоВхождениеЗначенияВМассиве(Массив_exclude_type, НРег(СтрокаТаблицы.ТипШага)) Тогда
			Продолжить;
		КонецЕсли;

		НадоПродолжить = Истина;
		
		Если Массив_search_name.Количество() = 0 И Массив_search_description.Количество() = 0 И Массив_search_type.Количество() = 0 Тогда
			// Значит не нужно делать отбор
			НадоПродолжить = Ложь;
		КонецЕсли;	
		
		// Фильтр по вхождению
		// Имя, описание, тип
		Если (Массив_search_name.Количество() > 0 И НайденоВхождениеЗначенияВМассиве(Массив_search_name, НРег(СтрокаТаблицы.ПредставлениеТеста)))
			ИЛИ (Массив_search_description.Количество() > 0 И НайденоВхождениеЗначенияВМассиве(Массив_search_description, НРег(СтрокаТаблицы.ОписаниеШага)))
			ИЛИ Массив_search_type.Количество() > 0 И НайденоВхождениеЗначенияВМассиве(Массив_search_type, НРег(СтрокаТаблицы.ТипШага))
			Тогда
			НадоПродолжить = Ложь;
		КонецЕсли;
		Если НадоПродолжить Тогда
			Продолжить;
		КонецЕсли;	
		
		// Собираем категории
		Если ЗначениеЗаполнено(СтрокаТаблицы.ТипШага) Тогда
			МножествоКатегорий.Вставить(СтрокаТаблицы.ТипШага, Истина);
		КонецЕсли;
		
		// Лимит
		Счетчик = Счетчик + 1;
		Если Лимит > 0 И Счетчик > Лимит Тогда
			Продолжить; // Продолжаем считать категории, но не добавляем шаги
		КонецЕсли;
		
		ШагJSON = Новый Структура;
		ШагJSON.Вставить("name", СтрокаТаблицы.ПредставлениеТеста);
		ШагJSON.Вставить("description", СтрокаТаблицы.ОписаниеШага);
		ШагJSON.Вставить("category", СтрокаТаблицы.ТипШага);
		ШагJSON.Вставить("snippet", СтрокаТаблицы.Id);
		ШагJSON.Вставить("searchString", СтрокаТаблицы.СтрокаДляПоиска);
		ШагJSON.Вставить("sourceFile", СтрокаТаблицы.ИмяФайла);
		ШагJSON.Вставить("type", СтрокаТаблицы.ТипШага);
		
		МассивШагов.Добавить(ШагJSON);
		
	КонецЦикла;
	
	// Собираем список категорий
	МассивКатегорий = Новый Массив;
	Для Каждого КлючЗначение Из МножествоКатегорий Цикл
		МассивКатегорий.Добавить(КлючЗначение.Ключ);
	КонецЦикла;
	
	Результат = Новый Структура;
	Результат.Вставить("totalFound", Счетчик);
	Результат.Вставить("categories", МассивКатегорий);
	Результат.Вставить("steps", МассивШагов);
	Результат.Вставить("search_name", СокрЛП(search_name));
	Результат.Вставить("exclude_name", СокрЛП(exclude_name));
	Результат.Вставить("search_description", СокрЛП(search_description));
	Результат.Вставить("exclude_description", СокрЛП(exclude_description));
	Результат.Вставить("search_type", СокрЛП(search_type));
	Результат.Вставить("exclude_type", СокрЛП(exclude_type));
	Результат.Вставить("limit", Лимит);
	
	Возврат MCPСформироватьMarkdownРезультатаПоискаШагов(Результат);
	
КонецФункции	

&НаКлиенте
Функция MCPПодготовитьВариантыТочногоПоиска(ТекстЗапроса)
	ВариантыЗапроса = Новый Массив;
	Если Не ЗначениеЗаполнено(ТекстЗапроса) Тогда
		Возврат ВариантыЗапроса;
	КонецЕсли;

	Для Каждого ЧастьЗапроса Из Ванесса._СтрРазделить(ТекстЗапроса, "|") Цикл
		НормализованнаяЧасть = НРег(СокрЛП(Строка(ЧастьЗапроса)));
		Если ЗначениеЗаполнено(НормализованнаяЧасть) Тогда
			ВариантыЗапроса.Добавить(НормализованнаяЧасть);
		КонецЕсли;
	КонецЦикла;

	Возврат ВариантыЗапроса;

КонецФункции

&НаКлиенте
Функция НайденоВхождениеЗначенияВМассиве(Массив, Значение)
	
	Для Каждого Стр Из Массив Цикл
		Если Найти(Значение, Стр) > 0 Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции	

&НаКлиенте
Функция MCPСформироватьMarkdownРезультатаПоискаШагов(Данные)
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Найденные шаги Vanessa Automation"));
	Строки.Добавить("");
	Строки.Добавить(Ванесса.Локализовать("- Поисковый запрос по представлению шагов:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["search_name"]));
	Строки.Добавить(Ванесса.Локализовать("- Поисковый запрос по исключению в представлении шагов:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["exclude_name"]));
	Строки.Добавить(Ванесса.Локализовать("- Поисковый запрос по описанию шагов:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["search_description"]));
	Строки.Добавить(Ванесса.Локализовать("- Поисковый запрос по исключению в описании шагов:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["exclude_description"]));
	Строки.Добавить(Ванесса.Локализовать("- Поисковый запрос по типу шагов:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["search_type"]));
	Строки.Добавить(Ванесса.Локализовать("- Поисковый запрос по исключению в типе шагов:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["exclude_type"]));
	Строки.Добавить(Ванесса.Локализовать("- Лимит:") + " " + MCPПредставлениеЛимитаДляMarkdown(Данные["limit"]));
	Строки.Добавить(Ванесса.Локализовать("- Всего найдено:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["totalFound"]));
	Строки.Добавить(Ванесса.Локализовать("- Показано:") + " " + Строка(Данные["steps"].Количество()));
	Строки.Добавить(Ванесса.Локализовать("- Категории:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["categories"]));
	Строки.Добавить(Ванесса.Локализовать("- Примечание: поиск выполняется по полному точному совпадению без учета регистра. Для поиска альтернатив используйте разделитель '|', например 'первая часть|вторая часть'. Семантическая обработка не используется."));
	Строки.Добавить("");

	Если Данные["steps"].Количество() = 0 Тогда
		Строки.Добавить(Ванесса.Локализовать("Совпадения не найдены."));
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("Поиск выполнен с указанными выше параметрами."));
		Строки.Добавить(Ванесса.Локализовать("Если ожидался другой результат, проверьте поисковый запрос, категорию и область поиска."));
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	КонецЕсли;

	НомерШага = 0;
	Для Каждого Шаг Из Данные["steps"] Цикл
		НомерШага = НомерШага + 1;
		Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("## %1. %2"), Строка(НомерШага), MCPПредставлениеЗначенияДляMarkdown(Шаг["name"]))); 
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("- Категория:") + " " + MCPПредставлениеЗначенияДляMarkdown(Шаг["category"]));
		Строки.Добавить(Ванесса.Локализовать("- Описание:") + " " + MCPПредставлениеЗначенияДляMarkdown(Шаг["description"]));
		Строки.Добавить(Ванесса.Локализовать("- Сниппет: `") + MCPПредставлениеЗначенияДляMarkdown(Шаг["snippet"]) + "`");
		Строки.Добавить(Ванесса.Локализовать("- Тип: `") + MCPПредставлениеЗначенияДляMarkdown(Шаг["type"]) + "`");
		//Строки.Добавить(Ванесса.Локализовать("- Строка поиска:") + " " + MCPПредставлениеЗначенияДляMarkdown(Шаг["searchString"]));
		//Строки.Добавить(Ванесса.Локализовать("- Исходный файл:") + " " + MCPПредставлениеЗначенияДляMarkdown(Шаг["sourceFile"]));
		Строки.Добавить("");
	КонецЦикла;

	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);

КонецФункции

&НаКлиенте
Функция MCPПредставлениеОбластиПоискаДляMarkdown(ОбластьПоиска)
	Если ОбластьПоиска = "name" Тогда
		Возврат Ванесса.Локализовать("только название шага");
	КонецЕсли;

	Если ОбластьПоиска = "all" Тогда
		Возврат Ванесса.Локализовать("все свойства шага");
	КонецЕсли;

	Возврат MCPПредставлениеЗначенияДляMarkdown(ОбластьПоиска);

КонецФункции

&НаКлиенте
Функция MCPПредставлениеЛимитаДляMarkdown(Лимит)
	Если Лимит = 0 Тогда
		Возврат Ванесса.Локализовать("без ограничения");
	КонецЕсли;

	Возврат MCPПредставлениеЗначенияДляMarkdown(Лимит);

КонецФункции

&НаКлиенте
Процедура MCPИнструментСинтаксКонтроль(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_filePath = Ванесса.Локализовать("Путь к .feature файлу. Лучше передавать полный путь к файлу.");
    Свойства.Вставить("filePath", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_filePath));
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("filePath");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "check_syntax",
        Ванесса.Локализовать("Проверяет синтаксис Gherkin в активном документе редактора Vanessa Automation или в указанном feature-файле.
		|Возвращает Markdown с найденными ошибками: неизвестные шаги, структурные проблемы, ошибки ключевых слов."),
        Схема,
        Новый ОписаниеОповещения("СинтаксКонтроль", Вычислить("ЭтотОбъект")),
		Истина),
    	ИмяПровайдера());
		
	КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментОткрытьФичаФайл(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_filePath = Ванесса.Локализовать("Путь к .feature файлу. Лучше передавать полный путь к файлу.");
    Свойства.Вставить("filePath", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_filePath));
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("filePath");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "open_feature_file",
        Ванесса.Локализовать("Открывает .feature файл в Vanessa Editor или активирует уже открытую вкладку.
		|Используйте перед check_syntax, execute_feature_step и ручной навигацией по редактору."),
        Схема,
        Новый ОписаниеОповещения("ОткрытьФичаФайл", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
    	ИмяПровайдера());
		
КонецПроцедуры
	
&НаКлиенте
Процедура MCPИнструментОткрытьЗагрузитьИлиПерезагрузитьФичаФайл(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_path = Ванесса.Локализовать("Путь к каталогу или файлу. Если не указан — перезагрузка текущего фича файла или каталога фича файлов.");
    Свойства.Вставить("path", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_path));
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "load_features",
        Ванесса.Локализовать("Загружает или перезагружает .feature файлы.
		|Используйте после того как вы отредактировали .feature файл, чтобы перечитать файл с дика в Vanessa Automation.
		|Это лучший способ перезагрузить текущий файл в редакторе не указывая имя файла."),
        Схема,
        Новый ОписаниеОповещения("ОткрытьЗагрузитьИлиПерезагрузитьФичаФайл", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
    	ИмяПровайдера());
		
КонецПроцедуры

&НаКлиенте
Процедура MCPИнструментПолучитьСостояниеРедактора(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_includeContent = Ванесса.Локализовать("Включить полный текст активного документа. По умолчанию true.");
    Свойства.Вставить("includeContent", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("boolean", Описание_includeContent));
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_editor_state",
        Ванесса.Локализовать("Возвращает состояние Monaco-редактора Vanessa Automation:
		|открытые вкладки, содержимое активного документа, позицию курсора, ошибки.
		|Один вызов для полного понимания контекста редактора.
		|Если feature файл загружен в редактор Vanessa Automation, то уже можно запускать его сценарии на выполнение с помощью run_scenario."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьСостояниеРедактора", Вычислить("ЭтотОбъект")),
		Ложь), //Нет асинхронности
    	ИмяПровайдера());
		
КонецПроцедуры
	
&НаКлиенте
Процедура MCPИнструментВыбратьСценарий(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_name = Ванесса.Локализовать("Имя сценария. Обязательный параметр.");
    Свойства.Вставить("name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_name));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("name");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "select_scenario",
        Ванесса.Локализовать("Делает сценарий текущим по его имени"),
        Схема,
        Новый ОписаниеОповещения("ВыбратьСценарий", Вычислить("ЭтотОбъект")),
		Истина), //Нет асинхронности
    	ИмяПровайдера());
		
КонецПроцедуры
	
&НаКлиенте
Процедура MCPИнструментВыбратьШаг(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_lineNumber = Ванесса.Локализовать("Номер строки фича файла. Обязательный параметр.");
    Свойства.Вставить("lineNumber", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("integer", Описание_lineNumber));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("lineNumber");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
    ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "select_step",
        Ванесса.Локализовать("Делает шаг текущим по номеру строки"),
        Схема,
        Новый ОписаниеОповещения("ВыбратьШаг", Вычислить("ЭтотОбъект")),
		Истина), //Нет асинхронности
    	ИмяПровайдера());
		
КонецПроцедуры
	
&НаКлиенте
Функция MCPНормализоватьПутьКФичаФайлу(Путь)
	
	Если Не ЗначениеЗаполнено(Путь) Тогда
		ВызватьИсключение Ванесса.Локализовать("Не указан путь к .feature файлу.");
	КонецЕсли;
	
	НормализованныйПуть = Ванесса.ПреобразоватьПутьСТочкамиКНормальномуПути(Строка(Путь));
	Если Не ЗначениеЗаполнено(НормализованныйПуть) Тогда
		ВызватьИсключение Ванесса.Локализовать("Не удалось определить путь к .feature файлу.");
	КонецЕсли;
	
	Если Не Ванесса.ФайлСуществуетКомандаСистемы(НормализованныйПуть) Тогда
		ВызватьИсключение Ванесса.Локализовать("Файл не найден:") + " " + НормализованныйПуть;
	КонецЕсли;
	
	Если Ванесса.ЭтоКаталогКомандаСистемы(НормализованныйПуть) Тогда
		ВызватьИсключение Ванесса.Локализовать("Ожидался путь к .feature файлу, а не к каталогу:") + " " + НормализованныйПуть;
	КонецЕсли;
	
	Возврат НормализованныйПуть;
	
КонецФункции

&НаКлиенте
Процедура MCPOpenFeatureFileВРедакторе(ИмяФайла, ТипВызова, ДопПараметрыВызоваMCP = Неопределено)
	
	МодульМсп_Сервер = МодульМсп_Сервер();
	Если Не Ванесса.Объект.ИспользоватьРедакторVanessaEditor Тогда
		МодульМсп_Сервер.ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", Ванесса.Локализовать("Vanessa Editor отключен в настройках Vanessa Automation."));
		Возврат;
	КонецЕсли;
	
	Если Ванесса.VanessaTabs = Неопределено Тогда
		МодульМсп_Сервер.ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", Ванесса.Локализовать("Редактор Vanessa Editor не инициализирован."));
		Возврат;
	КонецЕсли;
	
	Если Ванесса.МодульРедакторТекста().СуществуетВкладкаСФайлом(ИмяФайла) Тогда
		Ванесса.МодульРедакторТекста().АктивизироватьВкладкуПоИмениФайла(ИмяФайла);
	КонецЕсли;
	
	Ванесса.Объект.КаталогФич = ИмяФайла;
	Ванесса.УстановитьФлагОповеститьMCPСерверПослеЗагрузкиФичи(ТипВызова);
	Если ДопПараметрыВызоваMCP <> Неопределено Тогда
		ДопПараметры = Новый Структура;
		ДопПараметры.Вставить("ДопПараметрыВызоваMCP", ДопПараметрыВызоваMCP);
	КонецЕсли;	
	Ванесса.ЗагрузитьФичи(ДопПараметры);
	
	// Контекст вызова вернётся в этот модуль после загрузки фичи в метод ОповеститьMCP_ОЗавершенииЗагрузкиФичи
	
КонецПроцедуры

&НаКлиенте
Функция MCPСформироватьMarkdownПроверкиСинтаксиса(Данные)
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Проверка синтаксиса Gherkin"));
	Строки.Добавить("");
	Если Данные["filePath"] <> Неопределено Тогда
		Строки.Добавить(Ванесса.Локализовать("- Файл:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["filePath"]));
	КонецЕсли;
	Строки.Добавить(Ванесса.Локализовать("- Синтаксис корректен:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["valid"]));
	Строки.Добавить(Ванесса.Локализовать("- Количество проблем:") + " " + MCPПредставлениеЗначенияДляMarkdown(Данные["problemCount"]));
	Строки.Добавить("");

	Если Данные["problemCount"] = 0 Тогда
		Строки.Добавить(Ванесса.Локализовать("Проблем не найдено."));
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	КонецЕсли;

	Строки.Добавить(Ванесса.Локализовать("## Найденные проблемы"));
	Строки.Добавить("");

	НомерПроблемы = 0;
	Для Каждого Проблема Из Данные["problems"] Цикл
		НомерПроблемы = НомерПроблемы + 1;
		СтрокаПроблемы = Строка(НомерПроблемы) + ". ";
		Если Проблема["line"] <> Неопределено Тогда
			СтрокаПроблемы = СтрокаПроблемы + Ванесса.Локализовать("Строка") + " " + Строка(Проблема["line"]) + ": ";
		КонецЕсли;
		СтрокаПроблемы = СтрокаПроблемы + MCPПредставлениеЗначенияДляMarkdown(Проблема["message"]);
		Строки.Добавить(СтрокаПроблемы);
	КонецЦикла;

	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);

КонецФункции

&НаКлиенте
Функция MCPТекущийФайлРедактора()
	
	Если Ванесса.VanessaTabs <> Неопределено И Ванесса.VanessaTabs.current <> Неопределено Тогда
		Возврат Ванесса.VanessaTabs.current.filename;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

&НаКлиенте
Процедура ЗавершитьПроверкуСинтаксиса(ПутьКФайлу)
	
	Проблемы = Ванесса.ПолучитьПроблемыПоТекущейФиче();
	
	// Читаем найденные проблемы
	МассивПроблем = ПреобразоватьПроблемыПроверкиСинтаксиса(Проблемы);
	
	Результат = Новый Структура;
	Результат.Вставить("valid", МассивПроблем.Количество() = 0);
	Результат.Вставить("problemCount", МассивПроблем.Количество());
	Результат.Вставить("problems", МассивПроблем);
	Результат.Вставить("filePath", ПутьКФайлу);
	
	СтрокаРезультат = MCPСформироватьMarkdownПроверкиСинтаксиса(Результат);
	
	Результат = Новый Структура(
		"message,completedAt",
		СтрокаРезультат,
		Строка(ТекущаяДата()));
		
	МодульМсп_Сервер = МодульМсп_Сервер();
	МодульМсп_Сервер.ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), Результат);
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьОткрытиеФичаФайла(ПутьКФайлу)
	
	ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(
		Ванесса.Локализовать("# Файл открыт в редакторе
							|
							|- Открыт: да
							|- Путь: %1
							|
							|## Следующие шаги
							|- Проверьте синтаксис: `check_syntax`
							|- Получите состояние редактора: `get_editor_state`
							|- Перезагрузите дерево, если меняли файл на диске: `load_features`"),
		ПутьКФайлу);
		
	МодульМсп_Сервер = МодульМсп_Сервер();
	МодульМсп_Сервер.ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), ТекстОтвета);
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьОткрытьЗагрузитьИлиПерезагрузитьФичаФайл(ПутьКФайлу, ДопПараметрыВызоваMCP)
	
	Путь = ДопПараметрыВызоваMCP.ИсходныйПутьКФайлу;
	
	Если ЗначениеЗаполнено(Путь) Тогда
		ПутьОтвета = Путь;
	Иначе
		ПутьОтвета = Объект.КаталогФич + " " + Ванесса.Локализовать("(текущий)");
	КонецЕсли;
	ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
		"# Загрузка фич
		|
		|- Загружено: да
		|- Путь: %1
		|
		|## Следующие шаги
		|- Подключите клиента тестирования: `connect_test_client`
		|- Найдите нужные шаги: `search_for_steps_by_keywords`
		|- Запустите сценарий: `run_scenario`"),
		ПутьОтвета,
		Формат(Ванесса.ТаблицаИзвестныхStepDefinition.Количество(), "ЧГ=0; ЧН=0"));
		
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), ТекстОтвета);
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьВыполнениеШагаВРедакторе(ПутьКФайлу, ДопПараметрыВызоваMCP)
	
	// Проверяем что файл действительно открыт
	ТекущийФайл = MCPТекущийФайлРедактора();
	Если НРег(ТекущийФайл) <> НРег(ПутьКФайлу) Тогда
		ТекстСообщения = Ванесса.Локализовать("Не удалось активировать вкладку файла:") + " " + ПутьКФайлу;
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	ЭкземплярVanessaEditor = Ванесса.ПолучитьVanessaEditor();
	ПолныйТекст = ЭкземплярVanessaEditor.getContent();
	
	НомерСтроки = ДопПараметрыВызоваMCP.НомерСтроки;
	КоличествоСтрок = Ванесса._СтрРазделить(ПолныйТекст, Символы.ПС).Количество();
	Если НомерСтроки > КоличествоСтрок Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Строка %1 выходит за пределы файла (%2 строк): %3"),
				НомерСтроки, КоличествоСтрок, ПутьКФайлу);
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	
	СтрокаДерева = Ванесса.СтрокаДереваПоНомеруСтрокиФичи(НомерСтроки);
	Если СтрокаДерева = Неопределено Тогда
		ТекстСтроки = ЭкземплярVanessaEditor.getLineContent(НомерСтроки);
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("По строке %1 не получилось найти исполнимую строку фича файла.
		|Текст в строке: %2
		|Имя файла: %3"),
				НомерСтроки, ТекстСтроки, ПутьКФайлу);
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;	
	Ванесса.МодульРедакторТекста().АктивизироватьСтрокуРедактораПоИдСтрокиИНомеруСтрокиФичи(СтрокаДерева.ИдСтроки, НомерСтроки);
	
	// Запускаем обработчик ожидания, чтобы редактор смог обновиться и перейти к нужной строке
	глДопПараметрыВызоваMCP = ДопПараметрыВызоваMCP;
	ПодключитьОбработчикОжидания("ПослеСделатьПереходкКТекстуВРедакторе", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьВыполнитьСценарий1(ПутьКФайлу, ДопПараметрыВызоваMCP)
	
	// Обеспечить наличие активной вкладки .feature в VanessaEditor
	// (без неё КомандаВыполнитьСценарии/ВыполнитьВыделенныйСценарий молча завершаются)
	Если Ванесса.Объект.ИспользоватьРедакторVanessaEditor Тогда
		МСПНадоОткрытьФайл = Ложь;
		Если Ванесса.VanessaTabs = Неопределено ИЛИ Ванесса.VanessaTabs.current = Неопределено Тогда
			МСПНадоОткрытьФайл = Истина;
		ИначеЕсли НЕ Ванесса.ЭтоВкладкаEdit(Ванесса.VanessaTabs.current) Тогда
			МСПНадоОткрытьФайл = Истина;
		ИначеЕсли (НРег(Ванесса.Объект.КаталогФич) <> НРег(ПутьКФайлу)) И ЗначениеЗаполнено(ПутьКФайлу) Тогда
			МСПНадоОткрытьФайл = Истина;
		КонецЕсли;
		
		Если МСПНадоОткрытьФайл Тогда
			Ванесса.Объект.КаталогФич = ПутьКФайлу;
			ФайлВРедакторе = Ванесса.Объект.КаталогФич;
			Если ЗначениеЗаполнено(ФайлВРедакторе) И Прав(НРег(СокрЛП(ФайлВРедакторе)), 8) = ".feature" Тогда
				Попытка
					MCPOpenFeatureFileВРедакторе(ФайлВРедакторе, "ЗагрузитьФичуДляВыполненияСценария")
				Исключение
					ТекстСообщения = Ванесса.Локализовать("Не удалось открыть файл в редакторе:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
					МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
					Возврат;
				КонецПопытки;
			Иначе
				ТекстСообщения = Ванесса.Локализовать("Нет активного .feature файла в редакторе. Используйте параметр filePath, или сначала загрузите фичу через load_features(path='путь/к/файлу.feature') и openFeatureFile.");
				МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
				Возврат;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	ПродолжитьВыполнитьСценарий2(ПутьКФайлу, ДопПараметрыВызоваMCP);
	
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьВыполнитьСценарий2(ПутьКФайлу, ДопПараметрыВызоваMCP)
	
	MCPОжиданиеВыполненияСценария = Истина;
	Режим = ДопПараметрыВызоваMCP.Режим;
	
	Попытка
		
		Идентификатор = ДопПараметрыВызоваMCP.Идентификатор;
		Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
		Ванесса.УстановитьФлагОповеститьMCPСерверПослеВыполненияСценариев(Режим, ДопПараметрыВызоваMCP);
		// Вызов вернётся в ОповеститьMCP_ОЗавершенииВыполненияСценариев
		
		Если Режим = "selected" Тогда
			ЭкземплярVanessaEditor = Ванесса.ПолучитьVanessaEditor();
			ПозицияVanessaEditor = ЭкземплярVanessaEditor.getPosition();
			ТекНомерСтроки = ПозицияVanessaEditor.lineNumber;
			
			СтрокаДерева = Ванесса.СтрокаДереваПоНомеруСтрокиФичи(ТекНомерСтроки);
			Если СтрокаДерева = Неопределено Тогда
				ТекстСообщения = Ванесса.Локализовать("Не найдена строка внутреннего дерева по номеру строки фичи:") + ТекНомерСтроки;
				МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
				Возврат;
			КонецЕсли;	
			
			Ванесса.ВыполнитьВыделенныйСценарий();
		ИначеЕсли Режим = "all" Тогда
			Проблемы = Ванесса.ПолучитьПроблемыПоТекущейФиче();
			Если Проблемы.Количество() > 0 Тогда
				МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ОшибкаПроверкиСинтаксиса(Проблемы));
				Возврат;
			КонецЕсли;
			
			Ванесса.КомандаВыполнитьСценарии();
		ИначеЕсли Режим = "reloadAndRun" Тогда
			//Ванесса.ПерезагрузитьИВыполнитьДанныйСценарий();
			Если НЕ ЗначениеЗаполнено(ПутьКФайлу) Тогда
				ТекстОшибки = Неопределено;
				ПутьКФайлу = ПутьКФайлуИзТекущегоФайлаВРедакторе(ТекстОшибки);
				Если ЗначениеЗаполнено(ТекстОшибки) Тогда
					МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстОшибки);
					Возврат;
				КонецЕсли;
			КонецЕсли;	
			
			ДопПараметрыВызоваMCP = Новый Структура;
			ДопПараметрыВызоваMCP.Вставить("ИсходныйПутьКФайлу", ПутьКФайлу);
			ДопПараметрыВызоваMCP.Вставить("ЗапуститьВсеСценарииПослеЗагрузки", Истина);
			MCPOpenFeatureFileВРедакторе(ПутьКФайлу, "ЗагрузитьФичаФайлИВыполнитьСценарии", ДопПараметрыВызоваMCP);
			
		ИначеЕсли Режим = "fromCurrentStep" Тогда
			Проблемы = Ванесса.ПолучитьПроблемыПоТекущейФиче();
			Если Проблемы.Количество() > 0 Тогда
				МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ОшибкаПроверкиСинтаксиса(Проблемы));
				Возврат;
			КонецЕсли;
			
			Ванесса.ВыполнитьДанныйСценарийСТекущегоШага();
		ИначеЕсли Режим = "reloadAndRunFromLine" Тогда
			// Получаем номер строки из параметров
			НомерСтроки = ДопПараметрыВызоваMCP.НомерСтроки;
			Если НЕ ЗначениеЗаполнено(НомерСтроки) Тогда
				ТекстСообщения = Ванесса.Локализовать("Для режима reloadAndRunFromLine необходимо указать параметр lineNumber.");
				МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
				Возврат;
			КонецЕсли;	
			
			// Сначала загружаем фича файл
			Если НЕ ЗначениеЗаполнено(ПутьКФайлу) Тогда
				ТекстОшибки = Неопределено;
				ПутьКФайлу = ПутьКФайлуИзТекущегоФайлаВРедакторе(ТекстОшибки);
				Если ЗначениеЗаполнено(ТекстОшибки) Тогда
					МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстОшибки);
					Возврат;
				КонецЕсли;
			КонецЕсли;	
			
			// Сохраняем номер строки в доп параметрах для последующего использования
			ДопПараметрыЗагрузки = Новый Структура;
			ДопПараметрыЗагрузки.Вставить("ИсходныйПутьКФайлу", ПутьКФайлу);
			ДопПараметрыЗагрузки.Вставить("НомерСтроки", НомерСтроки);
			ДопПараметрыЗагрузки.Вставить("РежимПослеЗагрузки", "reloadAndRunFromLine");
			MCPOpenFeatureFileВРедакторе(ПутьКФайлу, "ЗагрузитьФичуДляВыполненияСценария", ДопПараметрыЗагрузки);
			Возврат;
		Иначе
			MCPОжиданиеВыполненияСценария = Ложь;
			ТекстСообщения = Ванесса.Локализовать("Неизвестный режим:") + " " + Режим + Ванесса.Локализовать(". Допустимые: selected, all, reloadAndRun, fromCurrentStep, reloadAndRunFromLine.");
			МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
			Возврат;
		КонецЕсли;
		
	Исключение
		MCPОжиданиеВыполненияСценария = Ложь;
		ТекстСообщения = Ванесса.Локализовать("Ошибка запуска:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецПопытки;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьВыполненияСценария(Тип, ДопПараметрыВызоваMCP)
	
	МассивРезультатПрохожденияТестовСценария = Ванесса.ПолучитьМассивРезультатПрохожденияТестовСценария();
	БылиОшибки = Ложь;
	Для Каждого ДанныеСценария Из МассивРезультатПрохожденияТестовСценария Цикл
		Если ДанныеСценария.Свойство("ОписаниеОшибки") И ЗначениеЗаполнено(ДанныеСценария.ОписаниеОшибки) Тогда
			БылиОшибки = Истина;
			Прервать;
		КонецЕсли;	
	КонецЦикла;	
	
	МассивСтрок = Новый Массив;
	Если БылиОшибки Тогда
		ТекстСтроки = Ванесса.Локализовать("## Результат выполнения сценариев: были ошибки");
	Иначе	
		ТекстСтроки = Ванесса.Локализовать("## Результат выполнения сценариев: все сценарии выполнены успешно");
	КонецЕсли;	
	МассивСтрок.Добавить(ТекстСтроки);
	МассивСтрок.Добавить("");
	
	НадоСнятьСкриншот = Ложь;
	
	Для Каждого ДанныеСценария Из МассивРезультатПрохожденияТестовСценария Цикл
		СтрокаСценария = Ванесса.СтрокаДереваПоИдСтроки(ДанныеСценария.ИДСтроки);
		
		ТекстСтроки = Ванесса.Локализовать("Имя сценария:" + " " + СтрокаСценария.Имя);
		МассивСтрок.Добавить(ТекстСтроки);
		Если ДанныеСценария.Свойство("Статус") Тогда
			ТекстСтроки = Ванесса.Локализовать("Результат прохождения сценария:") + " " + ДанныеСценария.Статус;
		КонецЕсли;	
		МассивСтрок.Добавить(ТекстСтроки);
		
		НомерСтрокиВФиче = Неопределено;
		
		Если ДанныеСценария.Свойство("ОписаниеОшибки") И ЗначениеЗаполнено(ДанныеСценария.ОписаниеОшибки) Тогда
			БылиОшибки = Истина;
			
			Если ДанныеСценария.Свойство("ДанныеДляФормированияJsonОбОшибке") Тогда
				СтрокаУпавшегоШага = Ванесса.СтрокаДереваПоИдСтроки(ДанныеСценария.ДанныеДляФормированияJsonОбОшибке.ИДСтрокиУпавшегоШага);
				ТекстСтроки = Ванесса.Локализовать("Текст упавшего шага:") + " " + СтрокаУпавшегоШага.Имя;
				МассивСтрок.Добавить(ТекстСтроки);
				ТекстСтроки = Ванесса.Локализовать("Статус упавшего шага:") + " " + СтрокаУпавшегоШага.Статус;
				МассивСтрок.Добавить(ТекстСтроки);
				ТекстСтроки = Ванесса.Локализовать("Номер строки упавшего шага в feature файле:") + " " + XMLСтрока(СтрокаУпавшегоШага.НомерСтрокиВФиче);
				МассивСтрок.Добавить(ТекстСтроки);
				ТекстСтроки = Ванесса.Локализовать("Текст ошибки:") + " " + ДанныеСценария.ДанныеДляФормированияJsonОбОшибке.ОписаниеОшибкиКратко;
				МассивСтрок.Добавить(ТекстСтроки);
				
				НомерСтрокиВФиче = СтрокаУпавшегоШага.НомерСтрокиВФиче;
			Иначе
				ТекстСтроки = Ванесса.Локализовать("Текст ошибки:") + " " + ДанныеСценария.ОписаниеОшибки;
				МассивСтрок.Добавить(ТекстСтроки);
			КонецЕсли;	
			
			ДобавитьВМассивСтрокДанныеОбАктивномОкне(МассивСтрок);
			
			МассивСтрок.Добавить("");
			МассивСтрок.Добавить("");
			
			Если НомерСтрокиВФиче <> Неопределено Тогда
				ОбновитьКешДанныхУпавшегоШагаИСказатьИИСнятьСкриншот(НомерСтрокиВФиче, МассивСтрок, НадоСнятьСкриншот);
			КонецЕсли;
			
			Если ДляШагаНадоВсегдаСниматьСкриншот(СтрокаУпавшегоШага) Тогда
				НадоСнятьСкриншот = Истина;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если НадоСнятьСкриншот Тогда
		СнятьСкришотыОконКлиентаТестированияИОтправитьИхИИДляАнализаПослеВыполненияСценария(МассивСтрок);
	КонецЕсли;
	
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), Ванесса._СтрСоединить(МассивСтрок, Символы.ПС));
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
	Ванесса.УстановитьФлагОповеститьMCPСерверПослеВыполненияСценариев(Неопределено, Неопределено);
КонецПроцедуры

&НаКлиенте
Процедура ПослеСделатьПереходкКТекстуВРедакторе()
	НомерСтроки = глДопПараметрыВызоваMCP.НомерСтроки;
	ПутьКФайлу = глДопПараметрыВызоваMCP.ПутьКФайлу;
	
	ЭкземплярVanessaEditor = Ванесса.ПолучитьVanessaEditor();
	ТекстСтроки = ЭкземплярVanessaEditor.getLineContent(НомерСтроки);
	
	Если НЕ ЗначениеЗаполнено(ТекстСтроки) Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Строка %1 пуста в файле: %2"),
				НомерСтроки, ПутьКФайлу);
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	Ванесса.УстановитьФлагОповеститьMCPСерверПослеВыполненияОднойСтрокиФичи(глДопПараметрыВызоваMCP.Тип);
	Ванесса.ВыполнитьОдинШаг();
	
	// Вызов вернётся в ОповеститьMCP_ОЗавершенииВыполненияОднойСтрокиФичи
	
	
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьУспешноеВыполнениеШагаВРедакторе(ДопПараметрыВызоваMCP)
	ТекстСообщения = Ванесса.Локализовать("Шаг в строке фича файла успешно выполнен");
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), ТекстСообщения);
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьВыполнениеШагаВРедактореСОшибкой(ДопПараметрыВызоваMCP, РезультатПрохожденияШага)
	ТекстСообщения = ТекстОшибкиИзОписанияОшибки(РезультатПрохожденияШага.ОписаниеОшибки);
	МассивСтрок = Новый Массив;
	МассивСтрок.Добавить(ТекстСообщения);
	ДобавитьВМассивСтрокДанныеОбАктивномОкне(МассивСтрок);
	
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), Ванесса._СтрСоединить(МассивСтрок, Символы.ПС));
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
КонецПроцедуры

&НаКлиенте
Процедура ОткрытиеФичаФайлаВРедакторе(ПутьКФайлу, Запрос, ТипВызова, ДопПараметрыВызоваMCP = Неопределено)
	
	Если ЗначениеЗаполнено(ПутьКФайлу) Тогда
		Если НЕ Ванесса.ФайлСуществуетКомандаСистемы(ПутьКФайлу) Тогда
			
			ПолныйПутьКФайлуВКаталогеПроекта = Ванесса.ДополнитьСлешВПуть(Ванесса.Объект.КаталогПроекта) + ПутьКФайлу;
			Если НЕ Ванесса.ФайлСуществуетКомандаСистемы(ПолныйПутьКФайлуВКаталогеПроекта) Тогда
				МодульМсп_Сервер = МодульМсп_Сервер();
				ТекстОшибки = Ванесса.Локализовать("Файл <%1> не был найден.");
				Если НЕ Ванесса.ЭтоLinux И Найти(ПутьКФайлу, ":") = 0 Тогда
					ТекстОшибки = Ванесса.Локализовать("Файл <%1> не был найден. В каталоге проекта файл <%2> тоже не был найден. Видимо был передан относительный путь, попробуйте передать полный путь к файлу.");
				КонецЕсли;
				
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(ТекстОшибки, ПутьКФайлу, ПолныйПутьКФайлуВКаталогеПроекта);
				МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Запрос.Идентификатор, "INVALID_PARAMS", ТекстОшибки);
				Возврат;
			Иначе
				ПутьКФайлу = ПолныйПутьКФайлуВКаталогеПроекта;
			КонецЕсли;	
			
		КонецЕсли;	
		ПутьКФайлу = MCPНормализоватьПутьКФичаФайлу(ПутьКФайлу);
		
		МодульМсп_Сервер = МодульМсп_Сервер();
		Идентификатор = Запрос.Идентификатор;
		Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
		МодульМсп_Сервер.УведомитьОПрогрессе(Идентификатор, 0, 3, Ванесса.Локализовать("Загрузка feature файла."));
		
		MCPOpenFeatureFileВРедакторе(ПутьКФайлу, ТипВызова, ДопПараметрыВызоваMCP);
	Иначе
		ТекстОшибки = Ванесса.Локализовать("Не передан путь к файлу.");
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Запрос.Идентификатор, "INVALID_PARAMS", ТекстОшибки);
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ТекстОшибкиИзОписанияОшибки(Знач Стр)
	СтрКраткая = Ванесса.УбратьТехническиеДанныеИзСтроки(Стр);
	Если ЗначениеЗаполнено(СтрКраткая) Тогда
		Возврат СтрКраткая;
	КонецЕсли;
	Возврат Стр;
КонецФункции	

&НаКлиенте
Процедура MCPОткрытьИсследовательФормыДляАнализа(ИдентификаторЗапроса, ФорматВывода)
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Язык", Ванесса.Объект.ЯзыкГенератораGherkin);
	ПараметрыФормы.Вставить("ТекущийTestclientЭтоWebКлиент", Ванесса.ТекущийTestclientЭтоWebКлиент);
	ПараметрыФормы.Вставить("ВладелецУникальныйИдентификатор", УникальныйИдентификатор);
	ПараметрыФормы.Вставить("MCPРежимЧтенияФормы", Истина);
	ПараметрыФормы.Вставить("MCPФорматЧтенияФормы", ФорматВывода);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("MCPИдентификаторЗапроса", ИдентификаторЗапроса);
	ДополнительныеПараметры.Вставить("MCPФорматВывода", ФорматВывода);
	
	Ванесса.УстановитьИдентификаторЗадачиMCP(ИдентификаторЗапроса);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("MCPПолученАнализФормы", ЭтаФорма, ДополнительныеПараметры);
	ОткрытьФорму(Ванесса.ПолучитьИмяОбработкиVA() + ".Форма.ИсследовательФормы", ПараметрыФормы, Ванесса, , , , ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура MCPПолученАнализФормы(РезультатЗакрытия, ДополнительныеПараметры) Экспорт
	
	ИдентификаторЗапроса = ДополнительныеПараметры.MCPИдентификаторЗапроса;
	ФорматВывода = ДополнительныеПараметры.MCPФорматВывода;
	
	ДеревоЭлементов = "";
	ШагиТекущегоЭлемента = "";
	
	Если ТипЗнч(РезультатЗакрытия) = Тип("Структура") Тогда
		Если РезультатЗакрытия.Свойство("treeText") Тогда
			ДеревоЭлементов = MCPПредставлениеЗначенияДляMarkdown(РезультатЗакрытия.treeText);
		КонецЕсли;
		Если РезультатЗакрытия.Свойство("stepText") Тогда
			ШагиТекущегоЭлемента = MCPПредставлениеЗначенияДляMarkdown(РезультатЗакрытия.stepText);
		КонецЕсли;
	ИначеЕсли РезультатЗакрытия <> Неопределено Тогда
		ТекстСообщения = Ванесса.Локализовать("Ошибка чтения результата анализа формы: неожиданный тип результата") + " " + Строка(ТипЗнч(РезультатЗакрытия));
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Анализ текущей формы"));
	Строки.Добавить("");
	
	Если ФорматВывода = "full" Тогда
		Попытка
			Ванесса.ПолучитьИзмененияФормыGherkin();
			Gherkin = Ванесса.Объект.СгенерированныйСценарий;
			Если ЗначениеЗаполнено(Gherkin) Тогда
				Строки.Добавить(Ванесса.Локализовать("## Gherkin-состояние формы"));
				Строки.Добавить("```gherkin");
				Строки.Добавить(Gherkin);
				Строки.Добавить("```");
				Строки.Добавить("");
			Иначе
				Строки.Добавить(Ванесса.Локализовать("## Gherkin-состояние формы"));
				Строки.Добавить(Ванесса.Локализовать("Нет данных."));
				Строки.Добавить("");
			КонецЕсли;
		Исключение
			Строки.Добавить(Ванесса.Локализовать("## Gherkin-состояние формы"));
			Строки.Добавить(Ванесса.Локализовать("ошибка:") + " " + ОписаниеОшибки());
			Строки.Добавить("");
		КонецПопытки;
	КонецЕсли;
	
	Строки.Добавить(Ванесса.Локализовать("## Элементы формы"));
	Если ЗначениеЗаполнено(ДеревоЭлементов) Тогда
		Строки.Добавить("```text");
		Строки.Добавить(ДеревоЭлементов);
		Строки.Добавить("```");
	Иначе
		Строки.Добавить(Ванесса.Локализовать("Нет данных."));
	КонецЕсли;
	
	Если ФорматВывода = "full" И ЗначениеЗаполнено(ШагиТекущегоЭлемента) Тогда
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("## Часто используемые шаги для текущего элемента"));
		Строки.Добавить("```gherkin");
		Строки.Добавить(ШагиТекущегоЭлемента);
		Строки.Добавить("```");
	КонецЕсли;
	
	Если ФорматВывода = "full" Тогда
		Попытка
			АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
		Исключение
		КонецПопытки;
		
		Если АктивноеОкно <> Неопределено Тогда
			CообщенияПользователю = ПолучитьСообщенияПользователюИзОкна(АктивноеОкно);
			Если CообщенияПользователю = Неопределено Тогда
				Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В окне <%1> нет сообщений пользователю."), АктивноеОкно.Заголовок));
			Иначе	
				Строки.Добавить(Ванесса.Локализовать("Обнаружены сообщения пользователю:"));
				Для Каждого Стр Из CообщенияПользователю Цикл
					Строки.Добавить(Стр);
				КонецЦикла;	
			КонецЕсли;	
		КонецЕсли;	
	КонецЕсли;	
	
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), Ванесса._СтрСоединить(Строки, Символы.ПС));
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
	
КонецПроцедуры

&НаКлиенте
Функция MCPСписокДоступныхПрофилей()
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("## Доступные профили"));
	
	Если Ванесса.ДанныеКлиентовТестирования.Количество() = 0 Тогда
		Строки.Добавить(Ванесса.Локализовать("Профили не настроены. Добавьте профиль на вкладке «Клиенты тестирования» в настройках VA."));
	Иначе
		Для Каждого СтрокаПрофиля Из Ванесса.ДанныеКлиентовТестирования Цикл
			Статус = "";
			Если СтрокаПрофиля.Подключен Тогда
				Статус = " " + Ванесса.Локализовать("✅ подключен");
			КонецЕсли;
			Строки.Добавить("- `" + СтрокаПрофиля.Имя + "`" + Статус);
			Строки.Добавить("  " + Ванесса.Локализовать("Синоним:") + " " + СтрокаПрофиля.Синоним);
			Строки.Добавить("  " + Ванесса.Локализовать("Строка подключения к базе:") + " " + СтрокаПрофиля.ПутьКИнфобазе);
			Строки.Добавить("  " + Ванесса.Локализовать("Дополнительные параметры подключения:") + " " + СтрокаПрофиля.ДопПараметры);
			Строки.Добавить("  " + Ванесса.Локализовать("Тип клиента:") + " " + СтрокаПрофиля.ТипКлиента);
			Строки.Добавить("  " + Ванесса.Локализовать("Имя компьютера:") + " " + СтрокаПрофиля.ИмяКомпьютера);
			Строки.Добавить("  " + Ванесса.Локализовать("Дескриптор окна:") + " " + СтрокаПрофиля.ДескрипторОкна);
			Строки.Добавить("  " + Ванесса.Локализовать("PID:") + " " + СтрокаПрофиля.PIDКлиентаТестирования);
		КонецЦикла;
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

&НаКлиенте
Функция ПолучитьОкнаДляСкриншотов(РезультатВызова)
	
	ДанныеОкон = Ванесса.ПрочитатьСтрокуJSON(РезультатВызова);
	ОкнаДляСкриншотов = Новый Массив;
	Если ТипЗнч(ДанныеОкон) = Тип("Массив") Тогда
		Для каждого ДанныеОкна из ДанныеОкон Цикл
			Если НЕ ДанныеОкна.Свойство("Title") Тогда
				Продолжить;
			КонецЕсли;
			
			Если Ванесса.ЭтоОкноНеДляСнятияСкриншотов(ДанныеОкна) Тогда
				Ванесса.ЗаписатьЛогВЖРИнформация("СнятиеСкриншота.ОкноПропущено.Class", Ванесса.ЗаписатьОбъектJSON(ДанныеОкна));
				Продолжить;
			КонецЕсли;
			
			Если ДанныеОкна.Свойство("Types") И Ванесса.ОкноДанногоТипаНеДляСнятияСкриншотов(ДанныеОкна.Types) Тогда
				Ванесса.ЗаписатьЛогВЖРИнформация("СнятиеСкриншота.ОкноПропущено.Types", Ванесса.ЗаписатьОбъектJSON(ДанныеОкна));
				Продолжить;
			КонецЕсли;	
			
			ДанныеОкнаОтвет = Новый Структура;
			ДанныеОкнаОтвет.Вставить("ПолныеДанныеОкна", ДанныеОкна);
			ОкнаДляСкриншотов.Добавить(ДанныеОкнаОтвет);
		КонецЦикла;
	КонецЕсли;
	
	Возврат ОкнаДляСкриншотов;
	
КонецФункции

// Отправка результата инструмента с текстом и изображением
&НаКлиенте
Процедура MCPОтправитьРезультатИнструментаСИзображением(Текст, ДвоичныеДанныеИзображения, ДополнительныеПараметры, MimeТип = "image/png")
	
	//Если MCPКомпонента = Неопределено Тогда
	//	Возврат;
	//КонецЕсли;
	
	Содержимое = Новый Массив;
	
	// Текстовая часть
	Если ЗначениеЗаполнено(Текст) Тогда
		ЭлементТекст = Новый Структура;
		ЭлементТекст.Вставить("type", "text");
		ЭлементТекст.Вставить("text", Текст);
		Содержимое.Добавить(ЭлементТекст);
	КонецЕсли;
	
	// Изображение в base64
	Если ДвоичныеДанныеИзображения <> Неопределено Тогда
		ИмяФайла = ПолучитьФайлКартинкиИзДвоичныхДанных(ДвоичныеДанныеИзображения, ДополнительныеПараметры);
		
		ТекстСообщения = Ванесса.Локализовать("Скриншот окна записан в файл:") + " " + ИмяФайла;
		МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), ТекстСообщения);
		Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
		Возврат;
	Иначе	
		ТекстСообщения = Ванесса.Локализовать("Не вышло получить скриншот окна.");
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	//ТелоРезультата = Новый Структура;
	//ТелоРезультата.Вставить("content", Содержимое);
	//
	//Ответ = Новый Структура;
	//Ответ.Вставить("id", Ванесса.ПолучитьИдентификаторЗадачиMCP());
	//
	//ЗаписьJSON = Новый ЗаписьJSON;
	//ЗаписьJSON.УстановитьСтроку();
	//ЗаписатьJSON(ЗаписьJSON, ТелоРезультата);
	//Ответ.Вставить("body", ЗаписьJSON.Закрыть());
	//
	//ЗаписьJSON = Новый ЗаписьJSON;
	//ЗаписьJSON.УстановитьСтроку();
	//ЗаписатьJSON(ЗаписьJSON, Ответ);
	//
	//Результат = ЗаписьJSON.Закрыть();
	
	
	//MCPКомпонента.НачатьВызовОтправитьОтвет(
	//	Новый ОписаниеОповещения("MCPПустойОбработчик", ЭтаФорма),
	//	ЗаписьJSON.Закрыть());
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОбработаннаяКартинкаСервер(Картинка)
	
	ОбрабатываемаяКартинка = Новый ОбрабатываемаяКартинка(Картинка);
	ОбрабатываемаяКартинка.УстановитьФормат(ФорматКартинки.PNG);
	ОбрабатываемаяКартинка.УстановитьГлубинуЦвета(ГлубинаЦвета.БитНаПиксел8);
	ОбрабатываемаяКартинка.ПреобразоватьВОттенкиСерого();
	
	Возврат ОбрабатываемаяКартинка.ПолучитьКартинку();
КонецФункции

&НаКлиенте
Функция ЗначениеИзСтруктуры(Структура, Ключ)
	Если Структура.Свойство(Ключ) Тогда
		Возврат Структура[Ключ];
	КонецЕсли;
	
	Возврат Неопределено;
КонецФункции	

&НаКлиенте
Процедура ЗавершитьВыборСценария()
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), Ванесса.Локализовать("Сценарий сделан текущим. Теперь при выполнении текущего сценария будет выполнен этот сценарий."));
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьВыборШага()
	МодульМсп_Сервер().ЗавершитьВызовИнструментаУспешно(Ванесса.ПолучитьИдентификаторЗадачиMCP(), Ванесса.Локализовать("Шаг сделан текущим. Теперь можно запустить сценарий с текущего шага."));
	Ванесса.УстановитьИдентификаторЗадачиMCP(Неопределено);
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьВыборШагаДляВыполненияСценарияСТекущегоШага()
	Проблемы = Ванесса.ПолучитьПроблемыПоТекущейФиче();
	Если Проблемы.Количество() > 0 Тогда
		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Ванесса.ИдентификаторЗадачиMCP(), "ACTION_FAILED", ОшибкаПроверкиСинтаксиса(Проблемы));
		Возврат;
	КонецЕсли;
	Ванесса.ВыполнитьДанныйСценарийСТекущегоШага();
КонецПроцедуры

&НаКлиенте
Функция ПолучитьСтрокуДляПоискаШагаПоСнипету(Снипет)
	
	Поз = Найти(Снипет, "(");
	Если Поз > 0 Тогда
		Возврат НРег(Лев(Снипет, Поз - 1));
	КонецЕсли;
	
	Возврат НРег(Снипет);
	
КонецФункции

&НаКлиенте
Функция ПолучитьСообщенияПользователюИзОкна(ПараметрОкно)
	
	CообщенияПользователю = Неопределено;
	Попытка
		CообщенияПользователю = ПараметрОкно.ПолучитьТекстыСообщенийПользователю();
	Исключение
	КонецПопытки;
	
	Возврат CообщенияПользователю;
	
КонецФункции

// Внутренняя функция для очистки окна сообщений пользователю
&НаКлиенте
Функция ОчиститьОкноСообщенийПользователю()
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	ТекстОшибки = Неопределено;
	
	Попытка
		// Очищаем сообщения пользователю в активном окне
		АктивноеОкно.ЗакрытьПанельСообщенийПользователю();
		
		Возврат Ванесса.Локализовать("Окно сообщений пользователю было успешно очищено.");
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка очистки окна сообщений:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Внутренняя функция для выбора из списка выбора формы
&НаКлиенте
Функция ВыбратьИзСпискаВыбораФормы(Значение)
	
	Если НЕ ЗначениеЗаполнено(Значение) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для действия 'select_from_form_dropdown' необходимо указать параметр value.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	ТекстОшибки = Неопределено;
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("КоличествоПопыток", 1);
	
	Попытка
		
		ТекущаяФорма = Ванесса.НайтиТекущуюФормуTestClient();
		ТекущаяФорма.ВыполнитьВыборИзСписка(Значение);
		Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Значение '%1' было успешно выбрано из списка выбора, привязанного к форме."), Значение);
		
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка выбора из списка выбора формы:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Универсальный инструмент для управления записью действий пользователя
&НаКлиенте
Функция ЗаписьДействийПользователя(Запрос, ДополнительныеПараметры) Экспорт
	
	// Получаем параметр action (по умолчанию 'start')
	Действие = Строка(ЗначениеПараметраИзЗапроса(Запрос, "action"));
	Если НЕ ЗначениеЗаполнено(Действие) Тогда
		Действие = "start";
	Иначе
		Действие = НРег(СокрЛП(Действие));
	КонецЕсли;
	
	// Проверка допустимого значения
	Если Действие <> "start" И Действие <> "stop" Тогда
		ТекстСообщения = Ванесса.Локализовать("Недопустимое значение action. Допустимые значения: 'start' (начать запись), 'stop' (остановить запись и получить шаги).");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	Если Действие = "start" Тогда
		// Начинаем запись действий пользователя
		Попытка
			Ванесса.НачатьЗаписьДействийПользователяЭкспорт();
			Возврат Ванесса.Локализовать("# Запись действий начата
			|
			|- Статус: запись включена
			|
			|## Следующие шаги
			|- Выполните действия пользователя в клиенте тестирования
			|- Вызовите user_actions_recording с action='stop' для получения шагов");
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка начала записи действий:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
	ИначеЕсли Действие = "stop" Тогда
		// Останавливаем запись и получаем шаги в формате Turbo Gherkin
		Попытка
			Ванесса.ЗавершитьЗаписьДействийПользователяЭкспорт();
			ТекстСценария = Ванесса.Объект.СгенерированныйСценарий;
			
			Если ЗначениеЗаполнено(ТекстСценария) Тогда
				Возврат "# Запись действий остановлена
				|
				|- Статус: запись выключена
				|
				|## Записанные шаги (Turbo Gherkin)
				|```gherkin
				|" + ТекстСценария + "
				|```";
			Иначе
				Возврат Ванесса.Локализовать("# Запись действий остановлена
				|
				|- Статус: запись выключена
				|
				|- Записанные шаги: не обнаружено действий за время записи");
			КонецЕсли;
		Исключение
			ТекстОшибки = Ванесса.Локализовать("Ошибка остановки записи действий:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецПопытки;
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция ПолныйТекстБазыЗнанийСервер()
	
	ОбъектНаСервере = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектНаСервере.ПолучитьМакет("БазаЗнанийДляMCP").ПолучитьТекст();
	
КонецФункции

&НаКлиенте
Функция ДанныеИзБазыЗнанийКлиент(ФорматОтвета, НомерВопроса, СтрокаПоиска)
	
	Если ФорматОтвета = "all" Тогда
		Возврат глКешБазыЗнаний;
	ИначеЕсли ФорматОтвета = "short_info" Тогда
		Возврат КороткаяИнформацияОБазеЗнаний();
	ИначеЕсли ФорматОтвета = "questions_only" Тогда
		Возврат ТолькоВопросыИзБазыЗнанийСНомерами();
	ИначеЕсли ФорматОтвета = "one_question" Тогда
		Возврат ПолучитьОтветНаОдинВопрос(НомерВопроса);
	ИначеЕсли ФорматОтвета = "question_search" Тогда
		Возврат ПоискПоВопросам(СтрокаПоиска);
	ИначеЕсли ФорматОтвета = "answers_search" Тогда
		Возврат ПоискПоОтветам(СтрокаПоиска);
	КонецЕсли;	
	
КонецФункции

&НаКлиенте
Функция КороткаяИнформацияОБазеЗнаний()
	
	Вопросы = ПолучитьМассивВопросов();
	КолВопросов = Вопросы.Количество();
	
	КоличествоСимволов = СтрДлина(глКешБазыЗнаний);
	
	Возврат Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("## Количество вопросов: %1, размер базы знаний в символах: %2"), XMLСтрока(КолВопросов), XMLСтрока(КоличествоСимволов));
	
КонецФункции

&НаКлиенте
Функция ТолькоВопросыИзБазыЗнанийСНомерами()
	
	Вопросы = ПолучитьМассивВопросов();
	КолВопросов = Вопросы.Количество();
	
	КоличествоСимволов = СтрДлина(глКешБазыЗнаний);

	Результат = Новый Массив;
	Результат.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("## Количество вопросов: %1, размер базы знаний в символах: %2"), XMLСтрока(КолВопросов), XMLСтрока(КоличествоСимволов)));
	Результат.Добавить("");
	Для Каждого ТекВопрос Из Вопросы Цикл
		Результат.Добавить("Вопрос №" + xmlСтрока(ТекВопрос.Номер));
		Результат.Добавить(ТекВопрос.Текст);
		Результат.Добавить("");
	КонецЦикла;	
	
	Возврат Ванесса._СтрСоединить(Результат, Символы.ПС);
	
КонецФункции

&НаКлиенте
Функция ПолучитьОтветНаОдинВопрос(НомерВопроса)
	
	Если НомерВопроса = Неопределено ИЛИ НомерВопроса <= 0 Тогда
		ТекстСообщения = Ванесса.Локализовать("Некорректно указан параметр 'question_number'");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Вопросы = ПолучитьМассивВопросов();
	Если НомерВопроса > Вопросы.Количество() Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не корректно указан параметр 'question_number'. Всего вопросов %1, а передано значение %2"), XMLСтрока(Вопросы.Количество()), XMLСтрока(НомерВопроса));
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	ТекВопрос = Вопросы[НомерВопроса - 1];
	Результат = Новый Массив;
	Результат.Добавить(ТекВопрос.ТекстВопросаБезОбработки);
	Для Каждого Стр Из ТекВопрос.Ответ Цикл
		Результат.Добавить(Стр);
	КонецЦикла;	
	Возврат Ванесса._СтрСоединить(Результат, Символы.ПС);

КонецФункции

&НаКлиенте
Функция ПолучитьМассивВопросов()
	
	Если глКешВопросов <> Неопределено Тогда
		Возврат глКешВопросов;
	КонецЕсли;	
	
	Вопросы = Новый Массив;
	
	СтрокаПоиска = ПолучитьСтрокуПоискаВопрос();
	КолВопросов = 0;
	МассивСтрок = Ванесса._СтрРазделить(глКешБазыЗнаний, Символы.ПС);
	Для Сч = 0 По МассивСтрок.Количество()-1 Цикл
		Стр_нрег = НРег(МассивСтрок[Сч]);
		
		Поз = Найти(Стр_нрег, СтрокаПоиска);
		Если Поз = 1 Тогда
			ТекстВопроса = СокрЛП(Сред(МассивСтрок[Сч], СтрДлина(СтрокаПоиска) + 1));
			КолВопросов = КолВопросов + 1;
			ДанныеВопроса = Новый Структура;
			ДанныеВопроса.Вставить("Номер", КолВопросов);
			ДанныеВопроса.Вставить("Текст", ТекстВопроса);
			ДанныеВопроса.Вставить("ТекстВопросаБезОбработки", МассивСтрок[Сч]);
			ДанныеВопроса.Вставить("НомерСтроки", Сч + 1); //Номер строки с которой начинается ответ
			ДанныеВопроса.Вставить("Ответ", Новый Массив);
			Вопросы.Добавить(ДанныеВопроса);
		КонецЕсли;	
	КонецЦикла;
	
	ДлинныйРазделитель = ДлинныйРазделитель();
	
	Для Каждого ТекВопрос Из Вопросы Цикл
		НомерСтроки = ТекВопрос.НомерСтроки - 1;
		Пока Истина Цикл
			НомерСтроки = НомерСтроки + 1;
			Если НомерСтроки > МассивСтрок.Количество() - 1 Тогда
				// Значит кончился файл
				Прервать;
			КонецЕсли;	
			Стр = МассивСтрок[НомерСтроки];
			
			Если Лев(НРег(СокрЛ(Стр)), СтрДлина(СтрокаПоиска)) = СтрокаПоиска Тогда
				// Значит найден следующий вопрос
				Прервать;
			ИначеЕсли Найти(НРег(Стр), ДлинныйРазделитель) > 0 Тогда
				// Значит найден разделитель
				Прервать;
			КонецЕсли;
			
			ТекВопрос.Ответ.Добавить(Стр);
		КонецЦикла;
	КонецЦикла;
	
	глКешВопросов = Вопросы;
	
	Возврат Вопросы;
	
КонецФункции

&НаКлиенте
Функция ПолучитьСтрокуПоискаВопрос()
	Возврат НРег(Ванесса.Локализовать("Вопрос:"));
КонецФункции	

&НаКлиенте
Функция ПоискПоВопросам(СтрокаПоиска)
	Если НЕ ЗначениеЗаполнено(СтрокаПоиска) Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не корректно указан параметр 'search_string'."));
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Массив_СтрокаПоиска = MCPПодготовитьВариантыТочногоПоиска(СтрокаПоиска);
	
	Вопросы = ПолучитьМассивВопросов();
	
	Результат = Новый Массив;
	Результат.Добавить(Ванесса.Локализовать("## Результат поиска по вопросам"));
	Результат.Добавить("");
	
	Для Каждого ТекВопрос Из Вопросы Цикл
		
		Если НайденоВхождениеЗначенияВМассиве(Массив_СтрокаПоиска, НРег(ТекВопрос.Текст)) Тогда
			Результат.Добавить("Вопрос №" + xmlСтрока(ТекВопрос.Номер));
			Результат.Добавить(ТекВопрос.Текст);
			Результат.Добавить("");
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Ванесса._СтрСоединить(Результат, Символы.ПС);
	
КонецФункции

&НаКлиенте
Функция ПоискПоОтветам(СтрокаПоиска)
	Если НЕ ЗначениеЗаполнено(СтрокаПоиска) Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не корректно указан параметр 'search_string'."));
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Массив_СтрокаПоиска = MCPПодготовитьВариантыТочногоПоиска(СтрокаПоиска);
	
	Вопросы = ПолучитьМассивВопросов();
	
	Результат = Новый Массив;
	Результат.Добавить(Ванесса.Локализовать("## Результат поиска по ответам"));
	Результат.Добавить("");
	
	НайденныеВопросы = Новый Массив;
	
	Для Каждого ТекВопрос Из Вопросы Цикл
		
		Для Каждого СтрокаОтвета Из ТекВопрос.Ответ Цикл
			Если НайденоВхождениеЗначенияВМассиве(Массив_СтрокаПоиска, НРег(СтрокаОтвета)) Тогда
				НайденныеВопросы.Добавить(ТекВопрос);
				Прервать;
			КонецЕсли;
		КонецЦикла;	
		
	КонецЦикла;
	
	Для Каждого ТекВопрос Из НайденныеВопросы Цикл
		Результат.Добавить("Вопрос №" + xmlСтрока(ТекВопрос.Номер));
		Результат.Добавить(ТекВопрос.ТекстВопросаБезОбработки);
		Для Каждого Стр Из ТекВопрос.Ответ Цикл
			Результат.Добавить(Стр);
		КонецЦикла;	
		Результат.Добавить("");
		Результат.Добавить("");
		Результат.Добавить("");
	КонецЦикла;	
	
	Возврат Ванесса._СтрСоединить(Результат, Символы.ПС);
	
КонецФункции

&НаКлиенте
Функция ДлинныйРазделитель()
	Возврат "-------------------------------------------------------------------------------------------------------------";
КонецФункции	

&НаСервере
Функция ПолучитьДанныеМакетаЧастоИспользуемыеШаги()
	ОбъектНаСервере = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектНаСервере.ПолучитьМакет("ЧастоИспользуемыеШаги").ПолучитьТекст();
КонецФункции	

&НаКлиенте
Функция ДанныеОЧастоИспользуемыхШагах(КоличествоВывод)
	
	ШагиИзМассиваСнипетов = ШагиИзМассиваСнипетов();
	
	Результат = Новый Массив;
	Результат.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("# Всего в статистике %1 шагов"),XMLСтрока(ШагиИзМассиваСнипетов.Количество())));
	Результат.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("# Вывожу данные по %1 шагов"),XMLСтрока(КоличествоВывод)));
	Результат.Добавить("");
	
	Счетчик = 0;
	Для Каждого ТекШаг Из ШагиИзМассиваСнипетов Цикл
		Счетчик = Счетчик + 1;
		
		Если Счетчик > КоличествоВывод Тогда
			Прервать;
		КонецЕсли;
		
		Результат.Добавить(Ванесса.Локализовать("## Номер шага:") + " " + XMLСтрока(Счетчик));
		Результат.Добавить(Ванесса.Локализовать("## Тип:") + " " + ТекШаг.Тип);
		Результат.Добавить(Ванесса.Локализовать("## Представление шага:") + Символы.ПС + ТекШаг.Имя);
		Результат.Добавить(Ванесса.Локализовать("## Описание:") + Символы.ПС + ТекШаг.Описание);
		Результат.Добавить("");
	КонецЦикла;	
	
	Возврат Ванесса._СтрСоединить(Результат, Символы.ПС);
	
КонецФункции

&НаКлиенте
Функция ШагиИзМассиваСнипетов()
	МассивСнипетов = Ванесса._СтрРазделить(глЧастоИспользуемыеШаги, Символы.ПС);
	
	Кеш = Новый Соответствие;
	Для Каждого СтрокаТаблицаИзвестныхStepDefinition Из Ванесса.ТаблицаИзвестныхStepDefinition Цикл
		ДанныеШага = Новый Структура;
		ДанныеШага.Вставить("Имя", СтрокаТаблицаИзвестныхStepDefinition.ПредставлениеТеста);
		ДанныеШага.Вставить("Описание", СтрокаТаблицаИзвестныхStepDefinition.ОписаниеШага);
		ДанныеШага.Вставить("Тип", СтрокаТаблицаИзвестныхStepDefinition.ТипШага);
		ДанныеШага.Вставить("СтрокаДляПоиска", СтрокаТаблицаИзвестныхStepDefinition.СтрокаДляПоиска);
		
		Кеш.Вставить(НРег(СтрокаТаблицаИзвестныхStepDefinition.СтрокаДляПоиска), ДанныеШага);
	КонецЦикла;	
	
	Результат = Новый Массив;
	Для Каждого ТекСнипет Из МассивСнипетов Цикл
		ДанныеШага = Кеш[ТекСнипет];
		Если ДанныеШага = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Результат.Добавить(ДанныеШага);
		
	КонецЦикла;
	
	Возврат Результат;
КонецФункции

&НаКлиенте
Функция ПутьКФайлуИзТекущегоФайлаВРедакторе(ТекстОшибки)
	
	Если Ванесса.Объект.ИспользоватьРедакторVanessaEditor Тогда
		// Значит в редакторе должен быть открыт фича файл
		Если НЕ Ванесса.ЭтоВкладкаEdit(Ванесса.VanessaTabs.current) Тогда
			// Значит файл не открыт
			ТекстТекстОшибки = Ванесса.Локализовать("Нельзя перезагрузить текущий фича файл, т.к. в редакторе нет открытого файла.");
			Возврат Неопределено;
		КонецЕсли;	
	КонецЕсли;	
	Возврат Ванесса.Объект.КаталогФич;
		
КонецФункции

&НаКлиенте
Процедура СделатьШагФичиТекущим(НомерСтроки, Идентификатор, ТекстОшибки)
	СтрокаДерева = Ванесса.СтрокаДереваПоНомеруСтрокиФичи(НомерСтроки);
	Если СтрокаДерева = Неопределено Тогда
		ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В строке №%1 нет шага, который можно выполнить."), XMLСтрока(НомерСтроки));
		Возврат;
	КонецЕсли;
	
	Если СтрокаДерева.Тип <> "Шаг" Тогда
		ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("В строке №%1 нет шага, который можно выполнить. В этой строке обнаружен тип строки: %2"), XMLСтрока(НомерСтроки), СтрокаДерева.Тип);
		Возврат;
	КонецЕсли;	
	
	Ванесса.МодульРедакторТекста().АктивизироватьСтрокуРедактораПоИдСтрокиИНомеруСтрокиФичи(СтрокаДерева.ИдСтроки, НомерСтроки);
	Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
КонецПроцедуры

&НаСервере
Функция ДвоичныеДанныеГолосСервер()
	ОбъектСервер = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектСервер.ПолучитьМакет("ОзвучкаОповещенияMCP");
КонецФункции

// Универсальный инструмент для получения реквизитов объекта связанного с формой
&НаКлиенте
Процедура MCPИнструментПолучитьРеквизитыОбъекта(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_data_mode = Ванесса.Локализовать("Режим получения данных: 'all' (все данные - шапка и табличные части), 'attributes_only' (только реквизиты шапки), 'single_attribute' (значение указанного реквизита шапки), 'tabular_sections_only' (только все табличные части), 'single_tabular_section' (только указанная табличная часть). По умолчанию 'all'.");
    Свойства.Вставить("data_mode", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_data_mode));
	
	Описание_attribute_name = Ванесса.Локализовать("Имя реквизита шапки для получения значения. Используется только при data_mode='single_attribute'. Обязательный параметр для этого режима.");
    Свойства.Вставить("attribute_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_attribute_name));
	
	Описание_tabular_section_name = Ванесса.Локализовать("Имя табличной части для получения данных. Используется только при data_mode='single_tabular_section'. Обязательный параметр для этого режима.");
    Свойства.Вставить("tabular_section_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_tabular_section_name));
	
	Описание_only_filled = Ванесса.Локализовать("Возвращать только заполненные реквизиты: true (только заполненные), false (все реквизиты, по умолчанию). Параметр не применяется при получении одного реквизита (data_mode='single_attribute').");
    Свойства.Вставить("only_filled", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("boolean", Описание_only_filled));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_object_attributes",
        Ванесса.Локализовать("Получает значения реквизитов объекта (и их тип), связанного с открытой формой. Работает только для форм объектов (Справочники, Документы и т.п.), не работает для форм списков и служебных форм.
		|Параметр data_mode определяет состав возвращаемых данных:
		|- 'all' - все данные (реквизиты шапки и все табличные части)
		|- 'attributes_only' - только реквизиты шапки
		|- 'single_attribute' - только значение указанного реквизита шапки (требуется attribute_name)
		|- 'tabular_sections_only' - только все табличные части
		|- 'single_tabular_section' - только указанная табличная часть (требуется tabular_section_name)
		|
		|Параметр only_filled определяет фильтровать ли пустые реквизиты:
		|- true - возвращать только заполненные реквизиты
		|- false - возвращать все реквизиты (по умолчанию)
		|
		|Возвращает данные в формате Markdown с именами, типами и значениями реквизитов."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьРеквизитыОбъекта", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Универсальный инструмент для получения данных из таблицы базы данных
&НаКлиенте
Процедура MCPИнструментПолучитьДанныеИзТаблицыБД(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_object_type = Ванесса.Локализовать("Тип объекта в виде числа: 1 = Справочник, 2 = Документ, 3 = Перечисление. Обязательный параметр.");
    Свойства.Вставить("object_type", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("number", Описание_object_type));
	
	Описание_object_name = Ванесса.Локализовать("Имя справочника или документа или перечисления. Обязательный параметр");
    Свойства.Вставить("object_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_object_name));
	
	Описание_limit = Ванесса.Локализовать("Максимальное количество элементов для возврата. Значение по умолчанию 10. Не имеет смысла передавать параметр для перечислений, т.к. это всегда фиксированный набор значений.");
    Свойства.Вставить("limit", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("number", Описание_limit));
	
	Описание_owner = Ванесса.Локализовать("Наименование владельца для подчиненного справочника. Необязательный параметр.");
    Свойства.Вставить("owner", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_owner));
	
	Описание_date_start = Ванесса.Локализовать("Дата начала периода для документов (формат дата + время). Необязательный параметр. Пример формата: 30.12.2025 15:45:55");
    Свойства.Вставить("date_start", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_date_start));
	
	Описание_date_end = Ванесса.Локализовать("Дата окончания периода для документов (формат дата + время). Необязательный параметр. Пример формата: 30.12.2025 15:45:55");
	Свойства.Вставить("date_end", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_date_end));
	
	Описание_name_filter = Ванесса.Локализовать("Отбор по наименованию элемента. Работает для Справочников. Необязательный параметр.");
	Свойства.Вставить("name_filter", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_name_filter));
	
	ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("object_type");
	ТребуемыеПоля.Добавить("object_name");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_table_data",
        Ванесса.Локализовать("Получает данные из таблицы базы данных.
		|Цель - понять какие тествые данные уже есть базе, чтобы лучше понимать какими данными заполнять объекты в тестах.
		|Нужно указать Тип - object_type: Справочник, Документ, Перечисление.
		|Надо указать Вид - object_name.
		|limit - сколько элементов возвращать. Необязательный. Значение по умолчанию 10.
		|Для справочников будет возвращено Наименование, Код, Владелец, если такие поля есть у данного вида справочника.
		|Если справочник подчинен другому справочнику, то должен быть параметр для указания владельца.
		|Для документа будут возвращены Дата, Номер.
		|Для справочников и документов будет также возвращена навигационная ссылка.
		|Для документов можно передать ДатаНачала и ДатаОкончания. Оба параметра необязательные.
		|name_filter - отбор по наименованию элемента. Работает для Справочников. Необязательный параметр. Нужен, чтобы проверить есть ли конкретный элемент в базе."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьДанныеИзТаблицыБД", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Универсальный инструмент для сохранения табличного документа в файл
&НаКлиенте
Процедура MCPИнструментСохранитьТабличныйДокументВФайл(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_ИмяФайла = Ванесса.Локализовать("Полный путь к файлу для сохранения табличного документа. Обязательный параметр.");
    Свойства.Вставить("file_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_ИмяФайла));
	
	Описание_Формат = Ванесса.Локализовать("Формат файла: 'mxl' (MXL, по умолчанию), 'xlsx' (Excel), 'pdf' (PDF).");
    Свойства.Вставить("format", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_Формат));
	
	Описание_ИмяЭлементаФормы = Ванесса.Локализовать("Имя элемента формы (табличного документа) для сохранения. Обязательный параметр.");
    Свойства.Вставить("form_element_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_ИмяЭлементаФормы));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("file_name");
	ТребуемыеПоля.Добавить("form_element_name");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "save_table_document_to_file",
        Ванесса.Локализовать("Сохраняет содержимое табличного документа в файл на диске.
		|Поддерживаемые форматы: 'mxl' (MXL), 'xlsx' (Excel), 'pdf' (PDF).
		|Параметр file_name обязателен и должен содержать полный путь к файлу.
		|Параметр format необязательный, по умолчанию 'mxl'.
		|Параметр form_element_name обязателен и должен содержать имя элемента формы (табличного документа) для сохранения."),
        Схема,
        Новый ОписаниеОповещения("СохранитьТабличныйДокументВФайл", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Универсальный инструмент для получения данных элемента формы
&НаКлиенте
Процедура MCPИнструментПолучитьДанныеЭлементаФормы(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_ИмяЭлемента = Ванесса.Локализовать("Имя элемента формы для получения данных. Обязательный параметр.");
    Свойства.Вставить("element_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_ИмяЭлемента));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("element_name");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "get_form_element_data",
        Ванесса.Локализовать("Получает данные указанного элемента формы клиента тестирования.
		|Возвращает информацию об элементе в формате Markdown:
		|- Имя элемента
		|- Заголовок
		|- Тип
		|- Вид
		|- Значение
		|- Представление (если применимо)
		|- Флаги: Видимость, Доступность, ТолькоПросмотр
		|
		|Параметр element_name обязателен и должен содержать имя элемента формы."),
        Схема,
        Новый ОписаниеОповещения("ПолучитьДанныеЭлементаФормы", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Универсальный инструмент для управления элементами формы
&НаКлиенте
Процедура MCPИнструментУправлениеЭлементамиФормы(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_action = Ванесса.Локализовать("Действие: 'click_button' (нажать кнопку), 'input_text' (ввести текст в поле), 'open_dropdown' (открыть выпадающий список, дождаться его открытия и вернуть содержимое), 'get_dropdown_state' (получить состояние открытого списка, если он уже открыт), 'select_from_dropdown' (выбрать значение из открытого списка), 'set_value' (установить значение в поле - выбор из списка или точное значение), 'open_choice_form' (открыть форму выбора), 'set_radio_button' (установить переключатель), 'set_checkbox' (установить флаг), 'unset_checkbox' (снять флаг), 'toggle_checkbox' (переключить флаг на противоположное значение), 'click_hyperlink' (нажать гиперссылку), 'simulate_tab' (эмуляция перехода TAB), 'activate_column' (активизировать колонку таблицы), 'navigate_next_column' (переход к следующей колонке), 'navigate_previous_column' (переход к предыдущей колонке), 'start_edit_row' (начать редактирование строки), 'end_edit_row' (закончить редактирование строки), 'navigate_to_row' (переход к строке по ключу), 'navigate_next_row' (переход к следующей строке таблицы), 'navigate_previous_row' (переход к предыдущей строке таблицы), 'select_row' (выбрать строку), 'get_selected_rows' (вернуть значение выделенных/текущей строки таблицы), 'create_new' (создать новый), 'switch_tab' (переключить закладку (группу формы)), 'toggle_group' (развернуть/свернуть группу), 'activate_header_element' (активизировать элемент шапки), 'clear_field' (очистить поле шапки или таблицы), 'clear_message_window' (очистить окно сообщений пользователю), 'select_from_form_dropdown' (выбрать значение из списка выбора, привязанного к текущей форме). Обязательный параметр.");
    Свойства.Вставить("action", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_action));
	
	Описание_element_name = Ванесса.Локализовать("Имя элемента формы(не заголовок, а именно внутреннее имя) для выполнения действия. Обязательный параметр для большинства действий.");
    Свойства.Вставить("element_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_element_name));
	
	Описание_table_name = Ванесса.Локализовать("Имя табличной части, если элемент находится в таблице. Если поле или кнопка находится в таблице, то этот параметр обязательно указывать.");
    Свойства.Вставить("table_name", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_table_name));
	
	Описание_value = Ванесса.Локализовать("Значение для ввода/выбора. Используется для действий: input_text, set_value, select_from_dropdown, activate_column (имя колонки), navigate_to_row (критерии строки), toggle_group (expand - развернуть, collapse - свернуть). Необязательный параметр.");
	   Свойства.Вставить("value", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_value));
	
	Описание_hyperlink_title = Ванесса.Локализовать("Заголовок гиперссылки для нажатия. Используется для действия click_hyperlink, когда в поле несколько гиперссылок. Необязательный параметр.");
    Свойства.Вставить("hyperlink_title", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_hyperlink_title));
	
	Описание_hyperlink_index = Ванесса.Локализовать("Индекс гиперссылки (начиная с 1) для нажатия. Используется для действия click_hyperlink, когда в поле несколько гиперссылок. Необязательный параметр.");
    Свойства.Вставить("hyperlink_index", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("number", Описание_hyperlink_index));
	
	Описание_tab_direction = Ванесса.Локализовать("Направление перехода для действия simulate_tab: 'next' (к следующему элементу, по умолчанию), 'previous' (к предыдущему элементу). Необязательный параметр.");
    Свойства.Вставить("tab_direction", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_tab_direction));
	
	Описание_exact_match = Ванесса.Локализовать("Флаг точного совпадения для действия set_value: true (точное значение), false (поиск по подстроке, по умолчанию). Необязательный параметр.");
    Свойства.Вставить("exact_match", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("boolean", Описание_exact_match));
	
	Описание_row_criteria = Ванесса.Локализовать("Критерии для перехода к строке таблицы (действие navigate_to_row). Формат: 'Колонка1=Значение1;Колонка2=Значение2'.");
	    Свойства.Вставить("row_criteria", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_row_criteria));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("action");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "manage_form_elements",
        Ванесса.Локализовать("Универсальный инструмент для управления элементами формы клиента тестирования.
		|Поддерживаемые действия (параметр action):
		|- 'click_button' - нажатие на кнопку формы (требуется element_name)
		|- 'input_text' - ввод текста в поле (требуются element_name и value)
		|- 'open_dropdown' - открыть выпадающий список (требуется element_name)
		|- 'get_dropdown_state' - получить состояние открытого списка (требуется element_name)
		|- 'select_from_dropdown' - выбрать значение из открытого списка (требуются element_name и value)
		|- 'set_value' - установить значение в поле одним действием (требуются element_name и value)
		|- 'open_choice_form' - открыть форму выбора для поля (требуется element_name)
		|- 'set_radio_button' - установить переключатель (требуется element_name). set_radio_button имеет смысл только для полей шапки.
		|- 'set_checkbox' - установить флаг (требуется element_name).
		|- 'unset_checkbox' - снять флаг (требуется element_name).
		|- 'toggle_checkbox' - переключить флаг на противоположное значение (требуется element_name).
		|- 'click_hyperlink' - нажать гиперссылку (требуется element_name, опционально hyperlink_title или hyperlink_index). click_hyperlink имеет смысл только для полей шапки.
		|- 'simulate_tab' - эмуляция перехода TAB (без параметров или с tab_direction)
		|- 'activate_column' - активизировать колонку таблицы (требуются table_name и value=column_name)
		|- 'navigate_next_column' - переход к следующей колонке в таблице (эмуляция TAB). параметры: table_name
		|- 'navigate_previous_column' - переход к предыдущей колонке в таблице (эмуляция SHIFT+TAB). параметры: table_name
		|- 'start_edit_row' - начать редактирование строки таблицы. параметры: table_name. Редактирование начнётся в текущей колонке таблицы. 
		|- 'end_edit_row' - закончить редактирование строки таблицы. параметры: table_name
		|- 'navigate_to_row' - переход к строке таблицы по ключу. параметры: table_name и row_criteria ('Колонка=Значение;Колонка2=Значение2')
		|- 'navigate_next_row' - переход к следующей строке таблицы. параметры: table_name
		|- 'navigate_previous_row' - переход к предыдущей строке таблицы. параметры: table_name
		|- 'select_row' - выбрать текущую строку таблицы (эмуляция нажатия ENTER на текущей строке и колонке таблицы). параметры: table_name
		|- 'get_selected_rows' - вернуть значение выделенных строк таблицы (по умолчанию возвращается значение текущей строки). параметры: table_name
		|- 'create_new' - вызывает команду создания нового элемента у указанного поля шапки или поля таблицы. параметры: element_name (обязательный), table_name (опционально, если элемент находится в таблице)
		|- 'switch_tab' - переключиться на закладку (группу формы). параметры: element_name (имя группы/закладки)
		|- 'toggle_group' - развернуть/свернуть группу. параметры: element_name (имя группы), value (expand - развернуть, collapse - свернуть, toggle или не указано - переключить)
		|- 'activate_header_element' - активизировать элемент шапки формы. параметры: element_name
		|- 'clear_field' - очистить поле (требуется element_name, опционально table_name для полей в таблицах)
		|- 'clear_message_window' - очистить окно сообщений пользователю в текущем окне (не требует параметров)
		|- 'select_from_form_dropdown' - выбрать значение из списка выбора формы (требуются только value)
		|
		|Для элементов в таблицах используйте параметр table_name.
		|Требует подключённого клиента тестирования."),
        Схема,
        Новый ОписаниеОповещения("УправлениеЭлементамиФормы", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Универсальный инструмент для управления записью действий пользователя
&НаКлиенте
Процедура MCPИнструментЗаписьДействийПользователя(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	
	Описание_action = Ванесса.Локализовать("Действие: 'start' (начать запись), 'stop' (остановить запись и получить шаги). Обязательный параметр.");
    Свойства.Вставить("action", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_action));
	
    ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("action");
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "user_actions_recording",
        Ванесса.Локализовать("Управляет записью действий пользователя в клиенте тестирования.
		|Поддерживаемые действия (параметр action):
		|- 'start' (по умолчанию) - начать запись действий пользователя
		|- 'stop' - остановить запись и вернуть записанные шаги в формате Turbo Gherkin
		|
		|При остановке записи возвращается текст шагов в формате Turbo Gherkin, которые были выполнены пользователем за время записи.
		|Требует подключённого клиента тестирования."),
        Схема,
        Новый ОписаниеОповещения("ЗаписьДействийПользователя", Вычислить("ЭтотОбъект")),
		Ложь),
	    ИмяПровайдера());
		
КонецПроцедуры

// Закрывает клиент тестирования по имени профиля
&НаКлиенте
Процедура MCPИнструментЗакрытьКлиентТестирования(ПараметрыСозданияMCP)
	
    Свойства = Новый Структура;
	Описание_profileName = Ванесса.Локализовать("Имя профиля клиента тестирования для закрытия. Если не указано — закроет текущий подключенный профиль.");
    Свойства.Вставить("profileName", ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСвойствоСхемы("string", Описание_profileName));
	
    ТребуемыеПоля = Новый Массив;
    Схема = ПараметрыСозданияMCP.МодульМсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);
	ПараметрыСозданияMCP.МодульМсп_Сервер.ЗарегистрироватьИнструмент(ПараметрыСозданияMCP.МодульМсп_Сервер.НовыйИнструмент(
        "close_test_client",
        Ванесса.Локализовать("Закрывает сеанс клиента тестирования по имени профиля. Если профиль не указан — закрывает текущий подключенный профиль.
		|Используйте для завершения работы с клиентом тестирования после выполнения тестов."),
        Схема,
        Новый ОписаниеОповещения("ЗакрытьКлиентТестирования", Вычислить("ЭтотОбъект")),
		Истина), // асинхронный метод
	    ИмяПровайдера());
		
КонецПроцедуры

// Закрывает клиент тестирования по имени профиля
&НаКлиенте
Функция ЗакрытьКлиентТестирования(Запрос, ДополнительныеПараметры) Экспорт
	
	// Получаем параметр profileName (необязательный)
	ИмяПрофиля = Строка(ЗначениеПараметраИзЗапроса(Запрос, "profileName"));
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Нечего закрывать.") + " " + ТекстОшибки);
	КонецПопытки;
	
	// Определяем какой профиль закрывать
	Если НЕ ЗначениеЗаполнено(ИмяПрофиля) Тогда
		// Закрываем текущий подключенный профиль
		ТекущиеДанные = Ванесса.ТекущиеДанныеКлиентаТестирования();
		Если ТекущиеДанные = Неопределено Тогда
			Возврат Ванесса.Локализовать("# Клиент тестирования не был подключен
			|
			|- Статус: не подключен
			|
			|Клиент тестирования не был подключен, поэтому закрывать нечего.");
		КонецЕсли;
		ИмяПрофиля = ТекущиеДанные.Имя;
	КонецЕсли;
	
	// Ищем профиль в таблице
	НайденныйПрофиль = Ванесса.ПолучитьСвойстваПодключенногоTestClient(ИмяПрофиля);
	
	Если НайденныйПрофиль = Неопределено Тогда
		ТекстСообщения = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не найдено подключенного клиента тестирования с именем профиля '%1'."), ИмяПрофиля);
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Закрываем окно клиента тестирования
	Попытка
		ТекстОшибки = Неопределено;
		Ванесса.ЯЗакрываюTestClient(НайденныйПрофиль.Имя, ТекстОшибки);
		Если ТекстОшибки <> Неопределено Тогда
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
		КонецЕсли;
		ТекстОтвета = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать(
			"# Клиент тестирования закрыт
			|
			|- Профиль: %1
			|- Статус: закрыт успешно
			|
			|## Следующие шаги
			|- Используйте `connect_test_client` для повторного подключения"),
			ИмяПрофиля);
		Возврат ТекстОтвета;
	Исключение
		ТекстОшибки = Ванесса.Локализовать("Ошибка закрытия клиента тестирования:") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецПопытки;
	
КонецФункции

// Возвращает значения реквизитов объекта связанного с формой
&НаКлиенте
Функция ПолучитьРеквизитыОбъекта(Запрос, ДополнительныеПараметры) Экспорт
	
	// Получаем параметр data_mode (по умолчанию 'all')
	РежимДанных = Строка(ЗначениеПараметраИзЗапроса(Запрос, "data_mode"));
	Если НЕ ЗначениеЗаполнено(РежимДанных) Тогда
		РежимДанных = "all";
	Иначе
		РежимДанных = НРег(СокрЛП(РежимДанных));
	КонецЕсли;
	
	// Проверка допустимого значения
	ДопустимыеРежимы = Новый Массив;
	ДопустимыеРежимы.Добавить("all");
	ДопустимыеРежимы.Добавить("attributes_only");
	ДопустимыеРежимы.Добавить("single_attribute");
	ДопустимыеРежимы.Добавить("tabular_sections_only");
	ДопустимыеРежимы.Добавить("single_tabular_section");
	
	Если ДопустимыеРежимы.Найти(РежимДанных) = Неопределено Тогда
		ТекстСообщения = Ванесса.Локализовать("Недопустимое значение data_mode. Допустимые значения: 'all', 'attributes_only', 'single_attribute', 'tabular_sections_only', 'single_tabular_section'.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр attribute_name для режима single_attribute
	ИмяРеквизита = Строка(ЗначениеПараметраИзЗапроса(Запрос, "attribute_name"));
	Если РежимДанных = "single_attribute" И НЕ ЗначениеЗаполнено(ИмяРеквизита) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для режима data_mode='single_attribute' необходимо указать параметр attribute_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр tabular_section_name для режима single_tabular_section
	ИмяТабличнойЧасти = Строка(ЗначениеПараметраИзЗапроса(Запрос, "tabular_section_name"));
	Если РежимДанных = "single_tabular_section" И НЕ ЗначениеЗаполнено(ИмяТабличнойЧасти) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для режима data_mode='single_tabular_section' необходимо указать параметр tabular_section_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр only_filled (по умолчанию false - возвращать все реквизиты)
	ТолькоЗаполненные = ЗначениеПараметраИзЗапроса(Запрос, "only_filled");
	Если ТолькоЗаполненные = Неопределено Тогда
		ТолькоЗаполненные = Ложь;
	Иначе
		Попытка
			ТолькоЗаполненные = Булево(ТолькоЗаполненные);
		Исключение
			ТолькоЗаполненные = Ложь;
		КонецПопытки;
	КонецЕсли;
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	ТекстОшибки = Неопределено;
	ДанныеОбъекта = ДанныеОбъектаИзУказанногоОкна(АктивноеОкно.Заголовок, ТекстОшибки);
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;
	
	// Формируем результат
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Реквизиты объекта"));
	Строки.Добавить("");
	
	// Получаем реквизиты шапки
	Если РежимДанных = "all" ИЛИ РежимДанных = "attributes_only" ИЛИ РежимДанных = "single_attribute" Тогда
		Если РежимДанных = "single_attribute" Тогда
			//Получаем только указанный реквизит
			Нашли = Ложь;
			Попытка
				Для Каждого ТекРеквизит Из ДанныеОбъекта.СтандартныеРеквизиты Цикл
					Если НРег(ТекРеквизит.Имя) = НРег(ИмяРеквизита) Тогда
						ДобавитьРеквизитКВыводуВMarkdown(Строки, ТекРеквизит);
						Нашли = Истина;
						Прервать;
					КонецЕсли;	
				КонецЦикла;
				
				Если НЕ Нашли Тогда
					Для Каждого ТекРеквизит Из ДанныеОбъекта.РеквизитыШапки Цикл
						Если НРег(ТекРеквизит.Имя) = НРег(ИмяРеквизита) Тогда
							ДобавитьРеквизитКВыводуВMarkdown(Строки, ТекРеквизит);
							Нашли = Истина;
							Прервать;
						КонецЕсли;	
					КонецЦикла;
				КонецЕсли;	
				
			Исключение
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Реквизит '%1' не найден в объекте.") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки()), ИмяРеквизита);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецПопытки;
			
			Если НЕ Нашли Тогда
				ТекстОшибки = Ванесса.Локализовать("Реквизит '%1' не найден в объекте.");
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(ТекстОшибки, ИмяРеквизита);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;	
			
		Иначе
			// Получаем все реквизиты шапки или все данные
			Строки.Добавить(Ванесса.Локализовать("## Реквизиты шапки"));
			Строки.Добавить("");
			
			Для Каждого ТекРеквизит Из ДанныеОбъекта.СтандартныеРеквизиты Цикл
				ДобавитьРеквизитКВыводуВMarkdown(Строки, ТекРеквизит, ТолькоЗаполненные);
			КонецЦикла;
			
			Для Каждого ТекРеквизит Из ДанныеОбъекта.РеквизитыШапки Цикл
				ДобавитьРеквизитКВыводуВMarkdown(Строки, ТекРеквизит, ТолькоЗаполненные);
			КонецЦикла;
			
		КонецЕсли;
	КонецЕсли;
	
	// Получаем табличные части
	Если РежимДанных = "all" ИЛИ РежимДанных = "tabular_sections_only" ИЛИ РежимДанных = "single_tabular_section" Тогда
		Если РежимДанных = "single_tabular_section" Тогда
			// Получаем только указанную табличную часть
			Попытка
				
				Нашли = Ложь;
				Для Каждого ТекТабличнаяЧасть Из ДанныеОбъекта.ТабличныеЧасти Цикл
					Если НРег(ТекТабличнаяЧасть.Имя) = НРег(ИмяТабличнойЧасти) Тогда
						ВывестиРеквизитыТабличнойЧастиВMarkdown(Строки, ТекТабличнаяЧасть);
						Нашли = Истина;
						Прервать;
					КонецЕсли;	
				КонецЦикла;
				
			Исключение
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Табличная часть '%1' не найдена в объекте.") + " " + ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки()), ИмяТабличнойЧасти);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецПопытки;
			
			Если НЕ Нашли Тогда
				ТекстОшибки = Ванесса.Локализовать("Табличная часть '%1' не найдена в объекте.");
				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(ТекстОшибки, ИмяТабличнойЧасти);
				Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
			КонецЕсли;	
			
		Иначе
			// Получаем все табличные части или все данные
			Строки.Добавить(Ванесса.Локализовать("## Табличные части"));
			Строки.Добавить("");
			
			Для Каждого ТекТабличнаяЧасть Из ДанныеОбъекта.ТабличныеЧасти Цикл
				ВывестиРеквизитыТабличнойЧастиВMarkdown(Строки, ТекТабличнаяЧасть, ТолькоЗаполненные);
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

&НаКлиенте
Функция ДанныеОбъектаИзУказанногоОкна(ЗаголовокОкна, ТекстОшибки)
	Ванесса.ОткрытьНавигационнуюСсылку("e1cib/app/Обработка.VAExtension_ДанныеРеквизитовОбъекта");
	ТестовоеПриложение = Ванесса.ПолучитьТестовоеПриложение();
	НужнаяФорма = ТестовоеПриложение.НайтиОбъект(Тип("ТестируемаяФорма"),, "*.VAExtension_ДанныеРеквизитовОбъекта.*",5);
	Если НужнаяФорма = Неопределено Тогда
		ТекстОшибки = Ванесса.Локализовать("Клиент тестирования подключен, но не найдена форма 'VAExtension_ДанныеРеквизитовОбъекта' из служебного расширения.");
		Возврат Неопределено
	КонецЕсли;	
	
	РеквизитВыражениеВычислено = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ОбработкаВыполнена", Истина);
	РеквизитЗаголовокОкна = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ЗаголовокОкна", Истина);
	РеквизитЗаголовокОкна.ВвестиТекст(ЗаголовокОкна);
	
	АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	
	КнопкаВыполнитьОбработку = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяКнопкаФормы"),,"ФормаВыполнитьОбработку");
	
	КнопкаЗакрытьФорму = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяКнопкаФормы"),,"ЗакрытьФорму");
	
	КнопкаВыполнитьОбработку.Нажать();
	
	БылаОшибка = Истина;
	Ванесса.ОжиданиеОкончанияОбработки(БылаОшибка, РеквизитВыражениеВычислено);
	
	Если БылаОшибка Тогда
		ТекстОшибки = Ванесса.Локализовать("Не вышло получить данные объекта из текущего окна.");
		Возврат Неопределено
	КонецЕсли;
	
	РеквизитЗначениеДляВозврата = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ЗначениеРезультат", Истина);
	ПредставлениеРезультата = Ванесса.ПолучитьПредставлениеДанныхЭлементаФормы(РеквизитЗначениеДляВозврата);
	
	Данные = Ванесса.ПрочитатьОбъектJSON(ПредставлениеРезультата);
	
	Попытка
		КнопкаЗакрытьФорму.Нажать();
	Исключение
		Ванесса.ЗаписатьЛогВЖРИнформация("ДанныеОбъектаИзУказанногоОкна", ОписаниеОшибки());
	КонецПопытки;
	
	Возврат Данные;
КонецФункции	

&НаКлиенте
Процедура ДобавитьРеквизитКВыводуВMarkdown(Строки, ТекРеквизит, ТолькоЗаполненные = Ложь)
	ЗначениеРеквизита = ТекРеквизит.Значение;
	ТипРеквизита = ТекРеквизит.Тип;
	
	// Если установлен флаг ТолькоЗаполненные и значение пустое - пропускаем реквизит
	Если ТолькоЗаполненные И НЕ ЗначениеЗаполнено(ЗначениеРеквизита) Тогда
		Возврат;
	КонецЕсли;
	
	Строки.Добавить(Ванесса.Локализовать("## Реквизит:") + " " + ТекРеквизит.Имя);
	Строки.Добавить("");
	Строки.Добавить(Ванесса.Локализовать("- Тип:") + " " + ТипРеквизита);
	Строки.Добавить(Ванесса.Локализовать("- Значение:") + " " + MCPПредставлениеЗначенияДляMarkdown(ЗначениеРеквизита));
КонецПроцедуры

&НаКлиенте
Процедура ВывестиРеквизитыТабличнойЧастиВMarkdown(Строки, ТабличнаяЧасть, ТолькоЗаполненные = Ложь)
	
	Строки.Добавить(Ванесса.Локализовать("## Табличная часть: ") + ТабличнаяЧасть.Имя);
	Строки.Добавить("");
	Строки.Добавить(Ванесса.Локализовать("Количество строк: ") + Формат(ТабличнаяЧасть.ЗначениеТаблицы.Количество(), "ЧГ=0; ЧН=0"));
	Строки.Добавить("");
	
	Если ТабличнаяЧасть.ЗначениеТаблицы.Количество() > 0 Тогда
		// Получаем колонки
		
		Строки.Добавить(Ванесса.Локализовать("### Данные"));
		Строки.Добавить("");
		Для Сч = 0 По ТабличнаяЧасть.ЗначениеТаблицы.Количество() - 1 Цикл
			СтрокаТЧ = ТабличнаяЧасть.ЗначениеТаблицы[Сч];
			
			// Если установлен флаг ТолькоЗаполненные, проверяем есть ли в строке заполненные значения
			Если ТолькоЗаполненные Тогда
				ЕстьЗаполненныеЗначения = Ложь;
				Для Каждого ТекРеквизит Из СтрокаТЧ Цикл
					Если ЗначениеЗаполнено(ТекРеквизит.Значение) Тогда
						ЕстьЗаполненныеЗначения = Истина;
						Прервать;
					КонецЕсли;
				КонецЦикла;
				
				Если НЕ ЕстьЗаполненныеЗначения Тогда
					Продолжить;
				КонецЕсли;
			КонецЕсли;
			
			Строки.Добавить(Ванесса.Локализовать("#### Строка ") + Формат(Сч + 1, "ЧГ=0; ЧН=0"));
			Строки.Добавить("");
			Для Каждого ТекРеквизит Из СтрокаТЧ Цикл
				ЗначениеКолонки = ТекРеквизит.Значение;
				
				// Если установлен флаг ТолькоЗаполненные и значение пустое - пропускаем колонку
				Если ТолькоЗаполненные И НЕ ЗначениеЗаполнено(ЗначениеКолонки) Тогда
					Продолжить;
				КонецЕсли;
				
				Строки.Добавить(Ванесса.Локализовать("- ") + ТекРеквизит.Имя + ": " + MCPПредставлениеЗначенияДляMarkdown(ЗначениеКолонки) + " [" + ТекРеквизит.Тип + "]");
			КонецЦикла;
			Строки.Добавить("");
		КонецЦикла;
	Иначе
		Строки.Добавить(Ванесса.Локализовать("Табличная часть пуста."));
	КонецЕсли;
	
КонецПроцедуры

// Возвращает данные из таблицы базы данных (справочники, документы, перечисления)
&НаКлиенте
Функция ПолучитьДанныеИзТаблицыБД(Запрос, ДополнительныеПараметры) Экспорт
	
	// Получаем параметр object_type (обязательный, числовой)
	// 1 = Справочник, 2 = Документ, 3 = Перечисление
	Попытка
		ТипОбъекта = Число(ЗначениеПараметраИзЗапроса(Запрос, "object_type"));
	Исключение
		ТекстСообщения = Ванесса.Локализовать("Параметр object_type должен быть числом. Допустимые значения: 1 = Справочник, 2 = Документ, 3 = Перечисление.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецПопытки;
	
	// Проверка допустимого значения
	Если ТипОбъекта <> 1 И ТипОбъекта <> 2 И ТипОбъекта <> 3 Тогда
		ТекстСообщения = Ванесса.Локализовать("Недопустимое значение object_type. Допустимые значения: 1 = Справочник, 2 = Документ, 3 = Перечисление.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр object_name
	ИмяОбъекта = Строка(ЗначениеПараметраИзЗапроса(Запрос, "object_name"));
	Если НЕ ЗначениеЗаполнено(ИмяОбъекта) Тогда
		ТекстСообщения = Ванесса.Локализовать("Необходимо указать параметр object_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	// Получаем параметр limit (по умолчанию 10)
	Попытка
		Лимит = Число(ЗначениеПараметраИзЗапроса(Запрос, "limit"));
	Исключение
		Лимит = 0;
	КонецПопытки;
	
	Если Лимит = Неопределено ИЛИ Лимит <= 0 Тогда
		Лимит = 10;
	КонецЕсли;
	
	// Получаем параметр owner (для подчиненных справочников)
	Владелец = Строка(ЗначениеПараметраИзЗапроса(Запрос, "owner"));
	
	// Получаем параметр name_filter (для отбора по наименованию)
	ОтборПоНаименованию = Строка(ЗначениеПараметраИзЗапроса(Запрос, "name_filter"));
	
	// Получаем параметры даты для документов
	ДатаНачала = ЗначениеПараметраИзЗапроса(Запрос, "date_start");
	ДатаОкончания = ЗначениеПараметраИзЗапроса(Запрос, "date_end");
	
	Если ЗначениеЗаполнено(ДатаНачала) Тогда
		Попытка
			ДатаНачала = Ванесса.ДатаИзСтрокиКомандыСистемы(ДатаНачала);
		Исключение
			ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не получилось преобразовать строку <%1> в дату начала."), ДатаНачала) + Символы.ПС  + ТекстОшибки);
		КонецПопытки;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДатаОкончания) Тогда
		Попытка
			ДатаОкончания = Ванесса.ДатаИзСтрокиКомандыСистемы(ДатаОкончания);
		Исключение
			ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
			Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не получилось преобразовать строку <%1> в дату окончания."), ДатаОкончания) + Символы.ПС  + ТекстОшибки);
		КонецПопытки;
	КонецЕсли;
	
	// Проверяем подключение клиента тестирования
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Исключение
		ТекстОшибки = ТекстОшибкиИзОписанияОшибки(ОписаниеОшибки());
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", Ванесса.Локализовать("Клиент тестирования не подключен. Сначала вызовите connect_test_client.") + " " + ТекстОшибки);
	КонецПопытки;
	
	// Формируем результат в зависимости от типа объекта
	Если ТипОбъекта = 3 Тогда
		// Перечисление
		Возврат ПолучитьДанныеПеречисления(ИмяОбъекта);
	ИначеЕсли ТипОбъекта = 1 Тогда
		// Справочник
		Возврат ПолучитьДанныеСправочника(ИмяОбъекта, Владелец, Лимит, ОтборПоНаименованию);
	ИначеЕсли ТипОбъекта = 2 Тогда
		// Документ
		Возврат ПолучитьДанныеДокумента(ИмяОбъекта, ДатаНачала, ДатаОкончания, Лимит);
	КонецЕсли;
	
КонецФункции

// Возвращает данные перечисления
&НаКлиенте
Функция ПолучитьДанныеПеречисления(ИмяПеречисления)
	
	Если НЕ ЗначениеЗаполнено(ИмяПеречисления) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для типа объекта 'Перечисление' необходимо указать параметр object_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Строки = Новый Массив;
	
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("Тип", "Перечисление");
	ДопПараметры.Вставить("Вид", ИмяПеречисления);
	
	ТекстОшибки = Неопределено;
	Данные = ДанныеИзБазыДанныхКлиентаТестирования(ДопПараметры, ТекстОшибки);
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;
	
	Если ТипЗнч(Данные) = Тип("Структура") Тогда
		ОшибкаНетОбъектаМетаданных = Данные.ОшибкаНетОбъектаМетаданных;
		Строки.Добавить(Ванесса.Локализовать("# В базе клиента тестирования нет перечисления:") + " " + ИмяПеречисления);
		Строки.Добавить(Ванесса.Локализовать("# Доступные значения:"));
		Для Каждого ТекИмяОМ Из Данные.ВсеОбъекты Цикл
			Если Лев(НРег(ТекИмяОМ), 7) = "удалить" ИЛИ Лев(НРег(ТекИмяОМ), 6) = "delete" Тогда
				// Значит значение устарело
				Продолжить;
			КонецЕсли;
			Строки.Добавить("  - " + ТекИмяОМ);
		КонецЦикла;	
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	КонецЕсли;
	
	Строки.Добавить(Ванесса.Локализовать("# Перечисление:") + " " + ИмяПеречисления);
	Строки.Добавить("");
	
	Счетчик = 0;
	Для Каждого ТекДанные Из Данные Цикл
		
		Если Лев(НРег(ТекДанные.Имя), 7) = "удалить" ИЛИ Лев(НРег(ТекДанные.Имя), 6) = "delete" Тогда
			// Значит значение устарело
			Продолжить;
		КонецЕсли;
		
		Счетчик = Счетчик + 1;
		
		Строки.Добавить(Ванесса.Локализовать("## Значение") + " " + Формат(Счетчик, "ЧГ=0; ЧН=0"));
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("- Имя:") + " " + ТекДанные.Имя);
		Если ЗначениеЗаполнено(ТекДанные.Представление) Тогда
			Строки.Добавить(Ванесса.Локализовать("- Представление для пользоватля:") + " " + ТекДанные.Представление);
		Иначе	
			Строки.Добавить(Ванесса.Локализовать("- Представление для пользоватля:") + " " + ТекДанные.Имя);
		КонецЕсли;
		Строки.Добавить("");
	КонецЦикла;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Возвращает данные справочника
&НаКлиенте
Функция ПолучитьДанныеСправочника(ИмяСправочника, Владелец, Лимит, ОтборПоНаименованию = Неопределено)
	
	Если НЕ ЗначениеЗаполнено(ИмяСправочника) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для типа объекта 'Справочник' необходимо указать параметр object_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Строки = Новый Массив;

	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("Тип", "Справочник");
	ДопПараметры.Вставить("Вид", ИмяСправочника);
	ДопПараметры.Вставить("Владелец", Владелец);
	ДопПараметры.Вставить("Лимит", Лимит);
	Если ЗначениеЗаполнено(ОтборПоНаименованию) Тогда
		ДопПараметры.Вставить("ОтборПоНаименованию", ОтборПоНаименованию);
	КонецЕсли;
	
	ТекстОшибки = Неопределено;
	Данные = ДанныеИзБазыДанныхКлиентаТестирования(ДопПараметры, ТекстОшибки);
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;
	
	Если ТипЗнч(Данные) = Тип("Структура") Тогда
		ОшибкаНетОбъектаМетаданных = Данные.ОшибкаНетОбъектаМетаданных;
		Строки.Добавить(Ванесса.Локализовать("# В базе клиента тестирования нет справочника:") + " " + ИмяСправочника);
		Строки.Добавить(Ванесса.Локализовать("# Доступные значения:"));
		Для Каждого ТекИмяОМ Из Данные.ВсеОбъекты Цикл
			Если Лев(НРег(ТекИмяОМ), 7) = "удалить" ИЛИ Лев(НРег(ТекИмяОМ), 6) = "delete" Тогда
				// Значит значение устарело
				Продолжить;
			КонецЕсли;
			Строки.Добавить("  - " + ТекИмяОМ);
		КонецЦикла;	
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	КонецЕсли;
	
	Строки.Добавить(Ванесса.Локализовать("# Справочник:") + " " + ИмяСправочника);
	Строки.Добавить("");

	Счетчик = 0;
	Для Каждого ТекСтрока Из Данные Цикл
		Счетчик = Счетчик + 1;
		
		Строки.Добавить(Ванесса.Локализовать("## Элемент") + " " + Формат(Счетчик, "ЧГ=0; ЧН=0"));
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("- Навигационная ссылка:") + " " + Строка(ТекСтрока.НавигационнаяСсылка));
		Строки.Добавить(Ванесса.Локализовать("- Наименование:") + " " + ТекСтрока.Наименование);
		Строки.Добавить(Ванесса.Локализовать("- Пометка удаления:") + " " + ТекСтрока.ПометкаУдаления);
		Если ТекСтрока.Свойство("Код") Тогда
			Строки.Добавить(Ванесса.Локализовать("- Код:") + " " + ТекСтрока.Код);
		КонецЕсли;
		Если ТекСтрока.Свойство("Владелец") Тогда
			Строки.Добавить(Ванесса.Локализовать("- Владелец:") + " " + Строка(ТекСтрока.Владелец));
		КонецЕсли;
		Строки.Добавить("");
	КонецЦикла;
	
	Если Счетчик = 0 Тогда
		Если ЗначениеЗаполнено(ОтборПоНаименованию) Тогда
			Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("Не найдено ни одного элемента с Наименованием `%1`."), ОтборПоНаименованию));
		Иначе	
			Строки.Добавить(Ванесса.Локализовать("Элементов справочника не найдено."));
		КонецЕсли;
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

// Возвращает данные документа
&НаКлиенте
Функция ПолучитьДанныеДокумента(ИмяДокумента, ДатаНачала, ДатаОкончания, Лимит)
	
	Если НЕ ЗначениеЗаполнено(ИмяДокумента) Тогда
		ТекстСообщения = Ванесса.Локализовать("Для типа объекта 'Документ' необходимо указать параметр object_name.");
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("INVALID_PARAMS", ТекстСообщения);
	КонецЕсли;
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.Локализовать("# Документ: ") + ИмяДокумента);
	Строки.Добавить("");
	
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("Тип", "Документ");
	ДопПараметры.Вставить("Вид", ИмяДокумента);
	ДопПараметры.Вставить("Лимит", Лимит);
	ДопПараметры.Вставить("ДатаНачала", ДатаНачала);
	ДопПараметры.Вставить("ДатаОкончания", ДатаОкончания);
	
	ТекстОшибки = Неопределено;
	Данные = ДанныеИзБазыДанныхКлиентаТестирования(ДопПараметры, ТекстОшибки);
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		Возврат МодульМсп_Сервер().ОшибкаИнструмента("ACTION_FAILED", ТекстОшибки);
	КонецЕсли;
	
	Если ТипЗнч(Данные) = Тип("Структура") Тогда
		ОшибкаНетОбъектаМетаданных = Данные.ОшибкаНетОбъектаМетаданных;
		Строки.Добавить(Ванесса.Локализовать("# В базе клиента тестирования нет документа:") + " " + ИмяДокумента);
		Строки.Добавить(Ванесса.Локализовать("# Доступные значения:"));
		Для Каждого ТекИмяОМ Из Данные.ВсеОбъекты Цикл
			Если Лев(НРег(ТекИмяОМ), 7) = "удалить" ИЛИ Лев(НРег(ТекИмяОМ), 6) = "delete" Тогда
				// Значит значение устарело
				Продолжить;
			КонецЕсли;
			Строки.Добавить("  - " + ТекИмяОМ);
		КонецЦикла;	
		Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	КонецЕсли;
	
	Счетчик = 0;
	Для Каждого ТекСтрока Из Данные Цикл
		Счетчик = Счетчик + 1;
		
		Строки.Добавить(Ванесса.Локализовать("## Документ") + " " + Формат(Счетчик, "ЧГ=0; ЧН=0"));
		Строки.Добавить("");
		Строки.Добавить(Ванесса.Локализовать("- Представление:") + " " + Строка(ТекСтрока.Ссылка));
		Строки.Добавить(Ванесса.Локализовать("- Навигационная ссылка:") + " " + Строка(ТекСтрока.НавигационнаяСсылка));
		Строки.Добавить(Ванесса.Локализовать("- Дата:") + " " + ТекСтрока.Дата);
		Если ТекСтрока.Свойство("Номер") Тогда
			Строки.Добавить(Ванесса.Локализовать("- Номер:") + " " + ТекСтрока.Номер);
		КонецЕсли;
		Строки.Добавить(Ванесса.Локализовать("- Пометка удаления:") + " " + ТекСтрока.ПометкаУдаления);
		Если ТекСтрока.Свойство("Проведен") Тогда
			Строки.Добавить(Ванесса.Локализовать("- Проведен:") + " " + ТекСтрока.Проведен);
		КонецЕсли;
		Строки.Добавить("");
	КонецЦикла;
	
	Если Счетчик = 0 Тогда
		Строки.Добавить(Ванесса.Локализовать("Документы не найдены."));
	КонецЕсли;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

&НаКлиенте
Функция ДанныеИзБазыДанныхКлиентаТестирования(ДопПараметры, ТекстОшибки)
	Ванесса.ОткрытьНавигационнуюСсылку("e1cib/app/Обработка.VAExtension_ПолучениеДанныхИзБазы");
	ТестовоеПриложение = Ванесса.ПолучитьТестовоеПриложение();
	НужнаяФорма = ТестовоеПриложение.НайтиОбъект(Тип("ТестируемаяФорма"),, "*.VAExtension_ПолучениеДанныхИзБазы.*",5);
	Если НужнаяФорма = Неопределено Тогда
		ТекстОшибки = Ванесса.Локализовать("Клиент тестирования подключен, но не найдена форма 'VAExtension_ПолучениеДанныхИзБазы' из служебного расширения.");
		Возврат Неопределено
	КонецЕсли;	
	
	РеквизитВыражениеВычислено = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ОбработкаВыполнена", Истина);
	РеквизитТип = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("Тип", Истина);
	РеквизитВид = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("Вид", Истина);
	РеквизитТип.ВвестиТекст(ДопПараметры.Тип);
	РеквизитВид.ВвестиТекст(ДопПараметры.Вид);
	
	Если ДопПараметры.Свойство("Владелец") Тогда
		РеквизитИмяВладельца = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ИмяВладельца", Истина);
		РеквизитИмяВладельца.ВвестиТекст(ДопПараметры.Владелец);
	КонецЕсли;
	
	Если ДопПараметры.Свойство("Лимит") Тогда
		РеквизитКоличествоСсылок = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("КоличествоСсылок", Истина);
		РеквизитКоличествоСсылок.ВвестиТекст(XMLСтрока(ДопПараметры.Лимит));
	КонецЕсли;
	
	Если ДопПараметры.Свойство("ОтборПоНаименованию") И ЗначениеЗаполнено(ДопПараметры.ОтборПоНаименованию) Тогда
		РеквизитОтборПоНаименованию = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ОтборПоНаименованию", Истина);
		РеквизитОтборПоНаименованию.ВвестиТекст(ДопПараметры.ОтборПоНаименованию);
	КонецЕсли;
	
	ДанныеДат = Новый Структура;
	ПереданыДаты = Ложь;
	Если ДопПараметры.Свойство("ДатаНачала") И ЗначениеЗаполнено(ДопПараметры.ДатаНачала) Тогда
		ПереданыДаты = Истина;
		ДанныеДат.Вставить("ДатаНачала", ДопПараметры.ДатаНачала);
	КонецЕсли;
	
	Если ДопПараметры.Свойство("ДатаОкончания") И ЗначениеЗаполнено(ДопПараметры.ДатаОкончания) Тогда
		ПереданыДаты = Истина;
		ДанныеДат.Вставить("ДатаОкончания", ДопПараметры.ДатаОкончания);
	КонецЕсли;
	
	АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
	Если ПереданыДаты Тогда
		РеквизитДляУстановкиПараметров = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ДляУстановкиПараметров", Истина);
		РеквизитДляУстановкиПараметров.ВвестиТекст(Ванесса.ЗаписатьОбъектJSON(ДанныеДат));
		КнопкаПолучитьПараметры = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяКнопкаФормы"),,"ФормаПолучитьПараметры");
		КнопкаПолучитьПараметры.Нажать();
	КонецЕсли;
	
	КнопкаВыполнитьОбработку = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяКнопкаФормы"),,"ФормаВыполнитьОбработку");
	
	КнопкаЗакрытьФорму = АктивноеОкно.НайтиОбъект(Тип("ТестируемаяКнопкаФормы"),,"ЗакрытьФорму");
	
	КнопкаВыполнитьОбработку.Нажать();
	
	БылаОшибка = Истина;
	Ванесса.ОжиданиеОкончанияОбработки(БылаОшибка, РеквизитВыражениеВычислено);
	
	Если БылаОшибка Тогда
		ТекстОшибки = Ванесса.Локализовать("Не вышло получить данные из базы данных клиента тестирования.");
		Возврат Неопределено
	КонецЕсли;
	
	РеквизитЗначениеДляВозврата = Ванесса.НайтиРеквизитОткрытойФормыПоЗаголовку("ЗначениеРезультат", Истина);
	ПредставлениеРезультата = Ванесса.ПолучитьПредставлениеДанныхЭлементаФормы(РеквизитЗначениеДляВозврата);
	
	Данные = Ванесса.ПрочитатьОбъектJSON(ПредставлениеРезультата);
	
	Попытка
		КнопкаЗакрытьФорму.Нажать();
	Исключение
		Ванесса.ЗаписатьЛогВЖРИнформация("ДанныеИзБазыДанныхКлиентаТестирования", ОписаниеОшибки());
	КонецПопытки;
	
	Возврат Данные;
	
КонецФункции	

&НаКлиенте
Функция ПреобразоватьПроблемыПроверкиСинтаксиса(Проблемы)
	МассивПроблем = Новый Массив;
	Попытка
		Если Проблемы <> Неопределено Тогда
			Для Каждого Проблема Из Проблемы Цикл
				ПроблемаJSON = Новый Структура;
				Если ТипЗнч(Проблема) = Тип("Структура") Тогда
					ПроблемаJSON.Вставить("line", MCPБезопасноеСвойство(Проблема, "НомерСтроки"));
					ПроблемаJSON.Вставить("message", MCPБезопасноеСвойство(Проблема, "Текст"));
				ИначеЕсли ТипЗнч(Проблема) = Тип("Соответствие") Тогда
					ЗначениеСтроки = Проблема["НомерСтроки"];
					Если ЗначениеСтроки = Неопределено Тогда
						ЗначениеСтроки = Проблема["line"];
					КонецЕсли;
					ПроблемаJSON.Вставить("line", ЗначениеСтроки);
					ЗначениеТекста = Проблема["Текст"];
					Если ЗначениеТекста = Неопределено Тогда
						ЗначениеТекста = Проблема["message"];
					КонецЕсли;
					ПроблемаJSON.Вставить("message", ЗначениеТекста);
				Иначе
					ПроблемаJSON.Вставить("message", Строка(Проблема));
				КонецЕсли;
				МассивПроблем.Добавить(ПроблемаJSON);
			КонецЦикла;
		КонецЕсли;
	Исключение
	КонецПопытки;
	
	Возврат МассивПроблем;
КонецФункции

&НаКлиенте
Функция ОшибкаПроверкиСинтаксиса(Проблемы)
	ТекстОшибки = Неопределено;
	ПутьКФайлу = ПутьКФайлуИзТекущегоФайлаВРедакторе(ТекстОшибки);
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		Возврат ТекстОшибки;
	КонецЕсли;
	
	МассивПроблем = ПреобразоватьПроблемыПроверкиСинтаксиса(Проблемы);
	
	Строки = Новый Массив;
	Строки.Добавить(Ванесса.ПодставитьПараметрыВСтроку(Ванесса.Локализовать("## Обнаружены ошибки проверки синтаксиса файла '%1'"), ПутьКФайлу));
	Строки.Добавить("");

	НомерПроблемы = 0;
	Для Каждого Проблема Из МассивПроблем Цикл
		НомерПроблемы = НомерПроблемы + 1;
		СтрокаПроблемы = Строка(НомерПроблемы) + ". ";
		Если Проблема["line"] <> Неопределено Тогда
			СтрокаПроблемы = СтрокаПроблемы + Ванесса.Локализовать("Строка") + " " + Строка(Проблема["line"]) + ": ";
		КонецЕсли;
		СтрокаПроблемы = СтрокаПроблемы + MCPПредставлениеЗначенияДляMarkdown(Проблема["message"]);
		Строки.Добавить(СтрокаПроблемы);
	КонецЦикла;
	
	Возврат Ванесса._СтрСоединить(Строки, Символы.ПС);
	
КонецФункции

&НаКлиенте
Функция СтрокаДляОшибкиТекстМодальногоДиалога()
	Возврат Ванесса.Локализовать("Текст модального диалога:");
КонецФункции

&НаКлиенте
Процедура ДобавитьВМассивСтрокДанныеОбАктивномОкне(МассивСтрок)
	Попытка
		АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
		ТекстСтроки = Ванесса.Локализовать("Заголовок активного окна:") + " " + АктивноеОкно.Заголовок;
		МассивСтрок.Добавить(ТекстСтроки);
		Если Ванесса.ЭтоМодальныйДиалог(АктивноеОкно) Тогда
			МассивСтрок.Добавить(Ванесса.Локализовать("Обнаружен модальный диалог."));
			ТекСообщение = Ванесса._СтрСоединить(МассивСтрок, Символы.ПС);
			Если Найти(ТекСообщение, СтрокаДляОшибкиТекстМодальногоДиалога()) = 0 Тогда
				ДобавитьВОтветИнформациюОМодальномДиалоге(МассивСтрок, АктивноеОкно);
			КонецЕсли;
		КонецЕсли;
		
		Попытка
			CообщенияПользователю = ПолучитьСообщенияПользователюИзОкна(АктивноеОкно);
			Если CообщенияПользователю <> Неопределено Тогда
				МассивСтрок.Добавить("");
				МассивСтрок.Добавить(Ванесса.Локализовать("## В окне есть сообщения пользователю:"));
				МассивСтрок.Добавить(Ванесса._СтрСоединить(CообщенияПользователю, Символы.ПС));
			КонецЕсли;
		Исключение
			
		КонецПопытки;
		
	Исключение
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьКешДанныхУпавшегоШагаИСказатьИИСнятьСкриншот(НомерСтрокиВФиче, МассивСтрок, НадоСнятьСкриншот)
	Если КешДанныхУпавшегоШага = Неопределено Тогда
		КешДанныхУпавшегоШага = Новый Структура;
	КонецЕсли;
	
	Если НЕ КешДанныхУпавшегоШага.Свойство("НомерСтрокиУпавшегоШага") Тогда
		КешДанныхУпавшегоШага.Вставить("НомерСтрокиУпавшегоШага", НомерСтрокиВФиче);
		КешДанныхУпавшегоШага.Вставить("КоличествоПадений", 1);
	Иначе
		Если КешДанныхУпавшегоШага.НомерСтрокиУпавшегоШага = НомерСтрокиВФиче Тогда
			КешДанныхУпавшегоШага.КоличествоПадений = КешДанныхУпавшегоШага.КоличествоПадений + 1;
		Иначе	
			КешДанныхУпавшегоШага.Вставить("НомерСтрокиУпавшегоШага", НомерСтрокиВФиче);
			КешДанныхУпавшегоШага.Вставить("КоличествоПадений", 1);
		КонецЕсли;
	КонецЕсли;
	
	Если КешДанныхУпавшегоШага.КоличествоПадений >= ПорогКоличествоОшибокШагаКогдаНадоСнятьСкриншот() Тогда
		// Значит надо снять скриншот активного окна
		НадоСнятьСкриншот = Истина;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СнятьСкришотыОконКлиентаТестированияИОтправитьИхИИДляАнализаПослеВыполненияСценария(МассивСтрок)
	
	КаталогДляСкриншотов = ПолучитьКаталогДляСкриншотов();
   	ФайлыСкриншотовДляАнализа = Новый Массив;
	Если КаталогДляСкриншотов <> Неопределено Тогда
		
		Если НЕ Ванесса.ФайлСуществуетКомандаСистемы(КаталогДляСкриншотов) Тогда
			Ванесса.СоздатьКаталогКомандаСистемы(КаталогДляСкриншотов);
		КонецЕсли;
		
		// Реальная проверка подключения клиента тестирования
		Попытка
			АктивноеОкно = Ванесса.ПолучитьАктивноеОкноИзТестовоеПриложение();
		Исключение
			Возврат;
		КонецПопытки;
		
		ProcessId = Ванесса.PIDТекущегоКлиентаТестирования();
		Если НЕ ЗначениеЗаполнено(ProcessId) Тогда
			Возврат;
		КонецЕсли;	
		
		ДопПараметры = Новый Структура;
		ДопПараметры.Вставить("ProcessId", ProcessId);
		Попытка
			РезультатВызова = Ванесса.ВнешняяКомпонентаДляСкриншотов.ПолучитьСписокОкон(ProcessId);
			ОкнаДляСкриншотов = ПолучитьОкнаДляСкриншотов(РезультатВызова);
		Исключение
			ОкнаДляСкриншотов = Новый Массив;
		КонецПопытки;
		Если ОкнаДляСкриншотов.Количество() = 0 Тогда
			Возврат;
		КонецЕсли;
		
		Для Каждого ТекОкно Из ОкнаДляСкриншотов Цикл
			Попытка
				ДвоичныеДанныеИзображения = Ванесса.ВнешняяКомпонентаДляСкриншотов.ПолучитьСнимокОкна(ТекОкно.ПолныеДанныеОкна.window);
				ДополнительныеПараметры = Новый Структура;
				ДополнительныеПараметры.Вставить("ЦветовойРежим", "grayscale");
				ДополнительныеПараметры.Вставить("ИмяФайла", Ванесса.ДополнитьСлешВПуть(КаталогДляСкриншотов) + XMLСтрока(ТекущаяУниверсальнаяДатаВМиллисекундах()) + ".png");
				ИмяФайла = ПолучитьФайлКартинкиИзДвоичныхДанных(ДвоичныеДанныеИзображения, ДополнительныеПараметры);
				ФайлыСкриншотовДляАнализа.Добавить(ИмяФайла);
			Исключение
				Продолжить;
			КонецПопытки;
		КонецЦикла;
	КонецЕсли;
	
	Если ФайлыСкриншотовДляАнализа.Количество() > 0 Тогда
		МассивСтрок.Добавить("");
		МассивСтрок.Добавить(Ванесса.Локализовать("## Чтобы понять причину ошибки прочитай файлы скриншотов клиента тестирования:"));
		Для Каждого ТекИмяФайла Из ФайлыСкриншотовДляАнализа Цикл
			МассивСтрок.Добавить("  -" + ТекИмяФайла);
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ПолучитьФайлКартинкиИзДвоичныхДанных(ДвоичныеДанныеИзображения, ДополнительныеПараметры)
	//Обработка картинки для уменьшения размера передаваемых данных
	Картинка = Новый Картинка(ДвоичныеДанныеИзображения);
	//Картинка.Записать("c:\temp\Исходная.png");
	
	// Получаем режим обработки изображения из дополнительных параметров
	ЦветовойРежим = "grayscale"; // по умолчанию
	Если ДополнительныеПараметры.Свойство("ЦветовойРежим") Тогда
		ЦветовойРежим = ДополнительныеПараметры.ЦветовойРежим;
	КонецЕсли;
	
	ИмяФайла = ДополнительныеПараметры.ИмяФайла;
	Если НЕ ЗначениеЗаполнено(ИмяФайла) Тогда
		ИмяФайла = ПолучитьИмяВременногоФайла("png");
	КонецЕсли;
	
	Если ЦветовойРежим = "grayscale" Тогда
		ОбработаннаяКартинка = ОбработаннаяКартинкаСервер(Картинка);
		ОбработаннаяКартинка.Записать(ИмяФайла);
	Иначе	
		Картинка.Записать(ИмяФайла);
	КонецЕсли;
	
	Возврат ИмяФайла;
	
КонецФункции

&НаКлиенте
Функция ПолучитьКаталогДляСкриншотов()
	ПутьКФайлу = Ванесса.Объект.КаталогФич;
	
	Если НЕ Ванесса.ФайлСуществуетКомандаСистемы(ПутьКФайлу) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если Ванесса.ЭтоКаталогКомандаСистемы(Ванесса.Объект.КаталогФич) Тогда
		Возврат Ванесса.ДополнитьСлешВПуть(Ванесса.Объект.КаталогФич) + "png";
	Иначе
		Файл = Новый Файл(Ванесса.Объект.КаталогФич);
		Возврат Ванесса.ДополнитьСлешВПуть(Файл.Путь) + "png";
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Функция ПорогКоличествоОшибокШагаКогдаНадоСнятьСкриншот()
	Возврат 2;
КонецФункции

&НаКлиенте
Функция ДляШагаНадоВсегдаСниматьСкриншот(СтрокаУпавшегоШага)
	НРегСтрокаРеальнойПроцедуры = НРег(СтрокаУпавшегоШага.СтрокаРеальнойПроцедуры);
	Если НРегСтрокаРеальнойПроцедуры = "втаблицеколичествострок" Тогда
		Возврат Истина;
	ИначеЕсли НРегСтрокаРеальнойПроцедуры = "втаблицеяперехожукстроке" Тогда
		Возврат Истина;
	ИначеЕсли НРегСтрокаРеальнойПроцедуры = "яждузакрытияокнавтечениесекунд" Тогда
		Возврат Истина;
	ИначеЕсли НРегСтрокаРеальнойПроцедуры = "втаблицеяперехожукследующейстроке" Тогда
		Возврат Истина;
	ИначеЕсли НРегСтрокаРеальнойПроцедуры = "втаблицеяперехожукследующейстроке" Тогда
		Возврат Истина;
	ИначеЕсли НРегСтрокаРеальнойПроцедуры = "втаблицеяперехожукпредыдущейстроке" Тогда
		Возврат Истина;
	КонецЕсли;
	
	
	Возврат Ложь;
КонецФункции

&НаКлиенте
Процедура ДобавитьВСообщениеОкнаДоступныеДляСкриншота(Строки, ОкнаДляСкриншотов)
	Строки.Добавить(Ванесса.Локализовать("## Окна клиента тестирования доступные для снятия скриншота:"));
	Для Каждого ТекОкно Из ОкнаДляСкриншотов Цикл
		Если НЕ ТекОкно.ПолныеДанныеОкна.Свойство("Title") Тогда
			Продолжить;
		КонецЕсли;
		Строки.Добавить("  -" + ТекОкно.ПолныеДанныеОкна.Title);
	КонецЦикла;
КонецПроцедуры

// Нужно, чтобы сработали обрабтчики в форме, чтобы после нажатия на кнопку получить правильное активное окно
&НаКлиенте
Функция ПослеНажатияНаКнопкуНадоСделатьПаузу(ИмяКнопки)
	ИмяКнопки_НРег = НРег(ИмяКнопки);
	Если Найти(ИмяКнопки_НРег, "записать") > 0 Тогда
		Возврат Истина;
	ИначеЕсли Найти(ИмяКнопки_НРег, "провести") > 0 Тогда
		Возврат Истина;
	ИначеЕсли Найти(ИмяКнопки_НРег, "save") > 0 Тогда
		Возврат Истина;
	ИначеЕсли Найти(ИмяКнопки_НРег, "post") > 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	Возврат Ложь;
КонецФункции

#КонецОбласти
