//начало текста модуля
&НаКлиенте
Перем Ванесса;

// Делает отключение модуля
&НаКлиенте
Функция ОтключениеМодуля() Экспорт

	Ванесса = Неопределено;
	Контекст = Неопределено;
	КонтекстСохраняемый = Неопределено;

КонецФункции

&НаКлиенте
Функция ПолучитьСписокТестов(КонтекстФреймворкаBDD) Экспорт
	Ванесса = КонтекстФреймворкаBDD;
	
	ВсеТесты = Новый Массив;

	//описание параметров
	//ДобавитьШагВМассивТестов(ВсеТесты,Снипет,ИмяПроцедуры,ПредставлениеТеста,Транзакция,Параметр);

	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"МнеДоступнаРоль(Парам01)","МнеДоступнаРоль","И мне доступна роль ""Администратор""", "Проверяет доступность роли. Проверка происходит на стороне менеджера тестирования. Имя роли должно быть указано также как в конфигураторе.", "Прочее.Права.Проверка доступности роли");
	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"МнеНеДоступнаРоль(Парам01)","МнеНеДоступнаРоль","И мне не доступна роль ""Администратор""", "Проверяет недоступность роли. Проверка происходит на стороне менеджера тестирования. Имя роли должно быть указано также как в конфигураторе.", "Прочее.Права.Проверка доступности роли");

	Возврат ВсеТесты;
КонецФункции

&НаКлиенте
Функция ПередНачаломСценария() Экспорт
	
КонецФункции

&НаКлиенте
Функция ПередОкончаниемСценария() Экспорт
	
КонецФункции

&НаСервереБезКонтекста
Функция РольДоступнаСервер(ИмяРоли)
	Возврат РольДоступна(ИмяРоли);
КонецФункции	

&НаКлиенте
//Мне доступна роль "Админ"
//@МнеДоступнаРоль(Парам01)
Функция МнеДоступнаРоль(ИмяРоли) Экспорт
	РольДоступна = РольДоступнаСервер(ИмяРоли);
	Ванесса.ПроверитьРавенство(РольДоступна,Истина,"Роль должна быть " + ИмяРоли + " доступна");
КонецФункции

&НаКлиенте
//Мне не доступна роль "НедоступнаяРоль"
//@МнеНеДоступнаРоль(Парам01)
Функция МнеНеДоступнаРоль(ИмяРоли) Экспорт
	РольДоступна = РольДоступнаСервер(ИмяРоли);
	Ванесса.ПроверитьРавенство(РольДоступна,Ложь,"Роль должна быть " + ИмяРоли + " доступна");
КонецФункции

//окончание текста модуля