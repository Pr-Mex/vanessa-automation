//начало текста модуля

///////////////////////////////////////////////////
//Служебные функции и процедуры
///////////////////////////////////////////////////

&НаКлиенте
// контекст фреймворка Vanessa-Behavior
Перем Ванесса;
 
&НаКлиенте
// Структура, в которой хранится состояние сценария между выполнением шагов. Очищается перед выполнением каждого сценария.
Перем Контекст Экспорт;
 
&НаКлиенте
// Структура, в которой можно хранить служебные данные между запусками сценариев. Существует, пока открыта форма Vanessa-Behavior.
Перем КонтекстСохраняемый Экспорт;

&НаКлиенте
// Функция экспортирует список шагов, которые реализованы в данной внешней обработке.
Функция ПолучитьСписокТестов(КонтекстФреймворкаBDD) Экспорт
	Ванесса = КонтекстФреймворкаBDD;
	
	ВсеТесты = Новый Массив;

	//описание параметров
	//Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,Снипет,ИмяПроцедуры,ПредставлениеТеста,ОписаниеШага,ТипШага,Транзакция,Параметр);

	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ВСтруктуреСценарияЕстьШагСОбычнымПараметромИСПараметромИЗначением(Парам01,Параметр,Значение)","ВСтруктуреСценарияЕстьШагСОбычнымПараметромИСПараметромИЗначением","И в структуре сценария есть шаг с обычным параметром ""ОбычныйПараметр"" и с параметром <Параметр> и значением <Значение>","","");

	Возврат ВсеТесты;
КонецФункции
	
&НаСервере
// Служебная функция.
Функция ПолучитьМакетСервер(ИмяМакета)
	ОбъектСервер = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектСервер.ПолучитьМакет(ИмяМакета);
КонецФункции
	
&НаКлиенте
// Служебная функция для подключения библиотеки создания fixtures.
Функция ПолучитьМакетОбработки(ИмяМакета) Экспорт
	Возврат ПолучитьМакетСервер(ИмяМакета);
КонецФункции



///////////////////////////////////////////////////
//Работа со сценариями
///////////////////////////////////////////////////

&НаКлиенте
// Процедура выполняется перед началом каждого сценария
Процедура ПередНачаломСценария() Экспорт
	
КонецПроцедуры

&НаКлиенте
// Процедура выполняется перед окончанием каждого сценария
Процедура ПередОкончаниемСценария() Экспорт
	
КонецПроцедуры



///////////////////////////////////////////////////
//Реализация шагов
///////////////////////////////////////////////////

&НаКлиенте
//И в структуре сценария есть шаг с обычным параметром "ОбычныйПараметр" и с параметром <Параметр> и значением <Значение>
//@ВСтруктуреСценарияЕстьШагСОбычнымПараметромИСПараметромИЗначением(Парам01,Параметр,Значение)
Процедура ВСтруктуреСценарияЕстьШагСОбычнымПараметромИСПараметромИЗначением(ОбычныйПараметр,Параметр,Значение) Экспорт
	КонтекстСохраняемый.Вставить("СлужебныйОбычныйПараметр",ОбычныйПараметр);
	
	Если Не КонтекстСохраняемый.Свойство("СлужебнаяПеременная") Тогда
		КонтекстСохраняемый.Вставить("СлужебнаяПеременная",0);
	КонецЕсли;	 
	
	КонтекстСохраняемый.СлужебнаяПеременная = КонтекстСохраняемый.СлужебнаяПеременная + Значение;
КонецПроцедуры

//окончание текста модуля