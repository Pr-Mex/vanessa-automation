//начало текста модуля

///////////////////////////////////////////////////
//Служебные функции и процедуры
///////////////////////////////////////////////////

&НаКлиенте
// контекст фреймворка Vanessa-Behavior
Перем Ванесса;
 
&НаКлиенте
// Структура, в которой хранится состояние сценария между выполнением шагов. Очищается перед выполнением каждого сценария.
Перем Контекст Экспорт;
 
&НаКлиенте
// Структура, в которой можно хранить служебные данные между запусками сценариев. Существует, пока открыта форма Vanessa-Behavior.
Перем КонтекстСохраняемый Экспорт;

&НаКлиенте
// Функция экспортирует список шагов, которые реализованы в данной внешней обработке.
Функция ПолучитьСписокТестов(КонтекстФреймворкаBDD) Экспорт
	Ванесса = КонтекстФреймворкаBDD;
	
	ВсеТесты = Новый Массив;

	//описание параметров
	//Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,Снипет,ИмяПроцедуры,ПредставлениеТеста,ОписаниеШага,ТипШага,Транзакция,Параметр);

	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ВЭтомШагеЯПринимаюСтроковыйПараметрИТаблицу(Парам01,ТабПарам)","ВЭтомШагеЯПринимаюСтроковыйПараметрИТаблицу","И в этом шаге я принимаю строковый параметр ""Параметр"" и таблицу","","");
	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ВЭтомШагеЯПринимаюСтроковыйПараметрИТриТаблицы(Парам01,ТабПарам1,ТабПарам2,ТабПарам3)","ВЭтомШагеЯПринимаюСтроковыйПараметрИТриТаблицы","И в этом шаге я принимаю строковый параметр ""Параметр"" и три таблицы","","");

	Возврат ВсеТесты;
КонецФункции
	
&НаСервере
// Служебная функция.
Функция ПолучитьМакетСервер(ИмяМакета)
	ОбъектСервер = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектСервер.ПолучитьМакет(ИмяМакета);
КонецФункции
	
&НаКлиенте
// Служебная функция для подключения библиотеки создания fixtures.
Функция ПолучитьМакетОбработки(ИмяМакета) Экспорт
	Возврат ПолучитьМакетСервер(ИмяМакета);
КонецФункции



///////////////////////////////////////////////////
//Работа со сценариями
///////////////////////////////////////////////////

&НаКлиенте
// Процедура выполняется перед началом каждого сценария
Процедура ПередНачаломСценария() Экспорт
	
КонецПроцедуры

&НаКлиенте
// Процедура выполняется перед окончанием каждого сценария
Процедура ПередОкончаниемСценария() Экспорт
	
КонецПроцедуры



///////////////////////////////////////////////////
//Реализация шагов
///////////////////////////////////////////////////

&НаКлиенте
//И в этом шаге я принимаю строковый параметр "Параметр" и таблицу
//@ВЭтомШагеЯПринимаюСтроковыйПараметрИТаблицу(Парам01,ТабПарам)
Процедура ВЭтомШагеЯПринимаюСтроковыйПараметрИТаблицу(Парам01,ТабПарам) Экспорт
	Для Каждого СтрТабПарам Из ТабПарам Цикл
		КонтекстСохраняемый.Вставить(СтрТабПарам.Кол1,СтрТабПарам.Кол2);
	КонецЦикла;	
КонецПроцедуры

//окончание текста модуля

&НаКлиенте
//И в этом шаге я принимаю строковый параметр "Параметр" и три таблицы
//@ВЭтомШагеЯПринимаюСтроковыйПараметрИТриТаблицы(Парам01,ТабПарам1,ТабПарам2,ТабПарам3)
Процедура ВЭтомШагеЯПринимаюСтроковыйПараметрИТриТаблицы(Парам01,ТабПарам1,ТабПарам2,ТабПарам3) Экспорт
	Для Каждого СтрТабПарам Из ТабПарам1 Цикл
		КонтекстСохраняемый.Вставить(СтрТабПарам.Кол1,СтрТабПарам.Кол2);
	КонецЦикла;	
	Для Каждого СтрТабПарам Из ТабПарам2 Цикл
		КонтекстСохраняемый.Вставить(СтрТабПарам.Кол1,СтрТабПарам.Кол2);
	КонецЦикла;	
	Для Каждого СтрТабПарам Из ТабПарам3 Цикл
		КонтекстСохраняемый.Вставить(СтрТабПарам.Кол1,СтрТабПарам.Кол2);
	КонецЦикла;	
КонецПроцедуры
