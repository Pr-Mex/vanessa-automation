//начало текста модуля

///////////////////////////////////////////////////
//Служебные функции и процедуры
///////////////////////////////////////////////////

&НаКлиенте
// контекст фреймворка Vanessa-Behavior
Перем Ванесса;
 
&НаКлиенте
// Структура, в которой хранится состояние сценария между выполнением шагов. Очищается перед выполнением каждого сценария.
Перем Контекст Экспорт;
 
&НаКлиенте
// Структура, в которой можно хранить служебные данные между запусками сценариев. Существует, пока открыта форма Vanessa-Behavior.
Перем КонтекстСохраняемый Экспорт;

// Делает отключение модуля
&НаКлиенте
Функция ОтключениеМодуля() Экспорт

	Ванесса = Неопределено;
	Контекст = Неопределено;
	КонтекстСохраняемый = Неопределено;

КонецФункции

&НаКлиенте
// Функция экспортирует список шагов, которые реализованы в данной внешней обработке.
Функция ПолучитьСписокТестов(КонтекстФреймворкаBDD) Экспорт
	Ванесса = КонтекстФреймворкаBDD;
	
	ВсеТесты = Новый Массив;

	//описание параметров
	//ДобавитьШагВМассивТестов(ВсеТесты,Снипет,ИмяПроцедуры,ПредставлениеТеста,Транзакция,Параметр);

	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ЯЗадаюСистемныеПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол(Парам01,Парам02,Парам03,Парам04)","ЯЗадаюСистемныеПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол","Когда Я задаю системные параметры считывания макета НачСтр 1 НачКол 1 КолСтр 100 КолКол 20");
	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ЯЗадаюПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол(Парам01,Парам02,Парам03,Парам04)","ЯЗадаюПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол","Когда Я задаю параметры считывания макета НачСтр 1 НачКол 1 КолСтр 100 КолКол 20");
	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ЯЗадаюПараметрыЧтенияОбластиМакета(Парам01)","ЯЗадаюПараметрыЧтенияОбластиМакета","Когда Я задаю параметры чтения области макета ""R1C1:R100C20""","Указывает область чтения макета, когда он считывается по одной ячейке из данных TestClient","UI.Табличный документ.Область чтения");
	
	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ЯУстанавливаюТекущемуСеансуЗаголовокПриложения(Парам01)","ЯУстанавливаюТекущемуСеансуЗаголовокПриложения","И я устанавливаю текущему сеансу заголовок приложения ""Управляющий сеанс""","Устанавливает текущему сеансу 1С нужный заголовок приложения.","Прочее.Установить заголовок приложения");

	Возврат ВсеТесты;
КонецФункции
	
&НаСервере
// Служебная функция.
Функция ПолучитьМакетСервер(ИмяМакета)
	ОбъектСервер = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектСервер.ПолучитьМакет(ИмяМакета);
КонецФункции
	
&НаКлиенте
// Служебная функция для подключения библиотеки создания fixtures.
Функция ПолучитьМакетОбработки(ИмяМакета) Экспорт
	Возврат ПолучитьМакетСервер(ИмяМакета);
КонецФункции



///////////////////////////////////////////////////
//Работа со сценариями
///////////////////////////////////////////////////

&НаКлиенте
// Функция выполняется перед началом каждого сценария
Функция ПередНачаломСценария() Экспорт
	
КонецФункции

&НаКлиенте
// Функция выполняется перед окончанием каждого сценария
Функция ПередОкончаниемСценария() Экспорт
	
КонецФункции



///////////////////////////////////////////////////
//Реализация шагов
///////////////////////////////////////////////////

&НаКлиенте
//Когда Я задаю системные параметры считывания макета НачСтр 1 НачКол 1 КолСтр 100 КолКол 20
//@ЯЗадаюСистемныеПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол(Парам01,Парам02,Парам03,Парам04)
Функция ЯЗадаюСистемныеПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол(НачСтр,НачКол,КолСтр,КолКол) Экспорт
	Ванесса.СчитываниеМакетаНачСтр = НачСтр;
	Ванесса.СчитываниеМакетаНачКол = НачКол;
	Ванесса.СчитываниеМакетаКолСтр = КолСтр;
	Ванесса.СчитываниеМакетаКолКол = КолКол;
КонецФункции

&НаКлиенте
//Когда Я задаю системные параметры считывания макета НачСтр 1 НачКол 1 КолСтр 100 КолКол 20
//@ЯЗадаюПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол(Парам01,Парам02,Парам03,Парам04)
Функция ЯЗадаюПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол(НачСтр,НачКол,КолСтр,КолКол) Экспорт
	ЯЗадаюСистемныеПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол(НачСтр,НачКол,КолСтр,КолКол);
КонецФункции

&НаКлиенте
//Когда Я задаю параметры чтения области макета "R1C1:R100C20"
//@ЯЗадаюПараметрыЧтенияОбластиМакета(Парам01)
Функция ЯЗадаюПараметрыЧтенияОбластиМакета(ИмяОбласти) Экспорт
	НачСтр = -1;
	НачКол = -1;
	КолСтр = -1;
	КолКол = -1;
	Попытка
		Ванесса.ПолучитьПараметрыОбластиМакета(ИмяОбласти,НачСтр,НачКол,КолСтр,КолКол);
	Исключение
		ТекстСообщения = Ванесса.ПолучитьТекстСообщенияПользователю("Не верный формат области <%1>. %2");
		ТекстСообщения = СтрЗаменить(ТекстСообщения,"%1",ИмяОбласти);
		ТекстСообщения = СтрЗаменить(ТекстСообщения,"%2",ОписаниеОшибки());
		ВызватьИсключение ТекстСообщения;
	КонецПопытки;
	
	ЯЗадаюСистемныеПараметрыСчитыванияМакетаНачСтрНачКолКолСтрКолКол(НачСтр,НачКол,КолСтр,КолКол);
КонецФункции

&НаКлиенте
//И я устанавливаю текущему сеансу заголовок приложения "Управляющий сеанс"
//@ЯУстанавливаюТекущемуСеансуЗаголовокПриложения(Парам01)
Функция ЯУстанавливаюТекущемуСеансуЗаголовокПриложения(ТекстЗаголовка) Экспорт
	УстановитьЗаголовокПриложения(ТекстЗаголовка);
КонецФункции
