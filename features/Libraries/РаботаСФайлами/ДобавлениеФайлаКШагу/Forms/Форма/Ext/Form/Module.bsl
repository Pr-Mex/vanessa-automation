
///////////////////////////////////////////////////
//Служебные функции и процедуры
///////////////////////////////////////////////////

&НаКлиенте
// контекст фреймворка Vanessa-Automation
Перем Ванесса;

&НаКлиенте
// Структура, в которой хранится состояние сценария между выполнением шагов. Очищается перед выполнением каждого сценария.
Перем Контекст Экспорт;

&НаКлиенте
// Структура, в которой можно хранить служебные данные между запусками сценариев. Существует, пока открыта форма Vanessa-Automation.
Перем КонтекстСохраняемый Экспорт;

// Делает отключение модуля
&НаКлиенте
Функция ОтключениеМодуля() Экспорт

	Ванесса = Неопределено;
	Контекст = Неопределено;
	КонтекстСохраняемый = Неопределено;

КонецФункции

&НаКлиенте
// Функция экспортирует список шагов, которые реализованы в данной внешней обработке.
Функция ПолучитьСписокТестов(КонтекстФреймворкаBDD) Экспорт
	
	Ванесса = КонтекстФреймворкаBDD;

	ВсеТесты = Новый Массив;

	//описание параметров
	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ЯДобавляюКШагуФайлИзСТипом(Парам01, Парам02)","ЯДобавляюКШагуФайлИзСТипом",
		"И Я добавляю к шагу файл из ""ИмяФайла"" с типом ""ТипФайла""",
		"### Описание
		|Добавляет файл к текущему шагу. Файл появится в отчете Allure. Также он будет добавлен в список файлов ошибки в формате Json.
		|Шаг нужен, чтобы в отчёте осталось содержимое файла: на машине сборки файлов после прогона уже нет, а из лога видно только, что файл не совпал с эталоном.
		|Первый параметр — полный путь к файлу, второй — его тип. Тип уходит в поле `type` вложения Allure как есть; по-хорошему это MIME-тип (`text/plain`, `application/json`, `image/png`), но в фичах VA передают и короткие значения вроде `feature` или `json`.
		|Прикрепляется файл к шагу, на котором стоит вызов, поэтому ставить его нужно рядом с проверкой, а не в конце сценария.
		|Файл уходит в отчёт Allure, если он включён, и в json-лог ошибок, если включён лог ошибок в текстовый файл; когда выключено и то и другое, шаг отработает, но файл никуда не попадёт.
		|### Пример. Приложить проверяемый файл к отчёту
		|```Gherkin
		|	И я получаю данные файла '*.json' в каталоге 'C:\Temp\Выгрузка'
		|	И Я добавляю к шагу файл из '$_ПолноеИмя$' с типом 'json'
		|	И файл '$_ПолноеИмя$' содержит строки
		|		| 'Заказ клиента' |
		|```
		|Если проверка упадёт, в отчёте рядом с ней будет лежать сам файл — видно, что в нём оказалось вместо ожидаемого.
		|",
		"Файлы.Прикрепление файлов к отчету Allure или файлу ошибки в формате Json");
	
	Возврат ВсеТесты;
	
КонецФункции

///////////////////////////////////////////////////
//Работа со сценариями
///////////////////////////////////////////////////

&НаКлиенте
// Функция выполняется перед началом каждого сценария
Функция ПередНачаломСценария() Экспорт

КонецФункции

&НаКлиенте
// Функция выполняется перед окончанием каждого сценария
Функция ПередОкончаниемСценария() Экспорт

КонецФункции

///////////////////////////////////////////////////
//Реализация шагов
///////////////////////////////////////////////////

&НаКлиенте
//И Я добавляю к шагу файл из "ИмяПеременной" с типом "ТипФайла"
//@ЯДобавляюКШагуФайлИзСТипом(Парам01, Парам02)
Функция ЯДобавляюКШагуФайлИзСТипом(ПолноеИмяФайла, ТипФайла) Экспорт
	
	Ванесса.ДобавитьФайлКШагу(ПолноеИмяФайла, ТипФайла);
	
КонецФункции