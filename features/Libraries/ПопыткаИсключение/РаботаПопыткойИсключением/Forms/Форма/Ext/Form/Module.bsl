
///////////////////////////////////////////////////
//Служебные функции и процедуры
///////////////////////////////////////////////////

&НаКлиенте
// контекст фреймворка Vanessa-Automation
Перем Ванесса;
 
&НаКлиенте
// Структура, в которой хранится состояние сценария между выполнением шагов. Очищается перед выполнением каждого сценария.
Перем Контекст Экспорт;
 
&НаКлиенте
// Структура, в которой можно хранить служебные данные между запусками сценариев. Существует, пока открыта форма Vanessa-Automation.
Перем КонтекстСохраняемый Экспорт;

// Делает отключение модуля
&НаКлиенте
Функция ОтключениеМодуля() Экспорт

	Ванесса = Неопределено;
	Контекст = Неопределено;
	КонтекстСохраняемый = Неопределено;

КонецФункции

&НаКлиенте
// Функция экспортирует список шагов, которые реализованы в данной внешней обработке.
Функция ПолучитьСписокТестов(КонтекстФреймворкаBDD) Экспорт
	Ванесса = КонтекстФреймворкаBDD;
	
	ВсеТесты = Новый Массив;

	//описание параметров
	//Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,Снипет,ИмяПроцедуры,ПредставлениеТеста,ОписаниеШага,ТипШага,Транзакция,Параметр);

	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,
		"ЯРегистрируюОшибкуПоДаннымИсключения(Парам01)",
		"ЯРегистрируюОшибкуПоДаннымИсключения",
		"И я регистрирую ошибку ""Описание ошибки"" по данным исключения",
		"### Описание
		|Должен использоваться внутри секции ""Исключение"". Фиксирует данные об ошибке, чтобы они потом они отобразились в отчетах и в системах управления ошибками. Параметр ""Описание ошибки"" будет использоваться как идентификатор ошибки в отчетах Allure и подобных.
		|Сам шаг помечается как упавший — так падение видно в дереве тестов и в отчётах, хотя сценарий продолжает выполняться. В описании ошибки работает подстановка переменных через `$ИмяПеременной$`.
		|### Пример. Сценарий продолжается, но ошибка попадает в отчёт
		|Шаг внутри `Попытка` падает, управление уходит в `Исключение`, ошибка регистрируется под своим именем — и сценарий идёт дальше. В окно сообщений попадёт `Ошибка зарегистрирована, идём дальше`.
		|```Gherkin
		|	И я запоминаю строку 'ТестоваяОшибка' в переменную 'ИмяПерем'
		|	Попытка
		|		Тогда я вызываю исключение 'ТестовоеИсключение'
		|	Исключение
		|		И я регистрирую ошибку 'Некоторая $ИмяПерем$' по данным исключения
		|	И я запоминаю строку 'Ошибка зарегистрирована, идём дальше' в переменную 'РезультатПроверки'
		|	И я вывожу значение переменной 'РезультатПроверки'
		|```
		|",
		"Прочее.ПопыткаИсключение");

	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,
		"ЯЗапоминаюТекстОшибкиВПеременную(Парам01)",
		"ЯЗапоминаюТекстОшибкиВПеременную",
		"И я запоминаю текст ошибки в переменную ""ИмяПеременной""",
		"### Описание
		|Запоминает текст ошибки, возникшей в конструкции Попытка/Исключения или в шаге, проверяющем другой шаг на исключение в переменную.
		|Дальше переменную можно проверить обычными шагами — например, убедиться, что упало именно то, что ожидалось, а не что-нибудь другое.
		|### Пример. Проверка текста возникшего исключения
		|В переменной окажется текст исключения, поэтому в окно сообщений попадёт `Текст исключения` и следом `Упало по нужной причине`.
		|```Gherkin
		|	Попытка
		|		И я вызываю исключение 'Текст исключения'
		|	Исключение
		|		И я запоминаю текст ошибки в переменную 'ТекстОшибки'
		|	И я вывожу значение переменной 'ТекстОшибки'
		|	Если 'Найти(Контекст.ТекстОшибки, ""Текст исключения"") > 0' Тогда
		|		И я запоминаю строку 'Упало по нужной причине' в переменную 'РезультатПроверки'
		|	Иначе
		|		И я запоминаю строку 'Упало по другой причине' в переменную 'РезультатПроверки'
		|	И я вывожу значение переменной 'РезультатПроверки'
		|```
		|",
		"Прочее.ПопыткаИсключение");


	Возврат ВсеТесты;
КонецФункции
	
&НаСервере
// Служебная функция.
Функция ПолучитьМакетСервер(ИмяМакета)
	ОбъектСервер = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектСервер.ПолучитьМакет(ИмяМакета);
КонецФункции
	
&НаКлиенте
// Служебная функция для подключения библиотеки создания fixtures.
Функция ПолучитьМакетОбработки(ИмяМакета) Экспорт
	Возврат ПолучитьМакетСервер(ИмяМакета);
КонецФункции



///////////////////////////////////////////////////
//Работа со сценариями
///////////////////////////////////////////////////

&НаКлиенте
// Функция выполняется перед началом каждого сценария
Функция ПередНачаломСценария() Экспорт
	
КонецФункции

&НаКлиенте
// Функция выполняется перед окончанием каждого сценария
Функция ПередОкончаниемСценария() Экспорт
	
КонецФункции



///////////////////////////////////////////////////
//Реализация шагов
///////////////////////////////////////////////////

&НаКлиенте
//И я регистрирую ошибку
//@ЯРегистрируюОшибкуПоДаннымИсключения()
Функция ЯРегистрируюОшибкуПоДаннымИсключения(ТекстОписаниеОшибки) Экспорт
	Ванесса.ЗарегистрироватьОшибкуПоПоследнемуУпавшемуШагуВПопыткеИсключении(ТекстОписаниеОшибки);
КонецФункции

&НаКлиенте
//И я запоминаю текст ошибки в переменную "ИмяПеременной"
//@ЯЗапоминаюТекстОшибкиВПеременную()
Функция ЯЗапоминаюТекстОшибкиВПеременную(ИмяПеременной) Экспорт
	Ванесса.СохранитьЗначениеПеременнойВКонтекст(ИмяПеременной, Ванесса.ПолучитьТекстОшибкиВозникшейВИсключении());
КонецФункции
