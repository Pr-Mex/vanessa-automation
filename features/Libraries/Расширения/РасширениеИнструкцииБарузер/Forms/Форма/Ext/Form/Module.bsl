
///////////////////////////////////////////////////
//Служебные функции и процедуры
///////////////////////////////////////////////////

&НаКлиенте
// контекст фреймворка Vanessa-Automation
Перем Ванесса;
 
&НаКлиенте
// Структура, в которой хранится состояние сценария между выполнением шагов. Очищается перед выполнением каждого сценария.
Перем Контекст Экспорт;
 
&НаКлиенте
// Структура, в которой можно хранить служебные данные между запусками сценариев. Существует, пока открыта форма Vanessa-Automation.
Перем КонтекстСохраняемый Экспорт;

// Делает отключение модуля
&НаКлиенте
Функция ОтключениеМодуля() Экспорт

	Ванесса = Неопределено;
	Контекст = Неопределено;
	КонтекстСохраняемый = Неопределено;

КонецФункции

&НаКлиенте
// Функция экспортирует список шагов, которые реализованы в данной внешней обработке.
Функция ПолучитьСписокТестов(КонтекстФреймворкаBDD) Экспорт
	Ванесса = КонтекстФреймворкаBDD;
	
	ВсеТесты = Новый Массив;

	//описание параметров
	//Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,Снипет,ИмяПроцедуры,ПредставлениеТеста,ОписаниеШага,ТипШага,Транзакция,Параметр);

	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ЯВключаяюПодсветкуАктивногоЭлементаВБраузере()","ЯВключаяюПодсветкуАктивногоЭлементаВБраузере","И я включаяю подсветку активного элемента в браузере",
		"### Описание
		|Включает подсветку элемента, с которым сейчас работает сценарий, при выполнении в браузере.
		|Подсветка нужна для видеоинструкций: в кадре видно, на какое поле или кнопку нажимает Vanessa Automation.
		|Технически шаг устанавливает константу `ДелатьПодсветку` расширения `VA_Browser` в значение `Истина`.
		|Расширение `VA_Browser` с его HTTP-сервисом должно быть подключено к базе, в которой запущен менеджер тестирования, иначе шаг завершится ошибкой.
		|Выключают подсветку шагом <И я выключаяю подсветку активного элемента в браузере>.
		|### Пример. Подсветка только на нужном фрагменте ролика
		|Заполнение поля попадёт в ролик с подсветкой, а служебные шаги после выключения — уже без неё.
		|```Gherkin
		|	И я включаяю подсветку активного элемента в браузере
		|	И в поле 'Комментарий' я ввожу текст 'Пример'
		|	И я выключаяю подсветку активного элемента в браузере
		|```
		|","");
	Ванесса.ДобавитьШагВМассивТестов(ВсеТесты,"ЯВыключаяюПодсветкуАктивногоЭлементаВБраузере()","ЯВыключаяюПодсветкуАктивногоЭлементаВБраузере","И я выключаяю подсветку активного элемента в браузере",
		"### Описание
		|Выключает подсветку активного элемента в браузере — парный шаг к <И я включаяю подсветку активного элемента в браузере>.
		|Устанавливает константу `ДелатьПодсветку` расширения `VA_Browser` в значение `Ложь`.
		|Значение константы хранится в базе и переживает перезапуск: если подсветку не выключить, она останется включённой
		|и в следующих прогонах.
		|### Пример. Выключение подсветки в конце сценария
		|```Gherkin
		|	И я включаяю подсветку активного элемента в браузере
		|	И я нажимаю на кнопку 'Провести и закрыть'
		|	И я выключаяю подсветку активного элемента в браузере
		|```
		|","");

	Возврат ВсеТесты;
КонецФункции
	
&НаСервере
// Служебная функция.
Функция ПолучитьМакетСервер(ИмяМакета)
	ОбъектСервер = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектСервер.ПолучитьМакет(ИмяМакета);
КонецФункции
	
&НаКлиенте
// Служебная функция для подключения библиотеки создания fixtures.
Функция ПолучитьМакетОбработки(ИмяМакета) Экспорт
	Возврат ПолучитьМакетСервер(ИмяМакета);
КонецФункции



///////////////////////////////////////////////////
//Работа со сценариями
///////////////////////////////////////////////////

&НаКлиенте
// Функция выполняется перед началом каждого сценария
Функция ПередНачаломСценария() Экспорт
	
КонецФункции

&НаКлиенте
// Функция выполняется перед окончанием каждого сценария
Функция ПередОкончаниемСценария() Экспорт
	
КонецФункции


&НаСервереБезКонтекста
Функция УстановитьЗначениеКонстантыДелатьПодсветку(Значение)
	Константы.ДелатьПодсветку.Установить(Значение);
КонецФункции 


///////////////////////////////////////////////////
//Реализация шагов
///////////////////////////////////////////////////

&НаКлиенте
//И я включаяю подсветку активного элемента в браузере
//@ЯВключаяюПодсветкуАктивногоЭлементаВБраузере()
Функция ЯВключаяюПодсветкуАктивногоЭлементаВБраузере() Экспорт
	УстановитьЗначениеКонстантыДелатьПодсветку(Истина);
КонецФункции

&НаКлиенте
//И я выключаяю подсветку активного элемента в браузере
//@ЯВыключаяюПодсветкуАктивногоЭлементаВБраузере()
Функция ЯВыключаяюПодсветкуАктивногоЭлементаВБраузере() Экспорт
	УстановитьЗначениеКонстантыДелатьПодсветку(Ложь);
КонецФункции
