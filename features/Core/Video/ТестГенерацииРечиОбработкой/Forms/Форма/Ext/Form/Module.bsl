&НаКлиенте
// Функция экспортирует список шагов, которые реализованы в данной внешней обработке.
Функция ПолучитьСписокТестов(КонтекстФреймворкаBDD) Экспорт
	
	Возврат Новый Массив;
	
КонецФункции
	
&НаСервере
Функция ПолучитьМакетСервер(ИмяМакета)
	ОбъектСервер = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектСервер.ПолучитьМакет(ИмяМакета);
КонецФункции

&НаКлиенте
Функция GetMp3File(Vanessa, VideoParams, Text, FileName, VoiceParams) Экспорт
	
	Если VideoParams.Свойство("ПроверитьТегиГруппы") Тогда
		
		Если VideoParams.Шаг.МассивТеговШага = Неопределено ИЛИ VideoParams.Шаг.МассивТеговШага.Количество() = 0 Тогда
			ВызватьИсключение "Ожидалось, что будут переданы теги группы.";
		КонецЕсли;	
		
		Если VideoParams.ПроверитьТегиГруппы <> VideoParams.Шаг.МассивТеговШага[0] Тогда
			ВызватьИсключение "Ожидалось, что будет передан тег: " + VideoParams.ПроверитьТегиГруппы;
		КонецЕсли;	 
		
	КонецЕсли;	 
	
	Если VideoParams.Свойство("СоздатьФайл") Тогда
		ЗТ = Новый ЗаписьТекста(VideoParams.СоздатьФайл,"UTF-8",, Ложь); 
		ЗТ.ЗаписатьСтроку("test"); 
		ЗТ.Закрыть();
	КонецЕсли;	 
	
	ДД = ПолучитьМакетСервер("TestMP3");
	ИмяФайла = ПолучитьИмяВременногоФайла("mp3");
	ДД.Записать(ИмяФайла);
	Возврат ИмяФайла; 
КонецФункции	 